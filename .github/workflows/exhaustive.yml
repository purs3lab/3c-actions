# This file is generated by generate-workflow.py. To update this file, update
# generate-workflow.py instead and re-run it. Some things in this file are
# explained by comments in generate-workflow.py.

name: Exhaustive testing and Performance Stats

on:
  # Run every day at the following time.
  schedule:
    - cron: "0 9 * * *"
  workflow_dispatch:
    inputs:
      branch:
        description: "Branch or commit ID of correctcomputation/checkedc-clang to run workflow on"
        required: true
        default: "main"

env:
  benchmark_tar_dir: "/home/github/checkedc-benchmarks"
  builddir: "${{github.workspace}}/b/ninja"
  benchmark_conv_dir: "${{github.workspace}}/benchmark_conv"
  branch_for_scheduled_run: "main"
  include_dir: "${{github.workspace}}/depsfolder/checkedc-clang/llvm/projects/checkedc-wrapper/checkedc/include"
  port_tools: "${{github.workspace}}/depsfolder/checkedc-clang/clang/tools/3c/utils/port_tools"

jobs:

  # Cleanup files left behind by prior runs
  clean:
    name: Clean
    runs-on: self-hosted
    steps:
      - name: Clean
        run: |
          rm -rf ${{env.benchmark_conv_dir}}
          mkdir -p ${{env.benchmark_conv_dir}}
          rm -rf ${{env.builddir}}
          mkdir -p ${{env.builddir}}
          rm -rf ${{github.workspace}}/depsfolder
          mkdir -p ${{github.workspace}}/depsfolder

  # Clone and build 3c and clang
  # (clang is needed to test compilation of converted benchmarks.)
  build_3c:
    name: Build 3c and clang
    needs: clean
    runs-on: self-hosted
    steps:
      - name: Check out the actions repository
        uses: actions/checkout@v2
        with:
          path: depsfolder/actions
      - name: Check that the workflow file is up to date with generate-workflow.py before running it
        run: |
          cd ${{github.workspace}}/depsfolder/actions
          ./generate-workflow.py
          git diff --exit-code

      - name: Branch or commit ID
        run: echo "${{ github.event.inputs.branch || env.branch_for_scheduled_run }}"
      - name: Check out the 3C repository and the Checked C system headers
        run: |
          git init ${{github.workspace}}/depsfolder/checkedc-clang
          cd ${{github.workspace}}/depsfolder/checkedc-clang
          git remote add origin https://github.com/correctcomputation/checkedc-clang
          git fetch --depth 1 origin "${{ github.event.inputs.branch || env.branch_for_scheduled_run }}"
          git checkout FETCH_HEAD
          # As of 2021-04-12, we're using CCI's `checkedc` repository because it
          # has a checked declaration for `syslog` that we want to use for our
          # experiments but have not yet submitted to Microsoft.
          git clone --depth 1 https://github.com/correctcomputation/checkedc ${{github.workspace}}/depsfolder/checkedc-clang/llvm/projects/checkedc-wrapper/checkedc

      - name: Build 3c and clang
        run: |
          cd ${{env.builddir}}
          # We'll be running the tools enough that it's worth spending the extra
          # time for an optimized build, and the easiest way to do that is to
          # use a "release" build. But we do want assertions and we do want
          # debug info in order to get symbols in assertion stack traces, so we
          # use -DLLVM_ENABLE_ASSERTIONS=ON and the RelWithDebInfo build type,
          # respectively. Furthermore, the tools rely on the llvm-symbolizer
          # helper program to actually read the debug info and generate the
          # symbolized stack trace when an assertion failure occurs. We could
          # build it here, but as of 2021-03-15, we just use Ubuntu's version
          # installed systemwide; it seems that llvm-symbolizer is a generic
          # tool and the difference in versions does not matter.
          cmake -G Ninja \
            -DLLVM_TARGETS_TO_BUILD=X86 \
            -DCMAKE_BUILD_TYPE="RelWithDebInfo" \
            -DLLVM_ENABLE_ASSERTIONS=ON \
            -DLLVM_OPTIMIZED_TABLEGEN=ON \
            -DLLVM_USE_SPLIT_DWARF=ON \
            -DLLVM_ENABLE_PROJECTS="clang" \
            ${{github.workspace}}/depsfolder/checkedc-clang/llvm
          ninja -l $(nproc) 3c clang
          chmod -R 777 ${{github.workspace}}/depsfolder
          chmod -R 777 ${{env.builddir}}

  # Run Test for 3C
  test_3c:
    name: 3C regression tests
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: 3C regression tests
        run: |
          cd ${{env.builddir}}
          ninja -l $(nproc) check-3c

  # Convert our benchmark programs

  test_vsftpd_no_expand_macros_no_alltypes:
    name: Test Vsftpd (not macro-expanded, no -alltypes)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Vsftpd
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes
          tar -xvzf ${{env.benchmark_tar_dir}}/vsftpd-3.0.3.tar.gz
          cd vsftpd-3.0.3
          bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang -w -ferror-limit=0 -Wno-enum-conversion"

      - name: Convert Vsftpd
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/vsftpd-3.0.3
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --project_path .

      - name: Copy 3c stats of Vsftpd
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/vsftpd-3.0.3
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of Vsftpd
        uses: actions/upload-artifact@v2
        with:
          name: Vsftpd_no_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/vsftpd-3.0.3/3c_performance_stats/
          retention-days: 5

      - name: Build converted Vsftpd
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/vsftpd-3.0.3
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang -w -ferror-limit=0 -Wno-enum-conversion" -k

  test_vsftpd_no_expand_macros_alltypes_only_g_sol:
    name: Test Vsftpd (not macro-expanded, -alltypes, greatest solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Vsftpd
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol
          tar -xvzf ${{env.benchmark_tar_dir}}/vsftpd-3.0.3.tar.gz
          cd vsftpd-3.0.3
          bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang -w -ferror-limit=0 -Wno-enum-conversion"

      - name: Convert Vsftpd
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/vsftpd-3.0.3
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of Vsftpd
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/vsftpd-3.0.3
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of Vsftpd
        uses: actions/upload-artifact@v2
        with:
          name: Vsftpd_no_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/vsftpd-3.0.3/3c_performance_stats/
          retention-days: 5

      - name: Build converted Vsftpd (filter bounds inference errors)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/vsftpd-3.0.3
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang -w -ferror-limit=0 -Wno-enum-conversion" -k 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py

  test_vsftpd_no_expand_macros_alltypes_only_l_sol:
    name: Test Vsftpd (not macro-expanded, -alltypes, least solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Vsftpd
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol
          tar -xvzf ${{env.benchmark_tar_dir}}/vsftpd-3.0.3.tar.gz
          cd vsftpd-3.0.3
          bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang -w -ferror-limit=0 -Wno-enum-conversion"

      - name: Convert Vsftpd
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/vsftpd-3.0.3
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of Vsftpd
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/vsftpd-3.0.3
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of Vsftpd
        uses: actions/upload-artifact@v2
        with:
          name: Vsftpd_no_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/vsftpd-3.0.3/3c_performance_stats/
          retention-days: 5

      - name: Build converted Vsftpd (filter bounds inference errors)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/vsftpd-3.0.3
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang -w -ferror-limit=0 -Wno-enum-conversion" -k 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py

  test_vsftpd_no_expand_macros_alltypes_disable_rds:
    name: Test Vsftpd (not macro-expanded, -alltypes, CCured solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Vsftpd
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds
          tar -xvzf ${{env.benchmark_tar_dir}}/vsftpd-3.0.3.tar.gz
          cd vsftpd-3.0.3
          bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang -w -ferror-limit=0 -Wno-enum-conversion"

      - name: Convert Vsftpd
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/vsftpd-3.0.3
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of Vsftpd
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/vsftpd-3.0.3
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of Vsftpd
        uses: actions/upload-artifact@v2
        with:
          name: Vsftpd_no_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/vsftpd-3.0.3/3c_performance_stats/
          retention-days: 5

      - name: Build converted Vsftpd (filter bounds inference errors)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/vsftpd-3.0.3
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang -w -ferror-limit=0 -Wno-enum-conversion" -k 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py

  test_vsftpd_no_expand_macros_alltypes_disable_fnedgs:
    name: Test Vsftpd (not macro-expanded, -alltypes, FuncRevEdges solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Vsftpd
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs
          tar -xvzf ${{env.benchmark_tar_dir}}/vsftpd-3.0.3.tar.gz
          cd vsftpd-3.0.3
          bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang -w -ferror-limit=0 -Wno-enum-conversion"

      - name: Convert Vsftpd
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/vsftpd-3.0.3
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of Vsftpd
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/vsftpd-3.0.3
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of Vsftpd
        uses: actions/upload-artifact@v2
        with:
          name: Vsftpd_no_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/vsftpd-3.0.3/3c_performance_stats/
          retention-days: 5

      - name: Build converted Vsftpd (filter bounds inference errors)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/vsftpd-3.0.3
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang -w -ferror-limit=0 -Wno-enum-conversion" -k 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py

  test_vsftpd_no_expand_macros_alltypes:
    name: Test Vsftpd (not macro-expanded, -alltypes)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Vsftpd
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes
          tar -xvzf ${{env.benchmark_tar_dir}}/vsftpd-3.0.3.tar.gz
          cd vsftpd-3.0.3
          bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang -w -ferror-limit=0 -Wno-enum-conversion"

      - name: Convert Vsftpd
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/vsftpd-3.0.3
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --project_path .

      - name: Copy 3c stats of Vsftpd
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/vsftpd-3.0.3
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of Vsftpd
        uses: actions/upload-artifact@v2
        with:
          name: Vsftpd_no_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/vsftpd-3.0.3/3c_performance_stats/
          retention-days: 5

      - name: Build converted Vsftpd (filter bounds inference errors)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/vsftpd-3.0.3
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang -w -ferror-limit=0 -Wno-enum-conversion" -k 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py

  test_vsftpd_expand_macros_no_alltypes:
    name: Test Vsftpd (macro-expanded, no -alltypes)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Vsftpd
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes
          tar -xvzf ${{env.benchmark_tar_dir}}/vsftpd-3.0.3.tar.gz
          cd vsftpd-3.0.3
          bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang -w -ferror-limit=0 -Wno-enum-conversion"

      - name: Convert Vsftpd
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/vsftpd-3.0.3
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of Vsftpd
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/vsftpd-3.0.3
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of Vsftpd
        uses: actions/upload-artifact@v2
        with:
          name: Vsftpd_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/vsftpd-3.0.3/3c_performance_stats/
          retention-days: 5

      - name: Build converted Vsftpd
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/vsftpd-3.0.3
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang -w -ferror-limit=0 -Wno-enum-conversion" -k

  test_vsftpd_expand_macros_alltypes_only_g_sol:
    name: Test Vsftpd (macro-expanded, -alltypes, greatest solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Vsftpd
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol
          tar -xvzf ${{env.benchmark_tar_dir}}/vsftpd-3.0.3.tar.gz
          cd vsftpd-3.0.3
          bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang -w -ferror-limit=0 -Wno-enum-conversion"

      - name: Convert Vsftpd
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/vsftpd-3.0.3
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of Vsftpd
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/vsftpd-3.0.3
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of Vsftpd
        uses: actions/upload-artifact@v2
        with:
          name: Vsftpd_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/vsftpd-3.0.3/3c_performance_stats/
          retention-days: 5

      - name: Build converted Vsftpd (filter bounds inference errors)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/vsftpd-3.0.3
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang -w -ferror-limit=0 -Wno-enum-conversion" -k 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py

  test_vsftpd_expand_macros_alltypes_only_l_sol:
    name: Test Vsftpd (macro-expanded, -alltypes, least solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Vsftpd
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol
          tar -xvzf ${{env.benchmark_tar_dir}}/vsftpd-3.0.3.tar.gz
          cd vsftpd-3.0.3
          bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang -w -ferror-limit=0 -Wno-enum-conversion"

      - name: Convert Vsftpd
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/vsftpd-3.0.3
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of Vsftpd
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/vsftpd-3.0.3
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of Vsftpd
        uses: actions/upload-artifact@v2
        with:
          name: Vsftpd_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/vsftpd-3.0.3/3c_performance_stats/
          retention-days: 5

      - name: Build converted Vsftpd (filter bounds inference errors)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/vsftpd-3.0.3
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang -w -ferror-limit=0 -Wno-enum-conversion" -k 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py

  test_vsftpd_expand_macros_alltypes_disable_rds:
    name: Test Vsftpd (macro-expanded, -alltypes, CCured solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Vsftpd
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds
          tar -xvzf ${{env.benchmark_tar_dir}}/vsftpd-3.0.3.tar.gz
          cd vsftpd-3.0.3
          bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang -w -ferror-limit=0 -Wno-enum-conversion"

      - name: Convert Vsftpd
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/vsftpd-3.0.3
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of Vsftpd
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/vsftpd-3.0.3
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of Vsftpd
        uses: actions/upload-artifact@v2
        with:
          name: Vsftpd_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/vsftpd-3.0.3/3c_performance_stats/
          retention-days: 5

      - name: Build converted Vsftpd (filter bounds inference errors)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/vsftpd-3.0.3
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang -w -ferror-limit=0 -Wno-enum-conversion" -k 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py

  test_vsftpd_expand_macros_alltypes_disable_fnedgs:
    name: Test Vsftpd (macro-expanded, -alltypes, FuncRevEdges solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Vsftpd
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs
          tar -xvzf ${{env.benchmark_tar_dir}}/vsftpd-3.0.3.tar.gz
          cd vsftpd-3.0.3
          bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang -w -ferror-limit=0 -Wno-enum-conversion"

      - name: Convert Vsftpd
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/vsftpd-3.0.3
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of Vsftpd
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/vsftpd-3.0.3
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of Vsftpd
        uses: actions/upload-artifact@v2
        with:
          name: Vsftpd_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/vsftpd-3.0.3/3c_performance_stats/
          retention-days: 5

      - name: Build converted Vsftpd (filter bounds inference errors)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/vsftpd-3.0.3
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang -w -ferror-limit=0 -Wno-enum-conversion" -k 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py

  test_vsftpd_expand_macros_alltypes:
    name: Test Vsftpd (macro-expanded, -alltypes)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Vsftpd
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/expand_macros_alltypes
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes
          tar -xvzf ${{env.benchmark_tar_dir}}/vsftpd-3.0.3.tar.gz
          cd vsftpd-3.0.3
          bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang -w -ferror-limit=0 -Wno-enum-conversion"

      - name: Convert Vsftpd
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/vsftpd-3.0.3
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of Vsftpd
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/vsftpd-3.0.3
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of Vsftpd
        uses: actions/upload-artifact@v2
        with:
          name: Vsftpd_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes/vsftpd-3.0.3/3c_performance_stats/
          retention-days: 5

      - name: Build converted Vsftpd (filter bounds inference errors)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/vsftpd-3.0.3
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang -w -ferror-limit=0 -Wno-enum-conversion" -k 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py

  test_vsftpd-reverted_no_expand_macros_no_alltypes:
    name: Test Vsftpd-Reverted (not macro-expanded, no -alltypes)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Vsftpd-Reverted
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes
          tar -xvzf ${{env.benchmark_tar_dir}}/vsftpd-3.0.3-reverted.tar.gz
          cd vsftpd-3.0.3-reverted
          bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang -w -ferror-limit=0 -Wno-enum-conversion"

      - name: Convert Vsftpd-Reverted
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/vsftpd-3.0.3-reverted
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --project_path .

      - name: Copy 3c stats of Vsftpd-Reverted
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/vsftpd-3.0.3-reverted
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of Vsftpd-Reverted
        uses: actions/upload-artifact@v2
        with:
          name: Vsftpd-Reverted_no_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/vsftpd-3.0.3-reverted/3c_performance_stats/
          retention-days: 5

      - name: Build converted Vsftpd-Reverted
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/vsftpd-3.0.3-reverted
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang -w -ferror-limit=0 -Wno-enum-conversion" -k

  test_vsftpd-reverted_no_expand_macros_alltypes_only_g_sol:
    name: Test Vsftpd-Reverted (not macro-expanded, -alltypes, greatest solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Vsftpd-Reverted
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol
          tar -xvzf ${{env.benchmark_tar_dir}}/vsftpd-3.0.3-reverted.tar.gz
          cd vsftpd-3.0.3-reverted
          bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang -w -ferror-limit=0 -Wno-enum-conversion"

      - name: Convert Vsftpd-Reverted
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/vsftpd-3.0.3-reverted
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of Vsftpd-Reverted
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/vsftpd-3.0.3-reverted
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of Vsftpd-Reverted
        uses: actions/upload-artifact@v2
        with:
          name: Vsftpd-Reverted_no_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/vsftpd-3.0.3-reverted/3c_performance_stats/
          retention-days: 5

      - name: Build converted Vsftpd-Reverted (filter bounds inference errors)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/vsftpd-3.0.3-reverted
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang -w -ferror-limit=0 -Wno-enum-conversion" -k 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py

  test_vsftpd-reverted_no_expand_macros_alltypes_only_l_sol:
    name: Test Vsftpd-Reverted (not macro-expanded, -alltypes, least solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Vsftpd-Reverted
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol
          tar -xvzf ${{env.benchmark_tar_dir}}/vsftpd-3.0.3-reverted.tar.gz
          cd vsftpd-3.0.3-reverted
          bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang -w -ferror-limit=0 -Wno-enum-conversion"

      - name: Convert Vsftpd-Reverted
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/vsftpd-3.0.3-reverted
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of Vsftpd-Reverted
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/vsftpd-3.0.3-reverted
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of Vsftpd-Reverted
        uses: actions/upload-artifact@v2
        with:
          name: Vsftpd-Reverted_no_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/vsftpd-3.0.3-reverted/3c_performance_stats/
          retention-days: 5

      - name: Build converted Vsftpd-Reverted (filter bounds inference errors)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/vsftpd-3.0.3-reverted
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang -w -ferror-limit=0 -Wno-enum-conversion" -k 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py

  test_vsftpd-reverted_no_expand_macros_alltypes_disable_rds:
    name: Test Vsftpd-Reverted (not macro-expanded, -alltypes, CCured solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Vsftpd-Reverted
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds
          tar -xvzf ${{env.benchmark_tar_dir}}/vsftpd-3.0.3-reverted.tar.gz
          cd vsftpd-3.0.3-reverted
          bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang -w -ferror-limit=0 -Wno-enum-conversion"

      - name: Convert Vsftpd-Reverted
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/vsftpd-3.0.3-reverted
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of Vsftpd-Reverted
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/vsftpd-3.0.3-reverted
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of Vsftpd-Reverted
        uses: actions/upload-artifact@v2
        with:
          name: Vsftpd-Reverted_no_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/vsftpd-3.0.3-reverted/3c_performance_stats/
          retention-days: 5

      - name: Build converted Vsftpd-Reverted (filter bounds inference errors)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/vsftpd-3.0.3-reverted
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang -w -ferror-limit=0 -Wno-enum-conversion" -k 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py

  test_vsftpd-reverted_no_expand_macros_alltypes_disable_fnedgs:
    name: Test Vsftpd-Reverted (not macro-expanded, -alltypes, FuncRevEdges solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Vsftpd-Reverted
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs
          tar -xvzf ${{env.benchmark_tar_dir}}/vsftpd-3.0.3-reverted.tar.gz
          cd vsftpd-3.0.3-reverted
          bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang -w -ferror-limit=0 -Wno-enum-conversion"

      - name: Convert Vsftpd-Reverted
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/vsftpd-3.0.3-reverted
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of Vsftpd-Reverted
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/vsftpd-3.0.3-reverted
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of Vsftpd-Reverted
        uses: actions/upload-artifact@v2
        with:
          name: Vsftpd-Reverted_no_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/vsftpd-3.0.3-reverted/3c_performance_stats/
          retention-days: 5

      - name: Build converted Vsftpd-Reverted (filter bounds inference errors)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/vsftpd-3.0.3-reverted
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang -w -ferror-limit=0 -Wno-enum-conversion" -k 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py

  test_vsftpd-reverted_no_expand_macros_alltypes:
    name: Test Vsftpd-Reverted (not macro-expanded, -alltypes)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Vsftpd-Reverted
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes
          tar -xvzf ${{env.benchmark_tar_dir}}/vsftpd-3.0.3-reverted.tar.gz
          cd vsftpd-3.0.3-reverted
          bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang -w -ferror-limit=0 -Wno-enum-conversion"

      - name: Convert Vsftpd-Reverted
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/vsftpd-3.0.3-reverted
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --project_path .

      - name: Copy 3c stats of Vsftpd-Reverted
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/vsftpd-3.0.3-reverted
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of Vsftpd-Reverted
        uses: actions/upload-artifact@v2
        with:
          name: Vsftpd-Reverted_no_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/vsftpd-3.0.3-reverted/3c_performance_stats/
          retention-days: 5

      - name: Build converted Vsftpd-Reverted (filter bounds inference errors)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/vsftpd-3.0.3-reverted
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang -w -ferror-limit=0 -Wno-enum-conversion" -k 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py

  test_vsftpd-reverted_expand_macros_no_alltypes:
    name: Test Vsftpd-Reverted (macro-expanded, no -alltypes)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Vsftpd-Reverted
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes
          tar -xvzf ${{env.benchmark_tar_dir}}/vsftpd-3.0.3-reverted.tar.gz
          cd vsftpd-3.0.3-reverted
          bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang -w -ferror-limit=0 -Wno-enum-conversion"

      - name: Convert Vsftpd-Reverted
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/vsftpd-3.0.3-reverted
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of Vsftpd-Reverted
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/vsftpd-3.0.3-reverted
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of Vsftpd-Reverted
        uses: actions/upload-artifact@v2
        with:
          name: Vsftpd-Reverted_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/vsftpd-3.0.3-reverted/3c_performance_stats/
          retention-days: 5

      - name: Build converted Vsftpd-Reverted
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/vsftpd-3.0.3-reverted
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang -w -ferror-limit=0 -Wno-enum-conversion" -k

  test_vsftpd-reverted_expand_macros_alltypes_only_g_sol:
    name: Test Vsftpd-Reverted (macro-expanded, -alltypes, greatest solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Vsftpd-Reverted
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol
          tar -xvzf ${{env.benchmark_tar_dir}}/vsftpd-3.0.3-reverted.tar.gz
          cd vsftpd-3.0.3-reverted
          bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang -w -ferror-limit=0 -Wno-enum-conversion"

      - name: Convert Vsftpd-Reverted
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/vsftpd-3.0.3-reverted
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of Vsftpd-Reverted
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/vsftpd-3.0.3-reverted
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of Vsftpd-Reverted
        uses: actions/upload-artifact@v2
        with:
          name: Vsftpd-Reverted_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/vsftpd-3.0.3-reverted/3c_performance_stats/
          retention-days: 5

      - name: Build converted Vsftpd-Reverted (filter bounds inference errors)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/vsftpd-3.0.3-reverted
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang -w -ferror-limit=0 -Wno-enum-conversion" -k 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py

  test_vsftpd-reverted_expand_macros_alltypes_only_l_sol:
    name: Test Vsftpd-Reverted (macro-expanded, -alltypes, least solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Vsftpd-Reverted
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol
          tar -xvzf ${{env.benchmark_tar_dir}}/vsftpd-3.0.3-reverted.tar.gz
          cd vsftpd-3.0.3-reverted
          bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang -w -ferror-limit=0 -Wno-enum-conversion"

      - name: Convert Vsftpd-Reverted
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/vsftpd-3.0.3-reverted
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of Vsftpd-Reverted
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/vsftpd-3.0.3-reverted
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of Vsftpd-Reverted
        uses: actions/upload-artifact@v2
        with:
          name: Vsftpd-Reverted_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/vsftpd-3.0.3-reverted/3c_performance_stats/
          retention-days: 5

      - name: Build converted Vsftpd-Reverted (filter bounds inference errors)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/vsftpd-3.0.3-reverted
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang -w -ferror-limit=0 -Wno-enum-conversion" -k 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py

  test_vsftpd-reverted_expand_macros_alltypes_disable_rds:
    name: Test Vsftpd-Reverted (macro-expanded, -alltypes, CCured solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Vsftpd-Reverted
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds
          tar -xvzf ${{env.benchmark_tar_dir}}/vsftpd-3.0.3-reverted.tar.gz
          cd vsftpd-3.0.3-reverted
          bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang -w -ferror-limit=0 -Wno-enum-conversion"

      - name: Convert Vsftpd-Reverted
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/vsftpd-3.0.3-reverted
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of Vsftpd-Reverted
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/vsftpd-3.0.3-reverted
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of Vsftpd-Reverted
        uses: actions/upload-artifact@v2
        with:
          name: Vsftpd-Reverted_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/vsftpd-3.0.3-reverted/3c_performance_stats/
          retention-days: 5

      - name: Build converted Vsftpd-Reverted (filter bounds inference errors)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/vsftpd-3.0.3-reverted
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang -w -ferror-limit=0 -Wno-enum-conversion" -k 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py

  test_vsftpd-reverted_expand_macros_alltypes_disable_fnedgs:
    name: Test Vsftpd-Reverted (macro-expanded, -alltypes, FuncRevEdges solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Vsftpd-Reverted
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs
          tar -xvzf ${{env.benchmark_tar_dir}}/vsftpd-3.0.3-reverted.tar.gz
          cd vsftpd-3.0.3-reverted
          bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang -w -ferror-limit=0 -Wno-enum-conversion"

      - name: Convert Vsftpd-Reverted
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/vsftpd-3.0.3-reverted
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of Vsftpd-Reverted
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/vsftpd-3.0.3-reverted
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of Vsftpd-Reverted
        uses: actions/upload-artifact@v2
        with:
          name: Vsftpd-Reverted_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/vsftpd-3.0.3-reverted/3c_performance_stats/
          retention-days: 5

      - name: Build converted Vsftpd-Reverted (filter bounds inference errors)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/vsftpd-3.0.3-reverted
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang -w -ferror-limit=0 -Wno-enum-conversion" -k 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py

  test_vsftpd-reverted_expand_macros_alltypes:
    name: Test Vsftpd-Reverted (macro-expanded, -alltypes)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Vsftpd-Reverted
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/expand_macros_alltypes
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes
          tar -xvzf ${{env.benchmark_tar_dir}}/vsftpd-3.0.3-reverted.tar.gz
          cd vsftpd-3.0.3-reverted
          bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang -w -ferror-limit=0 -Wno-enum-conversion"

      - name: Convert Vsftpd-Reverted
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/vsftpd-3.0.3-reverted
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of Vsftpd-Reverted
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/vsftpd-3.0.3-reverted
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of Vsftpd-Reverted
        uses: actions/upload-artifact@v2
        with:
          name: Vsftpd-Reverted_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes/vsftpd-3.0.3-reverted/3c_performance_stats/
          retention-days: 5

      - name: Build converted Vsftpd-Reverted (filter bounds inference errors)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/vsftpd-3.0.3-reverted
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang -w -ferror-limit=0 -Wno-enum-conversion" -k 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py

  test_vsftpd-manual_no_expand_macros_no_alltypes:
    name: Test Vsftpd-Manually-Converted (not macro-expanded, no -alltypes)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Vsftpd-Manually-Converted
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes
          tar -xvzf ${{env.benchmark_tar_dir}}/vsftpd-3.0.3-manual.tar.gz
          cd vsftpd-3.0.3-manual
          bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang -w -ferror-limit=0 -Wno-enum-conversion"

      - name: Convert Vsftpd-Manually-Converted
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/vsftpd-3.0.3-manual
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --project_path .

      - name: Copy 3c stats of Vsftpd-Manually-Converted
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/vsftpd-3.0.3-manual
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of Vsftpd-Manually-Converted
        uses: actions/upload-artifact@v2
        with:
          name: Vsftpd-Manually-Converted_no_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/vsftpd-3.0.3-manual/3c_performance_stats/
          retention-days: 5

      - name: Build converted Vsftpd-Manually-Converted
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/vsftpd-3.0.3-manual
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang -w -ferror-limit=0 -Wno-enum-conversion" -k

  test_vsftpd-manual_no_expand_macros_alltypes_only_g_sol:
    name: Test Vsftpd-Manually-Converted (not macro-expanded, -alltypes, greatest solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Vsftpd-Manually-Converted
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol
          tar -xvzf ${{env.benchmark_tar_dir}}/vsftpd-3.0.3-manual.tar.gz
          cd vsftpd-3.0.3-manual
          bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang -w -ferror-limit=0 -Wno-enum-conversion"

      - name: Convert Vsftpd-Manually-Converted
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/vsftpd-3.0.3-manual
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of Vsftpd-Manually-Converted
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/vsftpd-3.0.3-manual
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of Vsftpd-Manually-Converted
        uses: actions/upload-artifact@v2
        with:
          name: Vsftpd-Manually-Converted_no_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/vsftpd-3.0.3-manual/3c_performance_stats/
          retention-days: 5

      - name: Build converted Vsftpd-Manually-Converted (filter bounds inference errors)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/vsftpd-3.0.3-manual
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang -w -ferror-limit=0 -Wno-enum-conversion" -k 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py

  test_vsftpd-manual_no_expand_macros_alltypes_only_l_sol:
    name: Test Vsftpd-Manually-Converted (not macro-expanded, -alltypes, least solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Vsftpd-Manually-Converted
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol
          tar -xvzf ${{env.benchmark_tar_dir}}/vsftpd-3.0.3-manual.tar.gz
          cd vsftpd-3.0.3-manual
          bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang -w -ferror-limit=0 -Wno-enum-conversion"

      - name: Convert Vsftpd-Manually-Converted
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/vsftpd-3.0.3-manual
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of Vsftpd-Manually-Converted
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/vsftpd-3.0.3-manual
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of Vsftpd-Manually-Converted
        uses: actions/upload-artifact@v2
        with:
          name: Vsftpd-Manually-Converted_no_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/vsftpd-3.0.3-manual/3c_performance_stats/
          retention-days: 5

      - name: Build converted Vsftpd-Manually-Converted (filter bounds inference errors)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/vsftpd-3.0.3-manual
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang -w -ferror-limit=0 -Wno-enum-conversion" -k 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py

  test_vsftpd-manual_no_expand_macros_alltypes_disable_rds:
    name: Test Vsftpd-Manually-Converted (not macro-expanded, -alltypes, CCured solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Vsftpd-Manually-Converted
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds
          tar -xvzf ${{env.benchmark_tar_dir}}/vsftpd-3.0.3-manual.tar.gz
          cd vsftpd-3.0.3-manual
          bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang -w -ferror-limit=0 -Wno-enum-conversion"

      - name: Convert Vsftpd-Manually-Converted
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/vsftpd-3.0.3-manual
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of Vsftpd-Manually-Converted
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/vsftpd-3.0.3-manual
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of Vsftpd-Manually-Converted
        uses: actions/upload-artifact@v2
        with:
          name: Vsftpd-Manually-Converted_no_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/vsftpd-3.0.3-manual/3c_performance_stats/
          retention-days: 5

      - name: Build converted Vsftpd-Manually-Converted (filter bounds inference errors)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/vsftpd-3.0.3-manual
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang -w -ferror-limit=0 -Wno-enum-conversion" -k 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py

  test_vsftpd-manual_no_expand_macros_alltypes_disable_fnedgs:
    name: Test Vsftpd-Manually-Converted (not macro-expanded, -alltypes, FuncRevEdges solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Vsftpd-Manually-Converted
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs
          tar -xvzf ${{env.benchmark_tar_dir}}/vsftpd-3.0.3-manual.tar.gz
          cd vsftpd-3.0.3-manual
          bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang -w -ferror-limit=0 -Wno-enum-conversion"

      - name: Convert Vsftpd-Manually-Converted
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/vsftpd-3.0.3-manual
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of Vsftpd-Manually-Converted
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/vsftpd-3.0.3-manual
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of Vsftpd-Manually-Converted
        uses: actions/upload-artifact@v2
        with:
          name: Vsftpd-Manually-Converted_no_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/vsftpd-3.0.3-manual/3c_performance_stats/
          retention-days: 5

      - name: Build converted Vsftpd-Manually-Converted (filter bounds inference errors)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/vsftpd-3.0.3-manual
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang -w -ferror-limit=0 -Wno-enum-conversion" -k 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py

  test_vsftpd-manual_no_expand_macros_alltypes:
    name: Test Vsftpd-Manually-Converted (not macro-expanded, -alltypes)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Vsftpd-Manually-Converted
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes
          tar -xvzf ${{env.benchmark_tar_dir}}/vsftpd-3.0.3-manual.tar.gz
          cd vsftpd-3.0.3-manual
          bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang -w -ferror-limit=0 -Wno-enum-conversion"

      - name: Convert Vsftpd-Manually-Converted
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/vsftpd-3.0.3-manual
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --project_path .

      - name: Copy 3c stats of Vsftpd-Manually-Converted
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/vsftpd-3.0.3-manual
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of Vsftpd-Manually-Converted
        uses: actions/upload-artifact@v2
        with:
          name: Vsftpd-Manually-Converted_no_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/vsftpd-3.0.3-manual/3c_performance_stats/
          retention-days: 5

      - name: Build converted Vsftpd-Manually-Converted (filter bounds inference errors)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/vsftpd-3.0.3-manual
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang -w -ferror-limit=0 -Wno-enum-conversion" -k 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py

  test_vsftpd-manual_expand_macros_no_alltypes:
    name: Test Vsftpd-Manually-Converted (macro-expanded, no -alltypes)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Vsftpd-Manually-Converted
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes
          tar -xvzf ${{env.benchmark_tar_dir}}/vsftpd-3.0.3-manual.tar.gz
          cd vsftpd-3.0.3-manual
          bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang -w -ferror-limit=0 -Wno-enum-conversion"

      - name: Convert Vsftpd-Manually-Converted
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/vsftpd-3.0.3-manual
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of Vsftpd-Manually-Converted
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/vsftpd-3.0.3-manual
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of Vsftpd-Manually-Converted
        uses: actions/upload-artifact@v2
        with:
          name: Vsftpd-Manually-Converted_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/vsftpd-3.0.3-manual/3c_performance_stats/
          retention-days: 5

      - name: Build converted Vsftpd-Manually-Converted
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/vsftpd-3.0.3-manual
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang -w -ferror-limit=0 -Wno-enum-conversion" -k

  test_vsftpd-manual_expand_macros_alltypes_only_g_sol:
    name: Test Vsftpd-Manually-Converted (macro-expanded, -alltypes, greatest solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Vsftpd-Manually-Converted
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol
          tar -xvzf ${{env.benchmark_tar_dir}}/vsftpd-3.0.3-manual.tar.gz
          cd vsftpd-3.0.3-manual
          bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang -w -ferror-limit=0 -Wno-enum-conversion"

      - name: Convert Vsftpd-Manually-Converted
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/vsftpd-3.0.3-manual
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of Vsftpd-Manually-Converted
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/vsftpd-3.0.3-manual
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of Vsftpd-Manually-Converted
        uses: actions/upload-artifact@v2
        with:
          name: Vsftpd-Manually-Converted_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/vsftpd-3.0.3-manual/3c_performance_stats/
          retention-days: 5

      - name: Build converted Vsftpd-Manually-Converted (filter bounds inference errors)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/vsftpd-3.0.3-manual
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang -w -ferror-limit=0 -Wno-enum-conversion" -k 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py

  test_vsftpd-manual_expand_macros_alltypes_only_l_sol:
    name: Test Vsftpd-Manually-Converted (macro-expanded, -alltypes, least solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Vsftpd-Manually-Converted
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol
          tar -xvzf ${{env.benchmark_tar_dir}}/vsftpd-3.0.3-manual.tar.gz
          cd vsftpd-3.0.3-manual
          bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang -w -ferror-limit=0 -Wno-enum-conversion"

      - name: Convert Vsftpd-Manually-Converted
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/vsftpd-3.0.3-manual
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of Vsftpd-Manually-Converted
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/vsftpd-3.0.3-manual
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of Vsftpd-Manually-Converted
        uses: actions/upload-artifact@v2
        with:
          name: Vsftpd-Manually-Converted_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/vsftpd-3.0.3-manual/3c_performance_stats/
          retention-days: 5

      - name: Build converted Vsftpd-Manually-Converted (filter bounds inference errors)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/vsftpd-3.0.3-manual
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang -w -ferror-limit=0 -Wno-enum-conversion" -k 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py

  test_vsftpd-manual_expand_macros_alltypes_disable_rds:
    name: Test Vsftpd-Manually-Converted (macro-expanded, -alltypes, CCured solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Vsftpd-Manually-Converted
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds
          tar -xvzf ${{env.benchmark_tar_dir}}/vsftpd-3.0.3-manual.tar.gz
          cd vsftpd-3.0.3-manual
          bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang -w -ferror-limit=0 -Wno-enum-conversion"

      - name: Convert Vsftpd-Manually-Converted
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/vsftpd-3.0.3-manual
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of Vsftpd-Manually-Converted
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/vsftpd-3.0.3-manual
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of Vsftpd-Manually-Converted
        uses: actions/upload-artifact@v2
        with:
          name: Vsftpd-Manually-Converted_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/vsftpd-3.0.3-manual/3c_performance_stats/
          retention-days: 5

      - name: Build converted Vsftpd-Manually-Converted (filter bounds inference errors)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/vsftpd-3.0.3-manual
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang -w -ferror-limit=0 -Wno-enum-conversion" -k 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py

  test_vsftpd-manual_expand_macros_alltypes_disable_fnedgs:
    name: Test Vsftpd-Manually-Converted (macro-expanded, -alltypes, FuncRevEdges solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Vsftpd-Manually-Converted
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs
          tar -xvzf ${{env.benchmark_tar_dir}}/vsftpd-3.0.3-manual.tar.gz
          cd vsftpd-3.0.3-manual
          bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang -w -ferror-limit=0 -Wno-enum-conversion"

      - name: Convert Vsftpd-Manually-Converted
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/vsftpd-3.0.3-manual
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of Vsftpd-Manually-Converted
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/vsftpd-3.0.3-manual
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of Vsftpd-Manually-Converted
        uses: actions/upload-artifact@v2
        with:
          name: Vsftpd-Manually-Converted_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/vsftpd-3.0.3-manual/3c_performance_stats/
          retention-days: 5

      - name: Build converted Vsftpd-Manually-Converted (filter bounds inference errors)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/vsftpd-3.0.3-manual
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang -w -ferror-limit=0 -Wno-enum-conversion" -k 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py

  test_vsftpd-manual_expand_macros_alltypes:
    name: Test Vsftpd-Manually-Converted (macro-expanded, -alltypes)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Vsftpd-Manually-Converted
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/expand_macros_alltypes
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes
          tar -xvzf ${{env.benchmark_tar_dir}}/vsftpd-3.0.3-manual.tar.gz
          cd vsftpd-3.0.3-manual
          bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang -w -ferror-limit=0 -Wno-enum-conversion"

      - name: Convert Vsftpd-Manually-Converted
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/vsftpd-3.0.3-manual
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of Vsftpd-Manually-Converted
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/vsftpd-3.0.3-manual
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of Vsftpd-Manually-Converted
        uses: actions/upload-artifact@v2
        with:
          name: Vsftpd-Manually-Converted_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes/vsftpd-3.0.3-manual/3c_performance_stats/
          retention-days: 5

      - name: Build converted Vsftpd-Manually-Converted (filter bounds inference errors)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/vsftpd-3.0.3-manual
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang -w -ferror-limit=0 -Wno-enum-conversion" -k 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py

  test_Olden_no_expand_macros_no_alltypes:
    name: Test Olden (not macro-expanded, no -alltypes)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Olden
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes
          tar -xvzf ${{env.benchmark_tar_dir}}/Olden.tar.gz
          cd Olden
          for i in bh bisort em3d health mst perimeter power treeadd tsp voronoi ; do \
            (cd $i ; bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE") \
          done

      - name: Convert bh
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden/bh
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --project_path .

      - name: Copy 3c stats of bh
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden/bh
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of bh
        uses: actions/upload-artifact@v2
        with:
          name: bh_no_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden/bh/3c_performance_stats/
          retention-days: 5

      - name: Build converted bh (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden/bh
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" || echo bh >>${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden/failed-components-list.txt

      - name: Convert bisort
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden/bisort
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --project_path .

      - name: Copy 3c stats of bisort
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden/bisort
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of bisort
        uses: actions/upload-artifact@v2
        with:
          name: bisort_no_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden/bisort/3c_performance_stats/
          retention-days: 5

      - name: Build converted bisort (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden/bisort
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" || echo bisort >>${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden/failed-components-list.txt

      - name: Convert em3d
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden/em3d
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --project_path .

      - name: Copy 3c stats of em3d
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden/em3d
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of em3d
        uses: actions/upload-artifact@v2
        with:
          name: em3d_no_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden/em3d/3c_performance_stats/
          retention-days: 5

      - name: Build converted em3d (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden/em3d
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" || echo em3d >>${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden/failed-components-list.txt

      - name: Convert health
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden/health
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --project_path .

      - name: Copy 3c stats of health
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden/health
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of health
        uses: actions/upload-artifact@v2
        with:
          name: health_no_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden/health/3c_performance_stats/
          retention-days: 5

      - name: Build converted health (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden/health
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" || echo health >>${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden/failed-components-list.txt

      - name: Convert mst
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden/mst
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --project_path .

      - name: Copy 3c stats of mst
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden/mst
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of mst
        uses: actions/upload-artifact@v2
        with:
          name: mst_no_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden/mst/3c_performance_stats/
          retention-days: 5

      - name: Build converted mst (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden/mst
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" || echo mst >>${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden/failed-components-list.txt

      - name: Convert perimeter
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden/perimeter
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --project_path .

      - name: Copy 3c stats of perimeter
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden/perimeter
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of perimeter
        uses: actions/upload-artifact@v2
        with:
          name: perimeter_no_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden/perimeter/3c_performance_stats/
          retention-days: 5

      - name: Build converted perimeter (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden/perimeter
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" || echo perimeter >>${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden/failed-components-list.txt

      - name: Convert power
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden/power
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --project_path .

      - name: Copy 3c stats of power
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden/power
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of power
        uses: actions/upload-artifact@v2
        with:
          name: power_no_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden/power/3c_performance_stats/
          retention-days: 5

      - name: Build converted power (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden/power
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" || echo power >>${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden/failed-components-list.txt

      - name: Convert treeadd
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden/treeadd
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --project_path .

      - name: Copy 3c stats of treeadd
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden/treeadd
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of treeadd
        uses: actions/upload-artifact@v2
        with:
          name: treeadd_no_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden/treeadd/3c_performance_stats/
          retention-days: 5

      - name: Build converted treeadd (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden/treeadd
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" || echo treeadd >>${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden/failed-components-list.txt

      - name: Convert tsp
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden/tsp
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --project_path .

      - name: Copy 3c stats of tsp
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden/tsp
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of tsp
        uses: actions/upload-artifact@v2
        with:
          name: tsp_no_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden/tsp/3c_performance_stats/
          retention-days: 5

      - name: Build converted tsp (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden/tsp
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" || echo tsp >>${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden/failed-components-list.txt

      - name: Convert voronoi
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden/voronoi
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --project_path .

      - name: Copy 3c stats of voronoi
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden/voronoi
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of voronoi
        uses: actions/upload-artifact@v2
        with:
          name: voronoi_no_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden/voronoi/3c_performance_stats/
          retention-days: 5

      - name: Build converted voronoi (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden/voronoi
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" || echo voronoi >>${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden/failed-components-list.txt

      - name: Check for deferred post-conversion build failures
        run: |
          if [ -e ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden/failed-components-list.txt ]; then
              echo 'Failed components (see previous post-conversion build steps):'
              cat ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden/failed-components-list.txt
              exit 1
          fi

  test_Olden_no_expand_macros_alltypes_only_g_sol:
    name: Test Olden (not macro-expanded, -alltypes, greatest solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Olden
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol
          tar -xvzf ${{env.benchmark_tar_dir}}/Olden.tar.gz
          cd Olden
          for i in bh bisort em3d health mst perimeter power treeadd tsp voronoi ; do \
            (cd $i ; bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE") \
          done

      - name: Convert bh
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden/bh
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of bh
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden/bh
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of bh
        uses: actions/upload-artifact@v2
        with:
          name: bh_no_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden/bh/3c_performance_stats/
          retention-days: 5

      - name: Build converted bh (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden/bh
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo bh >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden/failed-components-list.txt

      - name: Convert bisort
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden/bisort
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of bisort
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden/bisort
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of bisort
        uses: actions/upload-artifact@v2
        with:
          name: bisort_no_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden/bisort/3c_performance_stats/
          retention-days: 5

      - name: Build converted bisort (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden/bisort
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo bisort >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden/failed-components-list.txt

      - name: Convert em3d
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden/em3d
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of em3d
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden/em3d
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of em3d
        uses: actions/upload-artifact@v2
        with:
          name: em3d_no_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden/em3d/3c_performance_stats/
          retention-days: 5

      - name: Build converted em3d (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden/em3d
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo em3d >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden/failed-components-list.txt

      - name: Convert health
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden/health
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of health
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden/health
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of health
        uses: actions/upload-artifact@v2
        with:
          name: health_no_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden/health/3c_performance_stats/
          retention-days: 5

      - name: Build converted health (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden/health
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo health >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden/failed-components-list.txt

      - name: Convert mst
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden/mst
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of mst
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden/mst
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of mst
        uses: actions/upload-artifact@v2
        with:
          name: mst_no_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden/mst/3c_performance_stats/
          retention-days: 5

      - name: Build converted mst (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden/mst
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo mst >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden/failed-components-list.txt

      - name: Convert perimeter
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden/perimeter
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of perimeter
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden/perimeter
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of perimeter
        uses: actions/upload-artifact@v2
        with:
          name: perimeter_no_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden/perimeter/3c_performance_stats/
          retention-days: 5

      - name: Build converted perimeter (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden/perimeter
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo perimeter >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden/failed-components-list.txt

      - name: Convert power
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden/power
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of power
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden/power
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of power
        uses: actions/upload-artifact@v2
        with:
          name: power_no_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden/power/3c_performance_stats/
          retention-days: 5

      - name: Build converted power (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden/power
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo power >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden/failed-components-list.txt

      - name: Convert treeadd
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden/treeadd
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of treeadd
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden/treeadd
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of treeadd
        uses: actions/upload-artifact@v2
        with:
          name: treeadd_no_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden/treeadd/3c_performance_stats/
          retention-days: 5

      - name: Build converted treeadd (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden/treeadd
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo treeadd >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden/failed-components-list.txt

      - name: Convert tsp
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden/tsp
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of tsp
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden/tsp
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of tsp
        uses: actions/upload-artifact@v2
        with:
          name: tsp_no_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden/tsp/3c_performance_stats/
          retention-days: 5

      - name: Build converted tsp (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden/tsp
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo tsp >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden/failed-components-list.txt

      - name: Convert voronoi
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden/voronoi
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of voronoi
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden/voronoi
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of voronoi
        uses: actions/upload-artifact@v2
        with:
          name: voronoi_no_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden/voronoi/3c_performance_stats/
          retention-days: 5

      - name: Build converted voronoi (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden/voronoi
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo voronoi >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden/failed-components-list.txt

      - name: Check for deferred post-conversion build failures
        run: |
          if [ -e ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden/failed-components-list.txt ]; then
              echo 'Failed components (see previous post-conversion build steps):'
              cat ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden/failed-components-list.txt
              exit 1
          fi

  test_Olden_no_expand_macros_alltypes_only_l_sol:
    name: Test Olden (not macro-expanded, -alltypes, least solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Olden
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol
          tar -xvzf ${{env.benchmark_tar_dir}}/Olden.tar.gz
          cd Olden
          for i in bh bisort em3d health mst perimeter power treeadd tsp voronoi ; do \
            (cd $i ; bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE") \
          done

      - name: Convert bh
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden/bh
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of bh
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden/bh
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of bh
        uses: actions/upload-artifact@v2
        with:
          name: bh_no_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden/bh/3c_performance_stats/
          retention-days: 5

      - name: Build converted bh (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden/bh
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo bh >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden/failed-components-list.txt

      - name: Convert bisort
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden/bisort
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of bisort
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden/bisort
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of bisort
        uses: actions/upload-artifact@v2
        with:
          name: bisort_no_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden/bisort/3c_performance_stats/
          retention-days: 5

      - name: Build converted bisort (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden/bisort
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo bisort >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden/failed-components-list.txt

      - name: Convert em3d
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden/em3d
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of em3d
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden/em3d
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of em3d
        uses: actions/upload-artifact@v2
        with:
          name: em3d_no_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden/em3d/3c_performance_stats/
          retention-days: 5

      - name: Build converted em3d (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden/em3d
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo em3d >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden/failed-components-list.txt

      - name: Convert health
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden/health
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of health
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden/health
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of health
        uses: actions/upload-artifact@v2
        with:
          name: health_no_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden/health/3c_performance_stats/
          retention-days: 5

      - name: Build converted health (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden/health
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo health >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden/failed-components-list.txt

      - name: Convert mst
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden/mst
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of mst
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden/mst
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of mst
        uses: actions/upload-artifact@v2
        with:
          name: mst_no_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden/mst/3c_performance_stats/
          retention-days: 5

      - name: Build converted mst (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden/mst
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo mst >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden/failed-components-list.txt

      - name: Convert perimeter
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden/perimeter
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of perimeter
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden/perimeter
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of perimeter
        uses: actions/upload-artifact@v2
        with:
          name: perimeter_no_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden/perimeter/3c_performance_stats/
          retention-days: 5

      - name: Build converted perimeter (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden/perimeter
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo perimeter >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden/failed-components-list.txt

      - name: Convert power
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden/power
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of power
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden/power
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of power
        uses: actions/upload-artifact@v2
        with:
          name: power_no_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden/power/3c_performance_stats/
          retention-days: 5

      - name: Build converted power (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden/power
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo power >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden/failed-components-list.txt

      - name: Convert treeadd
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden/treeadd
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of treeadd
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden/treeadd
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of treeadd
        uses: actions/upload-artifact@v2
        with:
          name: treeadd_no_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden/treeadd/3c_performance_stats/
          retention-days: 5

      - name: Build converted treeadd (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden/treeadd
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo treeadd >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden/failed-components-list.txt

      - name: Convert tsp
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden/tsp
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of tsp
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden/tsp
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of tsp
        uses: actions/upload-artifact@v2
        with:
          name: tsp_no_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden/tsp/3c_performance_stats/
          retention-days: 5

      - name: Build converted tsp (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden/tsp
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo tsp >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden/failed-components-list.txt

      - name: Convert voronoi
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden/voronoi
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of voronoi
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden/voronoi
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of voronoi
        uses: actions/upload-artifact@v2
        with:
          name: voronoi_no_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden/voronoi/3c_performance_stats/
          retention-days: 5

      - name: Build converted voronoi (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden/voronoi
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo voronoi >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden/failed-components-list.txt

      - name: Check for deferred post-conversion build failures
        run: |
          if [ -e ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden/failed-components-list.txt ]; then
              echo 'Failed components (see previous post-conversion build steps):'
              cat ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden/failed-components-list.txt
              exit 1
          fi

  test_Olden_no_expand_macros_alltypes_disable_rds:
    name: Test Olden (not macro-expanded, -alltypes, CCured solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Olden
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds
          tar -xvzf ${{env.benchmark_tar_dir}}/Olden.tar.gz
          cd Olden
          for i in bh bisort em3d health mst perimeter power treeadd tsp voronoi ; do \
            (cd $i ; bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE") \
          done

      - name: Convert bh
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden/bh
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of bh
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden/bh
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of bh
        uses: actions/upload-artifact@v2
        with:
          name: bh_no_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden/bh/3c_performance_stats/
          retention-days: 5

      - name: Build converted bh (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden/bh
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo bh >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden/failed-components-list.txt

      - name: Convert bisort
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden/bisort
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of bisort
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden/bisort
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of bisort
        uses: actions/upload-artifact@v2
        with:
          name: bisort_no_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden/bisort/3c_performance_stats/
          retention-days: 5

      - name: Build converted bisort (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden/bisort
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo bisort >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden/failed-components-list.txt

      - name: Convert em3d
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden/em3d
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of em3d
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden/em3d
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of em3d
        uses: actions/upload-artifact@v2
        with:
          name: em3d_no_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden/em3d/3c_performance_stats/
          retention-days: 5

      - name: Build converted em3d (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden/em3d
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo em3d >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden/failed-components-list.txt

      - name: Convert health
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden/health
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of health
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden/health
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of health
        uses: actions/upload-artifact@v2
        with:
          name: health_no_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden/health/3c_performance_stats/
          retention-days: 5

      - name: Build converted health (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden/health
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo health >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden/failed-components-list.txt

      - name: Convert mst
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden/mst
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of mst
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden/mst
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of mst
        uses: actions/upload-artifact@v2
        with:
          name: mst_no_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden/mst/3c_performance_stats/
          retention-days: 5

      - name: Build converted mst (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden/mst
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo mst >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden/failed-components-list.txt

      - name: Convert perimeter
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden/perimeter
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of perimeter
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden/perimeter
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of perimeter
        uses: actions/upload-artifact@v2
        with:
          name: perimeter_no_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden/perimeter/3c_performance_stats/
          retention-days: 5

      - name: Build converted perimeter (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden/perimeter
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo perimeter >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden/failed-components-list.txt

      - name: Convert power
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden/power
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of power
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden/power
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of power
        uses: actions/upload-artifact@v2
        with:
          name: power_no_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden/power/3c_performance_stats/
          retention-days: 5

      - name: Build converted power (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden/power
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo power >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden/failed-components-list.txt

      - name: Convert treeadd
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden/treeadd
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of treeadd
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden/treeadd
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of treeadd
        uses: actions/upload-artifact@v2
        with:
          name: treeadd_no_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden/treeadd/3c_performance_stats/
          retention-days: 5

      - name: Build converted treeadd (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden/treeadd
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo treeadd >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden/failed-components-list.txt

      - name: Convert tsp
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden/tsp
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of tsp
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden/tsp
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of tsp
        uses: actions/upload-artifact@v2
        with:
          name: tsp_no_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden/tsp/3c_performance_stats/
          retention-days: 5

      - name: Build converted tsp (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden/tsp
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo tsp >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden/failed-components-list.txt

      - name: Convert voronoi
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden/voronoi
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of voronoi
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden/voronoi
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of voronoi
        uses: actions/upload-artifact@v2
        with:
          name: voronoi_no_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden/voronoi/3c_performance_stats/
          retention-days: 5

      - name: Build converted voronoi (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden/voronoi
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo voronoi >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden/failed-components-list.txt

      - name: Check for deferred post-conversion build failures
        run: |
          if [ -e ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden/failed-components-list.txt ]; then
              echo 'Failed components (see previous post-conversion build steps):'
              cat ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden/failed-components-list.txt
              exit 1
          fi

  test_Olden_no_expand_macros_alltypes_disable_fnedgs:
    name: Test Olden (not macro-expanded, -alltypes, FuncRevEdges solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Olden
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs
          tar -xvzf ${{env.benchmark_tar_dir}}/Olden.tar.gz
          cd Olden
          for i in bh bisort em3d health mst perimeter power treeadd tsp voronoi ; do \
            (cd $i ; bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE") \
          done

      - name: Convert bh
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden/bh
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of bh
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden/bh
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of bh
        uses: actions/upload-artifact@v2
        with:
          name: bh_no_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden/bh/3c_performance_stats/
          retention-days: 5

      - name: Build converted bh (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden/bh
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo bh >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden/failed-components-list.txt

      - name: Convert bisort
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden/bisort
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of bisort
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden/bisort
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of bisort
        uses: actions/upload-artifact@v2
        with:
          name: bisort_no_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden/bisort/3c_performance_stats/
          retention-days: 5

      - name: Build converted bisort (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden/bisort
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo bisort >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden/failed-components-list.txt

      - name: Convert em3d
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden/em3d
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of em3d
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden/em3d
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of em3d
        uses: actions/upload-artifact@v2
        with:
          name: em3d_no_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden/em3d/3c_performance_stats/
          retention-days: 5

      - name: Build converted em3d (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden/em3d
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo em3d >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden/failed-components-list.txt

      - name: Convert health
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden/health
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of health
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden/health
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of health
        uses: actions/upload-artifact@v2
        with:
          name: health_no_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden/health/3c_performance_stats/
          retention-days: 5

      - name: Build converted health (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden/health
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo health >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden/failed-components-list.txt

      - name: Convert mst
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden/mst
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of mst
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden/mst
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of mst
        uses: actions/upload-artifact@v2
        with:
          name: mst_no_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden/mst/3c_performance_stats/
          retention-days: 5

      - name: Build converted mst (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden/mst
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo mst >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden/failed-components-list.txt

      - name: Convert perimeter
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden/perimeter
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of perimeter
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden/perimeter
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of perimeter
        uses: actions/upload-artifact@v2
        with:
          name: perimeter_no_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden/perimeter/3c_performance_stats/
          retention-days: 5

      - name: Build converted perimeter (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden/perimeter
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo perimeter >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden/failed-components-list.txt

      - name: Convert power
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden/power
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of power
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden/power
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of power
        uses: actions/upload-artifact@v2
        with:
          name: power_no_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden/power/3c_performance_stats/
          retention-days: 5

      - name: Build converted power (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden/power
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo power >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden/failed-components-list.txt

      - name: Convert treeadd
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden/treeadd
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of treeadd
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden/treeadd
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of treeadd
        uses: actions/upload-artifact@v2
        with:
          name: treeadd_no_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden/treeadd/3c_performance_stats/
          retention-days: 5

      - name: Build converted treeadd (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden/treeadd
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo treeadd >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden/failed-components-list.txt

      - name: Convert tsp
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden/tsp
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of tsp
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden/tsp
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of tsp
        uses: actions/upload-artifact@v2
        with:
          name: tsp_no_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden/tsp/3c_performance_stats/
          retention-days: 5

      - name: Build converted tsp (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden/tsp
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo tsp >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden/failed-components-list.txt

      - name: Convert voronoi
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden/voronoi
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of voronoi
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden/voronoi
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of voronoi
        uses: actions/upload-artifact@v2
        with:
          name: voronoi_no_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden/voronoi/3c_performance_stats/
          retention-days: 5

      - name: Build converted voronoi (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden/voronoi
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo voronoi >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden/failed-components-list.txt

      - name: Check for deferred post-conversion build failures
        run: |
          if [ -e ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden/failed-components-list.txt ]; then
              echo 'Failed components (see previous post-conversion build steps):'
              cat ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden/failed-components-list.txt
              exit 1
          fi

  test_Olden_no_expand_macros_alltypes:
    name: Test Olden (not macro-expanded, -alltypes)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Olden
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes
          tar -xvzf ${{env.benchmark_tar_dir}}/Olden.tar.gz
          cd Olden
          for i in bh bisort em3d health mst perimeter power treeadd tsp voronoi ; do \
            (cd $i ; bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE") \
          done

      - name: Convert bh
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden/bh
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --project_path .

      - name: Copy 3c stats of bh
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden/bh
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of bh
        uses: actions/upload-artifact@v2
        with:
          name: bh_no_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden/bh/3c_performance_stats/
          retention-days: 5

      - name: Build converted bh (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden/bh
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo bh >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden/failed-components-list.txt

      - name: Convert bisort
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden/bisort
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --project_path .

      - name: Copy 3c stats of bisort
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden/bisort
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of bisort
        uses: actions/upload-artifact@v2
        with:
          name: bisort_no_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden/bisort/3c_performance_stats/
          retention-days: 5

      - name: Build converted bisort (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden/bisort
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo bisort >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden/failed-components-list.txt

      - name: Convert em3d
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden/em3d
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --project_path .

      - name: Copy 3c stats of em3d
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden/em3d
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of em3d
        uses: actions/upload-artifact@v2
        with:
          name: em3d_no_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden/em3d/3c_performance_stats/
          retention-days: 5

      - name: Build converted em3d (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden/em3d
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo em3d >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden/failed-components-list.txt

      - name: Convert health
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden/health
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --project_path .

      - name: Copy 3c stats of health
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden/health
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of health
        uses: actions/upload-artifact@v2
        with:
          name: health_no_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden/health/3c_performance_stats/
          retention-days: 5

      - name: Build converted health (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden/health
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo health >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden/failed-components-list.txt

      - name: Convert mst
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden/mst
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --project_path .

      - name: Copy 3c stats of mst
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden/mst
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of mst
        uses: actions/upload-artifact@v2
        with:
          name: mst_no_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden/mst/3c_performance_stats/
          retention-days: 5

      - name: Build converted mst (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden/mst
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo mst >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden/failed-components-list.txt

      - name: Convert perimeter
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden/perimeter
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --project_path .

      - name: Copy 3c stats of perimeter
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden/perimeter
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of perimeter
        uses: actions/upload-artifact@v2
        with:
          name: perimeter_no_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden/perimeter/3c_performance_stats/
          retention-days: 5

      - name: Build converted perimeter (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden/perimeter
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo perimeter >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden/failed-components-list.txt

      - name: Convert power
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden/power
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --project_path .

      - name: Copy 3c stats of power
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden/power
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of power
        uses: actions/upload-artifact@v2
        with:
          name: power_no_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden/power/3c_performance_stats/
          retention-days: 5

      - name: Build converted power (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden/power
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo power >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden/failed-components-list.txt

      - name: Convert treeadd
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden/treeadd
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --project_path .

      - name: Copy 3c stats of treeadd
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden/treeadd
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of treeadd
        uses: actions/upload-artifact@v2
        with:
          name: treeadd_no_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden/treeadd/3c_performance_stats/
          retention-days: 5

      - name: Build converted treeadd (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden/treeadd
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo treeadd >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden/failed-components-list.txt

      - name: Convert tsp
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden/tsp
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --project_path .

      - name: Copy 3c stats of tsp
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden/tsp
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of tsp
        uses: actions/upload-artifact@v2
        with:
          name: tsp_no_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden/tsp/3c_performance_stats/
          retention-days: 5

      - name: Build converted tsp (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden/tsp
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo tsp >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden/failed-components-list.txt

      - name: Convert voronoi
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden/voronoi
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --project_path .

      - name: Copy 3c stats of voronoi
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden/voronoi
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of voronoi
        uses: actions/upload-artifact@v2
        with:
          name: voronoi_no_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden/voronoi/3c_performance_stats/
          retention-days: 5

      - name: Build converted voronoi (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden/voronoi
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo voronoi >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden/failed-components-list.txt

      - name: Check for deferred post-conversion build failures
        run: |
          if [ -e ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden/failed-components-list.txt ]; then
              echo 'Failed components (see previous post-conversion build steps):'
              cat ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden/failed-components-list.txt
              exit 1
          fi

  test_Olden_expand_macros_no_alltypes:
    name: Test Olden (macro-expanded, no -alltypes)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Olden
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes
          tar -xvzf ${{env.benchmark_tar_dir}}/Olden.tar.gz
          cd Olden
          for i in bh bisort em3d health mst perimeter power treeadd tsp voronoi ; do \
            (cd $i ; bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE") \
          done

      - name: Convert bh
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden/bh
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of bh
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden/bh
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of bh
        uses: actions/upload-artifact@v2
        with:
          name: bh_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden/bh/3c_performance_stats/
          retention-days: 5

      - name: Build converted bh (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden/bh
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" || echo bh >>${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden/failed-components-list.txt

      - name: Convert bisort
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden/bisort
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of bisort
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden/bisort
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of bisort
        uses: actions/upload-artifact@v2
        with:
          name: bisort_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden/bisort/3c_performance_stats/
          retention-days: 5

      - name: Build converted bisort (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden/bisort
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" || echo bisort >>${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden/failed-components-list.txt

      - name: Convert em3d
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden/em3d
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of em3d
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden/em3d
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of em3d
        uses: actions/upload-artifact@v2
        with:
          name: em3d_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden/em3d/3c_performance_stats/
          retention-days: 5

      - name: Build converted em3d (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden/em3d
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" || echo em3d >>${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden/failed-components-list.txt

      - name: Convert health
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden/health
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of health
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden/health
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of health
        uses: actions/upload-artifact@v2
        with:
          name: health_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden/health/3c_performance_stats/
          retention-days: 5

      - name: Build converted health (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden/health
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" || echo health >>${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden/failed-components-list.txt

      - name: Convert mst
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden/mst
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of mst
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden/mst
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of mst
        uses: actions/upload-artifact@v2
        with:
          name: mst_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden/mst/3c_performance_stats/
          retention-days: 5

      - name: Build converted mst (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden/mst
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" || echo mst >>${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden/failed-components-list.txt

      - name: Convert perimeter
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden/perimeter
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of perimeter
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden/perimeter
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of perimeter
        uses: actions/upload-artifact@v2
        with:
          name: perimeter_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden/perimeter/3c_performance_stats/
          retention-days: 5

      - name: Build converted perimeter (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden/perimeter
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" || echo perimeter >>${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden/failed-components-list.txt

      - name: Convert power
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden/power
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of power
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden/power
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of power
        uses: actions/upload-artifact@v2
        with:
          name: power_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden/power/3c_performance_stats/
          retention-days: 5

      - name: Build converted power (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden/power
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" || echo power >>${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden/failed-components-list.txt

      - name: Convert treeadd
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden/treeadd
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of treeadd
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden/treeadd
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of treeadd
        uses: actions/upload-artifact@v2
        with:
          name: treeadd_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden/treeadd/3c_performance_stats/
          retention-days: 5

      - name: Build converted treeadd (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden/treeadd
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" || echo treeadd >>${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden/failed-components-list.txt

      - name: Convert tsp
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden/tsp
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of tsp
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden/tsp
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of tsp
        uses: actions/upload-artifact@v2
        with:
          name: tsp_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden/tsp/3c_performance_stats/
          retention-days: 5

      - name: Build converted tsp (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden/tsp
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" || echo tsp >>${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden/failed-components-list.txt

      - name: Convert voronoi
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden/voronoi
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of voronoi
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden/voronoi
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of voronoi
        uses: actions/upload-artifact@v2
        with:
          name: voronoi_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden/voronoi/3c_performance_stats/
          retention-days: 5

      - name: Build converted voronoi (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden/voronoi
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" || echo voronoi >>${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden/failed-components-list.txt

      - name: Check for deferred post-conversion build failures
        run: |
          if [ -e ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden/failed-components-list.txt ]; then
              echo 'Failed components (see previous post-conversion build steps):'
              cat ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden/failed-components-list.txt
              exit 1
          fi

  test_Olden_expand_macros_alltypes_only_g_sol:
    name: Test Olden (macro-expanded, -alltypes, greatest solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Olden
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol
          tar -xvzf ${{env.benchmark_tar_dir}}/Olden.tar.gz
          cd Olden
          for i in bh bisort em3d health mst perimeter power treeadd tsp voronoi ; do \
            (cd $i ; bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE") \
          done

      - name: Convert bh
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden/bh
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of bh
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden/bh
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of bh
        uses: actions/upload-artifact@v2
        with:
          name: bh_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden/bh/3c_performance_stats/
          retention-days: 5

      - name: Build converted bh (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden/bh
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo bh >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden/failed-components-list.txt

      - name: Convert bisort
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden/bisort
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of bisort
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden/bisort
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of bisort
        uses: actions/upload-artifact@v2
        with:
          name: bisort_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden/bisort/3c_performance_stats/
          retention-days: 5

      - name: Build converted bisort (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden/bisort
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo bisort >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden/failed-components-list.txt

      - name: Convert em3d
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden/em3d
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of em3d
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden/em3d
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of em3d
        uses: actions/upload-artifact@v2
        with:
          name: em3d_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden/em3d/3c_performance_stats/
          retention-days: 5

      - name: Build converted em3d (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden/em3d
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo em3d >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden/failed-components-list.txt

      - name: Convert health
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden/health
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of health
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden/health
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of health
        uses: actions/upload-artifact@v2
        with:
          name: health_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden/health/3c_performance_stats/
          retention-days: 5

      - name: Build converted health (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden/health
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo health >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden/failed-components-list.txt

      - name: Convert mst
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden/mst
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of mst
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden/mst
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of mst
        uses: actions/upload-artifact@v2
        with:
          name: mst_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden/mst/3c_performance_stats/
          retention-days: 5

      - name: Build converted mst (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden/mst
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo mst >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden/failed-components-list.txt

      - name: Convert perimeter
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden/perimeter
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of perimeter
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden/perimeter
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of perimeter
        uses: actions/upload-artifact@v2
        with:
          name: perimeter_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden/perimeter/3c_performance_stats/
          retention-days: 5

      - name: Build converted perimeter (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden/perimeter
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo perimeter >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden/failed-components-list.txt

      - name: Convert power
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden/power
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of power
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden/power
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of power
        uses: actions/upload-artifact@v2
        with:
          name: power_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden/power/3c_performance_stats/
          retention-days: 5

      - name: Build converted power (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden/power
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo power >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden/failed-components-list.txt

      - name: Convert treeadd
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden/treeadd
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of treeadd
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden/treeadd
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of treeadd
        uses: actions/upload-artifact@v2
        with:
          name: treeadd_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden/treeadd/3c_performance_stats/
          retention-days: 5

      - name: Build converted treeadd (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden/treeadd
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo treeadd >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden/failed-components-list.txt

      - name: Convert tsp
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden/tsp
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of tsp
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden/tsp
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of tsp
        uses: actions/upload-artifact@v2
        with:
          name: tsp_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden/tsp/3c_performance_stats/
          retention-days: 5

      - name: Build converted tsp (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden/tsp
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo tsp >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden/failed-components-list.txt

      - name: Convert voronoi
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden/voronoi
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of voronoi
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden/voronoi
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of voronoi
        uses: actions/upload-artifact@v2
        with:
          name: voronoi_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden/voronoi/3c_performance_stats/
          retention-days: 5

      - name: Build converted voronoi (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden/voronoi
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo voronoi >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden/failed-components-list.txt

      - name: Check for deferred post-conversion build failures
        run: |
          if [ -e ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden/failed-components-list.txt ]; then
              echo 'Failed components (see previous post-conversion build steps):'
              cat ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden/failed-components-list.txt
              exit 1
          fi

  test_Olden_expand_macros_alltypes_only_l_sol:
    name: Test Olden (macro-expanded, -alltypes, least solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Olden
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol
          tar -xvzf ${{env.benchmark_tar_dir}}/Olden.tar.gz
          cd Olden
          for i in bh bisort em3d health mst perimeter power treeadd tsp voronoi ; do \
            (cd $i ; bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE") \
          done

      - name: Convert bh
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden/bh
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of bh
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden/bh
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of bh
        uses: actions/upload-artifact@v2
        with:
          name: bh_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden/bh/3c_performance_stats/
          retention-days: 5

      - name: Build converted bh (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden/bh
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo bh >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden/failed-components-list.txt

      - name: Convert bisort
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden/bisort
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of bisort
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden/bisort
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of bisort
        uses: actions/upload-artifact@v2
        with:
          name: bisort_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden/bisort/3c_performance_stats/
          retention-days: 5

      - name: Build converted bisort (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden/bisort
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo bisort >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden/failed-components-list.txt

      - name: Convert em3d
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden/em3d
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of em3d
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden/em3d
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of em3d
        uses: actions/upload-artifact@v2
        with:
          name: em3d_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden/em3d/3c_performance_stats/
          retention-days: 5

      - name: Build converted em3d (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden/em3d
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo em3d >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden/failed-components-list.txt

      - name: Convert health
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden/health
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of health
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden/health
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of health
        uses: actions/upload-artifact@v2
        with:
          name: health_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden/health/3c_performance_stats/
          retention-days: 5

      - name: Build converted health (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden/health
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo health >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden/failed-components-list.txt

      - name: Convert mst
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden/mst
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of mst
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden/mst
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of mst
        uses: actions/upload-artifact@v2
        with:
          name: mst_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden/mst/3c_performance_stats/
          retention-days: 5

      - name: Build converted mst (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden/mst
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo mst >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden/failed-components-list.txt

      - name: Convert perimeter
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden/perimeter
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of perimeter
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden/perimeter
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of perimeter
        uses: actions/upload-artifact@v2
        with:
          name: perimeter_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden/perimeter/3c_performance_stats/
          retention-days: 5

      - name: Build converted perimeter (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden/perimeter
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo perimeter >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden/failed-components-list.txt

      - name: Convert power
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden/power
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of power
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden/power
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of power
        uses: actions/upload-artifact@v2
        with:
          name: power_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden/power/3c_performance_stats/
          retention-days: 5

      - name: Build converted power (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden/power
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo power >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden/failed-components-list.txt

      - name: Convert treeadd
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden/treeadd
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of treeadd
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden/treeadd
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of treeadd
        uses: actions/upload-artifact@v2
        with:
          name: treeadd_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden/treeadd/3c_performance_stats/
          retention-days: 5

      - name: Build converted treeadd (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden/treeadd
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo treeadd >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden/failed-components-list.txt

      - name: Convert tsp
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden/tsp
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of tsp
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden/tsp
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of tsp
        uses: actions/upload-artifact@v2
        with:
          name: tsp_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden/tsp/3c_performance_stats/
          retention-days: 5

      - name: Build converted tsp (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden/tsp
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo tsp >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden/failed-components-list.txt

      - name: Convert voronoi
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden/voronoi
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of voronoi
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden/voronoi
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of voronoi
        uses: actions/upload-artifact@v2
        with:
          name: voronoi_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden/voronoi/3c_performance_stats/
          retention-days: 5

      - name: Build converted voronoi (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden/voronoi
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo voronoi >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden/failed-components-list.txt

      - name: Check for deferred post-conversion build failures
        run: |
          if [ -e ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden/failed-components-list.txt ]; then
              echo 'Failed components (see previous post-conversion build steps):'
              cat ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden/failed-components-list.txt
              exit 1
          fi

  test_Olden_expand_macros_alltypes_disable_rds:
    name: Test Olden (macro-expanded, -alltypes, CCured solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Olden
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds
          tar -xvzf ${{env.benchmark_tar_dir}}/Olden.tar.gz
          cd Olden
          for i in bh bisort em3d health mst perimeter power treeadd tsp voronoi ; do \
            (cd $i ; bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE") \
          done

      - name: Convert bh
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden/bh
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of bh
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden/bh
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of bh
        uses: actions/upload-artifact@v2
        with:
          name: bh_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden/bh/3c_performance_stats/
          retention-days: 5

      - name: Build converted bh (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden/bh
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo bh >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden/failed-components-list.txt

      - name: Convert bisort
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden/bisort
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of bisort
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden/bisort
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of bisort
        uses: actions/upload-artifact@v2
        with:
          name: bisort_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden/bisort/3c_performance_stats/
          retention-days: 5

      - name: Build converted bisort (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden/bisort
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo bisort >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden/failed-components-list.txt

      - name: Convert em3d
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden/em3d
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of em3d
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden/em3d
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of em3d
        uses: actions/upload-artifact@v2
        with:
          name: em3d_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden/em3d/3c_performance_stats/
          retention-days: 5

      - name: Build converted em3d (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden/em3d
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo em3d >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden/failed-components-list.txt

      - name: Convert health
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden/health
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of health
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden/health
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of health
        uses: actions/upload-artifact@v2
        with:
          name: health_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden/health/3c_performance_stats/
          retention-days: 5

      - name: Build converted health (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden/health
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo health >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden/failed-components-list.txt

      - name: Convert mst
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden/mst
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of mst
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden/mst
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of mst
        uses: actions/upload-artifact@v2
        with:
          name: mst_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden/mst/3c_performance_stats/
          retention-days: 5

      - name: Build converted mst (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden/mst
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo mst >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden/failed-components-list.txt

      - name: Convert perimeter
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden/perimeter
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of perimeter
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden/perimeter
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of perimeter
        uses: actions/upload-artifact@v2
        with:
          name: perimeter_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden/perimeter/3c_performance_stats/
          retention-days: 5

      - name: Build converted perimeter (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden/perimeter
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo perimeter >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden/failed-components-list.txt

      - name: Convert power
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden/power
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of power
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden/power
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of power
        uses: actions/upload-artifact@v2
        with:
          name: power_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden/power/3c_performance_stats/
          retention-days: 5

      - name: Build converted power (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden/power
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo power >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden/failed-components-list.txt

      - name: Convert treeadd
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden/treeadd
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of treeadd
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden/treeadd
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of treeadd
        uses: actions/upload-artifact@v2
        with:
          name: treeadd_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden/treeadd/3c_performance_stats/
          retention-days: 5

      - name: Build converted treeadd (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden/treeadd
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo treeadd >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden/failed-components-list.txt

      - name: Convert tsp
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden/tsp
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of tsp
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden/tsp
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of tsp
        uses: actions/upload-artifact@v2
        with:
          name: tsp_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden/tsp/3c_performance_stats/
          retention-days: 5

      - name: Build converted tsp (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden/tsp
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo tsp >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden/failed-components-list.txt

      - name: Convert voronoi
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden/voronoi
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of voronoi
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden/voronoi
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of voronoi
        uses: actions/upload-artifact@v2
        with:
          name: voronoi_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden/voronoi/3c_performance_stats/
          retention-days: 5

      - name: Build converted voronoi (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden/voronoi
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo voronoi >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden/failed-components-list.txt

      - name: Check for deferred post-conversion build failures
        run: |
          if [ -e ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden/failed-components-list.txt ]; then
              echo 'Failed components (see previous post-conversion build steps):'
              cat ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden/failed-components-list.txt
              exit 1
          fi

  test_Olden_expand_macros_alltypes_disable_fnedgs:
    name: Test Olden (macro-expanded, -alltypes, FuncRevEdges solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Olden
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs
          tar -xvzf ${{env.benchmark_tar_dir}}/Olden.tar.gz
          cd Olden
          for i in bh bisort em3d health mst perimeter power treeadd tsp voronoi ; do \
            (cd $i ; bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE") \
          done

      - name: Convert bh
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden/bh
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of bh
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden/bh
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of bh
        uses: actions/upload-artifact@v2
        with:
          name: bh_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden/bh/3c_performance_stats/
          retention-days: 5

      - name: Build converted bh (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden/bh
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo bh >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden/failed-components-list.txt

      - name: Convert bisort
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden/bisort
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of bisort
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden/bisort
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of bisort
        uses: actions/upload-artifact@v2
        with:
          name: bisort_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden/bisort/3c_performance_stats/
          retention-days: 5

      - name: Build converted bisort (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden/bisort
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo bisort >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden/failed-components-list.txt

      - name: Convert em3d
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden/em3d
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of em3d
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden/em3d
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of em3d
        uses: actions/upload-artifact@v2
        with:
          name: em3d_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden/em3d/3c_performance_stats/
          retention-days: 5

      - name: Build converted em3d (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden/em3d
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo em3d >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden/failed-components-list.txt

      - name: Convert health
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden/health
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of health
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden/health
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of health
        uses: actions/upload-artifact@v2
        with:
          name: health_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden/health/3c_performance_stats/
          retention-days: 5

      - name: Build converted health (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden/health
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo health >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden/failed-components-list.txt

      - name: Convert mst
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden/mst
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of mst
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden/mst
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of mst
        uses: actions/upload-artifact@v2
        with:
          name: mst_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden/mst/3c_performance_stats/
          retention-days: 5

      - name: Build converted mst (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden/mst
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo mst >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden/failed-components-list.txt

      - name: Convert perimeter
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden/perimeter
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of perimeter
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden/perimeter
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of perimeter
        uses: actions/upload-artifact@v2
        with:
          name: perimeter_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden/perimeter/3c_performance_stats/
          retention-days: 5

      - name: Build converted perimeter (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden/perimeter
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo perimeter >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden/failed-components-list.txt

      - name: Convert power
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden/power
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of power
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden/power
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of power
        uses: actions/upload-artifact@v2
        with:
          name: power_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden/power/3c_performance_stats/
          retention-days: 5

      - name: Build converted power (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden/power
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo power >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden/failed-components-list.txt

      - name: Convert treeadd
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden/treeadd
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of treeadd
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden/treeadd
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of treeadd
        uses: actions/upload-artifact@v2
        with:
          name: treeadd_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden/treeadd/3c_performance_stats/
          retention-days: 5

      - name: Build converted treeadd (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden/treeadd
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo treeadd >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden/failed-components-list.txt

      - name: Convert tsp
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden/tsp
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of tsp
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden/tsp
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of tsp
        uses: actions/upload-artifact@v2
        with:
          name: tsp_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden/tsp/3c_performance_stats/
          retention-days: 5

      - name: Build converted tsp (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden/tsp
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo tsp >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden/failed-components-list.txt

      - name: Convert voronoi
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden/voronoi
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of voronoi
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden/voronoi
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of voronoi
        uses: actions/upload-artifact@v2
        with:
          name: voronoi_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden/voronoi/3c_performance_stats/
          retention-days: 5

      - name: Build converted voronoi (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden/voronoi
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo voronoi >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden/failed-components-list.txt

      - name: Check for deferred post-conversion build failures
        run: |
          if [ -e ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden/failed-components-list.txt ]; then
              echo 'Failed components (see previous post-conversion build steps):'
              cat ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden/failed-components-list.txt
              exit 1
          fi

  test_Olden_expand_macros_alltypes:
    name: Test Olden (macro-expanded, -alltypes)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Olden
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/expand_macros_alltypes
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes
          tar -xvzf ${{env.benchmark_tar_dir}}/Olden.tar.gz
          cd Olden
          for i in bh bisort em3d health mst perimeter power treeadd tsp voronoi ; do \
            (cd $i ; bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE") \
          done

      - name: Convert bh
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden/bh
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of bh
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden/bh
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of bh
        uses: actions/upload-artifact@v2
        with:
          name: bh_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden/bh/3c_performance_stats/
          retention-days: 5

      - name: Build converted bh (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden/bh
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo bh >>${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden/failed-components-list.txt

      - name: Convert bisort
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden/bisort
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of bisort
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden/bisort
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of bisort
        uses: actions/upload-artifact@v2
        with:
          name: bisort_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden/bisort/3c_performance_stats/
          retention-days: 5

      - name: Build converted bisort (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden/bisort
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo bisort >>${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden/failed-components-list.txt

      - name: Convert em3d
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden/em3d
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of em3d
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden/em3d
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of em3d
        uses: actions/upload-artifact@v2
        with:
          name: em3d_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden/em3d/3c_performance_stats/
          retention-days: 5

      - name: Build converted em3d (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden/em3d
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo em3d >>${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden/failed-components-list.txt

      - name: Convert health
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden/health
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of health
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden/health
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of health
        uses: actions/upload-artifact@v2
        with:
          name: health_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden/health/3c_performance_stats/
          retention-days: 5

      - name: Build converted health (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden/health
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo health >>${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden/failed-components-list.txt

      - name: Convert mst
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden/mst
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of mst
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden/mst
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of mst
        uses: actions/upload-artifact@v2
        with:
          name: mst_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden/mst/3c_performance_stats/
          retention-days: 5

      - name: Build converted mst (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden/mst
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo mst >>${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden/failed-components-list.txt

      - name: Convert perimeter
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden/perimeter
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of perimeter
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden/perimeter
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of perimeter
        uses: actions/upload-artifact@v2
        with:
          name: perimeter_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden/perimeter/3c_performance_stats/
          retention-days: 5

      - name: Build converted perimeter (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden/perimeter
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo perimeter >>${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden/failed-components-list.txt

      - name: Convert power
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden/power
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of power
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden/power
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of power
        uses: actions/upload-artifact@v2
        with:
          name: power_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden/power/3c_performance_stats/
          retention-days: 5

      - name: Build converted power (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden/power
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo power >>${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden/failed-components-list.txt

      - name: Convert treeadd
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden/treeadd
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of treeadd
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden/treeadd
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of treeadd
        uses: actions/upload-artifact@v2
        with:
          name: treeadd_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden/treeadd/3c_performance_stats/
          retention-days: 5

      - name: Build converted treeadd (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden/treeadd
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo treeadd >>${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden/failed-components-list.txt

      - name: Convert tsp
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden/tsp
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of tsp
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden/tsp
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of tsp
        uses: actions/upload-artifact@v2
        with:
          name: tsp_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden/tsp/3c_performance_stats/
          retention-days: 5

      - name: Build converted tsp (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden/tsp
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo tsp >>${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden/failed-components-list.txt

      - name: Convert voronoi
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden/voronoi
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of voronoi
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden/voronoi
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of voronoi
        uses: actions/upload-artifact@v2
        with:
          name: voronoi_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden/voronoi/3c_performance_stats/
          retention-days: 5

      - name: Build converted voronoi (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden/voronoi
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo voronoi >>${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden/failed-components-list.txt

      - name: Check for deferred post-conversion build failures
        run: |
          if [ -e ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden/failed-components-list.txt ]; then
              echo 'Failed components (see previous post-conversion build steps):'
              cat ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden/failed-components-list.txt
              exit 1
          fi

  test_Olden-Reverted_no_expand_macros_no_alltypes:
    name: Test Olden-Reverted (not macro-expanded, no -alltypes)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Olden-Reverted
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes
          tar -xvzf ${{env.benchmark_tar_dir}}/Olden-reverted.tar.gz
          cd Olden-reverted
          for i in bh bisort em3d health mst perimeter power treeadd tsp voronoi ; do \
            (cd $i ; bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE") \
          done

      - name: Convert bh
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-reverted/bh
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --project_path .

      - name: Copy 3c stats of bh
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-reverted/bh
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of bh
        uses: actions/upload-artifact@v2
        with:
          name: bh_no_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-reverted/bh/3c_performance_stats/
          retention-days: 5

      - name: Build converted bh (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-reverted/bh
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" || echo bh >>${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-reverted/failed-components-list.txt

      - name: Convert bisort
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-reverted/bisort
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --project_path .

      - name: Copy 3c stats of bisort
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-reverted/bisort
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of bisort
        uses: actions/upload-artifact@v2
        with:
          name: bisort_no_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-reverted/bisort/3c_performance_stats/
          retention-days: 5

      - name: Build converted bisort (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-reverted/bisort
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" || echo bisort >>${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-reverted/failed-components-list.txt

      - name: Convert em3d
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-reverted/em3d
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --project_path .

      - name: Copy 3c stats of em3d
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-reverted/em3d
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of em3d
        uses: actions/upload-artifact@v2
        with:
          name: em3d_no_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-reverted/em3d/3c_performance_stats/
          retention-days: 5

      - name: Build converted em3d (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-reverted/em3d
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" || echo em3d >>${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-reverted/failed-components-list.txt

      - name: Convert health
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-reverted/health
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --project_path .

      - name: Copy 3c stats of health
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-reverted/health
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of health
        uses: actions/upload-artifact@v2
        with:
          name: health_no_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-reverted/health/3c_performance_stats/
          retention-days: 5

      - name: Build converted health (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-reverted/health
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" || echo health >>${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-reverted/failed-components-list.txt

      - name: Convert mst
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-reverted/mst
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --project_path .

      - name: Copy 3c stats of mst
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-reverted/mst
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of mst
        uses: actions/upload-artifact@v2
        with:
          name: mst_no_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-reverted/mst/3c_performance_stats/
          retention-days: 5

      - name: Build converted mst (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-reverted/mst
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" || echo mst >>${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-reverted/failed-components-list.txt

      - name: Convert perimeter
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-reverted/perimeter
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --project_path .

      - name: Copy 3c stats of perimeter
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-reverted/perimeter
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of perimeter
        uses: actions/upload-artifact@v2
        with:
          name: perimeter_no_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-reverted/perimeter/3c_performance_stats/
          retention-days: 5

      - name: Build converted perimeter (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-reverted/perimeter
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" || echo perimeter >>${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-reverted/failed-components-list.txt

      - name: Convert power
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-reverted/power
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --project_path .

      - name: Copy 3c stats of power
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-reverted/power
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of power
        uses: actions/upload-artifact@v2
        with:
          name: power_no_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-reverted/power/3c_performance_stats/
          retention-days: 5

      - name: Build converted power (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-reverted/power
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" || echo power >>${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-reverted/failed-components-list.txt

      - name: Convert treeadd
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-reverted/treeadd
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --project_path .

      - name: Copy 3c stats of treeadd
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-reverted/treeadd
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of treeadd
        uses: actions/upload-artifact@v2
        with:
          name: treeadd_no_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-reverted/treeadd/3c_performance_stats/
          retention-days: 5

      - name: Build converted treeadd (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-reverted/treeadd
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" || echo treeadd >>${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-reverted/failed-components-list.txt

      - name: Convert tsp
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-reverted/tsp
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --project_path .

      - name: Copy 3c stats of tsp
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-reverted/tsp
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of tsp
        uses: actions/upload-artifact@v2
        with:
          name: tsp_no_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-reverted/tsp/3c_performance_stats/
          retention-days: 5

      - name: Build converted tsp (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-reverted/tsp
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" || echo tsp >>${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-reverted/failed-components-list.txt

      - name: Convert voronoi
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-reverted/voronoi
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --project_path .

      - name: Copy 3c stats of voronoi
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-reverted/voronoi
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of voronoi
        uses: actions/upload-artifact@v2
        with:
          name: voronoi_no_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-reverted/voronoi/3c_performance_stats/
          retention-days: 5

      - name: Build converted voronoi (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-reverted/voronoi
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" || echo voronoi >>${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-reverted/failed-components-list.txt

      - name: Check for deferred post-conversion build failures
        run: |
          if [ -e ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-reverted/failed-components-list.txt ]; then
              echo 'Failed components (see previous post-conversion build steps):'
              cat ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-reverted/failed-components-list.txt
              exit 1
          fi

  test_Olden-Reverted_no_expand_macros_alltypes_only_g_sol:
    name: Test Olden-Reverted (not macro-expanded, -alltypes, greatest solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Olden-Reverted
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol
          tar -xvzf ${{env.benchmark_tar_dir}}/Olden-reverted.tar.gz
          cd Olden-reverted
          for i in bh bisort em3d health mst perimeter power treeadd tsp voronoi ; do \
            (cd $i ; bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE") \
          done

      - name: Convert bh
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-reverted/bh
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of bh
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-reverted/bh
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of bh
        uses: actions/upload-artifact@v2
        with:
          name: bh_no_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-reverted/bh/3c_performance_stats/
          retention-days: 5

      - name: Build converted bh (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-reverted/bh
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo bh >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-reverted/failed-components-list.txt

      - name: Convert bisort
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-reverted/bisort
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of bisort
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-reverted/bisort
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of bisort
        uses: actions/upload-artifact@v2
        with:
          name: bisort_no_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-reverted/bisort/3c_performance_stats/
          retention-days: 5

      - name: Build converted bisort (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-reverted/bisort
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo bisort >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-reverted/failed-components-list.txt

      - name: Convert em3d
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-reverted/em3d
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of em3d
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-reverted/em3d
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of em3d
        uses: actions/upload-artifact@v2
        with:
          name: em3d_no_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-reverted/em3d/3c_performance_stats/
          retention-days: 5

      - name: Build converted em3d (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-reverted/em3d
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo em3d >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-reverted/failed-components-list.txt

      - name: Convert health
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-reverted/health
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of health
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-reverted/health
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of health
        uses: actions/upload-artifact@v2
        with:
          name: health_no_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-reverted/health/3c_performance_stats/
          retention-days: 5

      - name: Build converted health (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-reverted/health
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo health >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-reverted/failed-components-list.txt

      - name: Convert mst
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-reverted/mst
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of mst
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-reverted/mst
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of mst
        uses: actions/upload-artifact@v2
        with:
          name: mst_no_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-reverted/mst/3c_performance_stats/
          retention-days: 5

      - name: Build converted mst (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-reverted/mst
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo mst >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-reverted/failed-components-list.txt

      - name: Convert perimeter
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-reverted/perimeter
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of perimeter
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-reverted/perimeter
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of perimeter
        uses: actions/upload-artifact@v2
        with:
          name: perimeter_no_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-reverted/perimeter/3c_performance_stats/
          retention-days: 5

      - name: Build converted perimeter (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-reverted/perimeter
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo perimeter >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-reverted/failed-components-list.txt

      - name: Convert power
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-reverted/power
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of power
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-reverted/power
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of power
        uses: actions/upload-artifact@v2
        with:
          name: power_no_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-reverted/power/3c_performance_stats/
          retention-days: 5

      - name: Build converted power (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-reverted/power
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo power >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-reverted/failed-components-list.txt

      - name: Convert treeadd
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-reverted/treeadd
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of treeadd
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-reverted/treeadd
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of treeadd
        uses: actions/upload-artifact@v2
        with:
          name: treeadd_no_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-reverted/treeadd/3c_performance_stats/
          retention-days: 5

      - name: Build converted treeadd (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-reverted/treeadd
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo treeadd >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-reverted/failed-components-list.txt

      - name: Convert tsp
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-reverted/tsp
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of tsp
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-reverted/tsp
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of tsp
        uses: actions/upload-artifact@v2
        with:
          name: tsp_no_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-reverted/tsp/3c_performance_stats/
          retention-days: 5

      - name: Build converted tsp (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-reverted/tsp
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo tsp >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-reverted/failed-components-list.txt

      - name: Convert voronoi
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-reverted/voronoi
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of voronoi
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-reverted/voronoi
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of voronoi
        uses: actions/upload-artifact@v2
        with:
          name: voronoi_no_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-reverted/voronoi/3c_performance_stats/
          retention-days: 5

      - name: Build converted voronoi (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-reverted/voronoi
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo voronoi >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-reverted/failed-components-list.txt

      - name: Check for deferred post-conversion build failures
        run: |
          if [ -e ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-reverted/failed-components-list.txt ]; then
              echo 'Failed components (see previous post-conversion build steps):'
              cat ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-reverted/failed-components-list.txt
              exit 1
          fi

  test_Olden-Reverted_no_expand_macros_alltypes_only_l_sol:
    name: Test Olden-Reverted (not macro-expanded, -alltypes, least solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Olden-Reverted
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol
          tar -xvzf ${{env.benchmark_tar_dir}}/Olden-reverted.tar.gz
          cd Olden-reverted
          for i in bh bisort em3d health mst perimeter power treeadd tsp voronoi ; do \
            (cd $i ; bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE") \
          done

      - name: Convert bh
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-reverted/bh
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of bh
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-reverted/bh
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of bh
        uses: actions/upload-artifact@v2
        with:
          name: bh_no_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-reverted/bh/3c_performance_stats/
          retention-days: 5

      - name: Build converted bh (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-reverted/bh
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo bh >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-reverted/failed-components-list.txt

      - name: Convert bisort
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-reverted/bisort
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of bisort
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-reverted/bisort
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of bisort
        uses: actions/upload-artifact@v2
        with:
          name: bisort_no_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-reverted/bisort/3c_performance_stats/
          retention-days: 5

      - name: Build converted bisort (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-reverted/bisort
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo bisort >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-reverted/failed-components-list.txt

      - name: Convert em3d
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-reverted/em3d
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of em3d
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-reverted/em3d
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of em3d
        uses: actions/upload-artifact@v2
        with:
          name: em3d_no_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-reverted/em3d/3c_performance_stats/
          retention-days: 5

      - name: Build converted em3d (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-reverted/em3d
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo em3d >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-reverted/failed-components-list.txt

      - name: Convert health
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-reverted/health
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of health
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-reverted/health
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of health
        uses: actions/upload-artifact@v2
        with:
          name: health_no_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-reverted/health/3c_performance_stats/
          retention-days: 5

      - name: Build converted health (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-reverted/health
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo health >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-reverted/failed-components-list.txt

      - name: Convert mst
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-reverted/mst
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of mst
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-reverted/mst
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of mst
        uses: actions/upload-artifact@v2
        with:
          name: mst_no_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-reverted/mst/3c_performance_stats/
          retention-days: 5

      - name: Build converted mst (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-reverted/mst
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo mst >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-reverted/failed-components-list.txt

      - name: Convert perimeter
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-reverted/perimeter
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of perimeter
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-reverted/perimeter
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of perimeter
        uses: actions/upload-artifact@v2
        with:
          name: perimeter_no_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-reverted/perimeter/3c_performance_stats/
          retention-days: 5

      - name: Build converted perimeter (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-reverted/perimeter
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo perimeter >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-reverted/failed-components-list.txt

      - name: Convert power
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-reverted/power
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of power
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-reverted/power
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of power
        uses: actions/upload-artifact@v2
        with:
          name: power_no_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-reverted/power/3c_performance_stats/
          retention-days: 5

      - name: Build converted power (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-reverted/power
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo power >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-reverted/failed-components-list.txt

      - name: Convert treeadd
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-reverted/treeadd
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of treeadd
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-reverted/treeadd
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of treeadd
        uses: actions/upload-artifact@v2
        with:
          name: treeadd_no_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-reverted/treeadd/3c_performance_stats/
          retention-days: 5

      - name: Build converted treeadd (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-reverted/treeadd
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo treeadd >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-reverted/failed-components-list.txt

      - name: Convert tsp
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-reverted/tsp
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of tsp
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-reverted/tsp
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of tsp
        uses: actions/upload-artifact@v2
        with:
          name: tsp_no_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-reverted/tsp/3c_performance_stats/
          retention-days: 5

      - name: Build converted tsp (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-reverted/tsp
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo tsp >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-reverted/failed-components-list.txt

      - name: Convert voronoi
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-reverted/voronoi
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of voronoi
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-reverted/voronoi
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of voronoi
        uses: actions/upload-artifact@v2
        with:
          name: voronoi_no_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-reverted/voronoi/3c_performance_stats/
          retention-days: 5

      - name: Build converted voronoi (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-reverted/voronoi
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo voronoi >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-reverted/failed-components-list.txt

      - name: Check for deferred post-conversion build failures
        run: |
          if [ -e ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-reverted/failed-components-list.txt ]; then
              echo 'Failed components (see previous post-conversion build steps):'
              cat ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-reverted/failed-components-list.txt
              exit 1
          fi

  test_Olden-Reverted_no_expand_macros_alltypes_disable_rds:
    name: Test Olden-Reverted (not macro-expanded, -alltypes, CCured solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Olden-Reverted
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds
          tar -xvzf ${{env.benchmark_tar_dir}}/Olden-reverted.tar.gz
          cd Olden-reverted
          for i in bh bisort em3d health mst perimeter power treeadd tsp voronoi ; do \
            (cd $i ; bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE") \
          done

      - name: Convert bh
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-reverted/bh
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of bh
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-reverted/bh
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of bh
        uses: actions/upload-artifact@v2
        with:
          name: bh_no_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-reverted/bh/3c_performance_stats/
          retention-days: 5

      - name: Build converted bh (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-reverted/bh
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo bh >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-reverted/failed-components-list.txt

      - name: Convert bisort
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-reverted/bisort
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of bisort
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-reverted/bisort
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of bisort
        uses: actions/upload-artifact@v2
        with:
          name: bisort_no_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-reverted/bisort/3c_performance_stats/
          retention-days: 5

      - name: Build converted bisort (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-reverted/bisort
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo bisort >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-reverted/failed-components-list.txt

      - name: Convert em3d
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-reverted/em3d
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of em3d
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-reverted/em3d
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of em3d
        uses: actions/upload-artifact@v2
        with:
          name: em3d_no_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-reverted/em3d/3c_performance_stats/
          retention-days: 5

      - name: Build converted em3d (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-reverted/em3d
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo em3d >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-reverted/failed-components-list.txt

      - name: Convert health
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-reverted/health
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of health
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-reverted/health
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of health
        uses: actions/upload-artifact@v2
        with:
          name: health_no_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-reverted/health/3c_performance_stats/
          retention-days: 5

      - name: Build converted health (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-reverted/health
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo health >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-reverted/failed-components-list.txt

      - name: Convert mst
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-reverted/mst
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of mst
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-reverted/mst
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of mst
        uses: actions/upload-artifact@v2
        with:
          name: mst_no_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-reverted/mst/3c_performance_stats/
          retention-days: 5

      - name: Build converted mst (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-reverted/mst
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo mst >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-reverted/failed-components-list.txt

      - name: Convert perimeter
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-reverted/perimeter
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of perimeter
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-reverted/perimeter
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of perimeter
        uses: actions/upload-artifact@v2
        with:
          name: perimeter_no_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-reverted/perimeter/3c_performance_stats/
          retention-days: 5

      - name: Build converted perimeter (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-reverted/perimeter
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo perimeter >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-reverted/failed-components-list.txt

      - name: Convert power
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-reverted/power
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of power
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-reverted/power
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of power
        uses: actions/upload-artifact@v2
        with:
          name: power_no_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-reverted/power/3c_performance_stats/
          retention-days: 5

      - name: Build converted power (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-reverted/power
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo power >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-reverted/failed-components-list.txt

      - name: Convert treeadd
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-reverted/treeadd
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of treeadd
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-reverted/treeadd
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of treeadd
        uses: actions/upload-artifact@v2
        with:
          name: treeadd_no_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-reverted/treeadd/3c_performance_stats/
          retention-days: 5

      - name: Build converted treeadd (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-reverted/treeadd
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo treeadd >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-reverted/failed-components-list.txt

      - name: Convert tsp
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-reverted/tsp
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of tsp
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-reverted/tsp
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of tsp
        uses: actions/upload-artifact@v2
        with:
          name: tsp_no_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-reverted/tsp/3c_performance_stats/
          retention-days: 5

      - name: Build converted tsp (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-reverted/tsp
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo tsp >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-reverted/failed-components-list.txt

      - name: Convert voronoi
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-reverted/voronoi
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of voronoi
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-reverted/voronoi
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of voronoi
        uses: actions/upload-artifact@v2
        with:
          name: voronoi_no_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-reverted/voronoi/3c_performance_stats/
          retention-days: 5

      - name: Build converted voronoi (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-reverted/voronoi
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo voronoi >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-reverted/failed-components-list.txt

      - name: Check for deferred post-conversion build failures
        run: |
          if [ -e ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-reverted/failed-components-list.txt ]; then
              echo 'Failed components (see previous post-conversion build steps):'
              cat ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-reverted/failed-components-list.txt
              exit 1
          fi

  test_Olden-Reverted_no_expand_macros_alltypes_disable_fnedgs:
    name: Test Olden-Reverted (not macro-expanded, -alltypes, FuncRevEdges solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Olden-Reverted
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs
          tar -xvzf ${{env.benchmark_tar_dir}}/Olden-reverted.tar.gz
          cd Olden-reverted
          for i in bh bisort em3d health mst perimeter power treeadd tsp voronoi ; do \
            (cd $i ; bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE") \
          done

      - name: Convert bh
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-reverted/bh
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of bh
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-reverted/bh
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of bh
        uses: actions/upload-artifact@v2
        with:
          name: bh_no_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-reverted/bh/3c_performance_stats/
          retention-days: 5

      - name: Build converted bh (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-reverted/bh
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo bh >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-reverted/failed-components-list.txt

      - name: Convert bisort
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-reverted/bisort
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of bisort
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-reverted/bisort
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of bisort
        uses: actions/upload-artifact@v2
        with:
          name: bisort_no_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-reverted/bisort/3c_performance_stats/
          retention-days: 5

      - name: Build converted bisort (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-reverted/bisort
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo bisort >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-reverted/failed-components-list.txt

      - name: Convert em3d
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-reverted/em3d
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of em3d
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-reverted/em3d
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of em3d
        uses: actions/upload-artifact@v2
        with:
          name: em3d_no_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-reverted/em3d/3c_performance_stats/
          retention-days: 5

      - name: Build converted em3d (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-reverted/em3d
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo em3d >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-reverted/failed-components-list.txt

      - name: Convert health
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-reverted/health
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of health
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-reverted/health
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of health
        uses: actions/upload-artifact@v2
        with:
          name: health_no_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-reverted/health/3c_performance_stats/
          retention-days: 5

      - name: Build converted health (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-reverted/health
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo health >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-reverted/failed-components-list.txt

      - name: Convert mst
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-reverted/mst
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of mst
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-reverted/mst
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of mst
        uses: actions/upload-artifact@v2
        with:
          name: mst_no_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-reverted/mst/3c_performance_stats/
          retention-days: 5

      - name: Build converted mst (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-reverted/mst
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo mst >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-reverted/failed-components-list.txt

      - name: Convert perimeter
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-reverted/perimeter
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of perimeter
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-reverted/perimeter
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of perimeter
        uses: actions/upload-artifact@v2
        with:
          name: perimeter_no_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-reverted/perimeter/3c_performance_stats/
          retention-days: 5

      - name: Build converted perimeter (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-reverted/perimeter
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo perimeter >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-reverted/failed-components-list.txt

      - name: Convert power
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-reverted/power
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of power
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-reverted/power
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of power
        uses: actions/upload-artifact@v2
        with:
          name: power_no_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-reverted/power/3c_performance_stats/
          retention-days: 5

      - name: Build converted power (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-reverted/power
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo power >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-reverted/failed-components-list.txt

      - name: Convert treeadd
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-reverted/treeadd
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of treeadd
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-reverted/treeadd
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of treeadd
        uses: actions/upload-artifact@v2
        with:
          name: treeadd_no_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-reverted/treeadd/3c_performance_stats/
          retention-days: 5

      - name: Build converted treeadd (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-reverted/treeadd
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo treeadd >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-reverted/failed-components-list.txt

      - name: Convert tsp
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-reverted/tsp
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of tsp
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-reverted/tsp
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of tsp
        uses: actions/upload-artifact@v2
        with:
          name: tsp_no_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-reverted/tsp/3c_performance_stats/
          retention-days: 5

      - name: Build converted tsp (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-reverted/tsp
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo tsp >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-reverted/failed-components-list.txt

      - name: Convert voronoi
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-reverted/voronoi
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of voronoi
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-reverted/voronoi
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of voronoi
        uses: actions/upload-artifact@v2
        with:
          name: voronoi_no_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-reverted/voronoi/3c_performance_stats/
          retention-days: 5

      - name: Build converted voronoi (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-reverted/voronoi
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo voronoi >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-reverted/failed-components-list.txt

      - name: Check for deferred post-conversion build failures
        run: |
          if [ -e ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-reverted/failed-components-list.txt ]; then
              echo 'Failed components (see previous post-conversion build steps):'
              cat ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-reverted/failed-components-list.txt
              exit 1
          fi

  test_Olden-Reverted_no_expand_macros_alltypes:
    name: Test Olden-Reverted (not macro-expanded, -alltypes)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Olden-Reverted
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes
          tar -xvzf ${{env.benchmark_tar_dir}}/Olden-reverted.tar.gz
          cd Olden-reverted
          for i in bh bisort em3d health mst perimeter power treeadd tsp voronoi ; do \
            (cd $i ; bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE") \
          done

      - name: Convert bh
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-reverted/bh
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --project_path .

      - name: Copy 3c stats of bh
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-reverted/bh
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of bh
        uses: actions/upload-artifact@v2
        with:
          name: bh_no_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-reverted/bh/3c_performance_stats/
          retention-days: 5

      - name: Build converted bh (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-reverted/bh
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo bh >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-reverted/failed-components-list.txt

      - name: Convert bisort
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-reverted/bisort
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --project_path .

      - name: Copy 3c stats of bisort
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-reverted/bisort
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of bisort
        uses: actions/upload-artifact@v2
        with:
          name: bisort_no_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-reverted/bisort/3c_performance_stats/
          retention-days: 5

      - name: Build converted bisort (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-reverted/bisort
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo bisort >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-reverted/failed-components-list.txt

      - name: Convert em3d
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-reverted/em3d
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --project_path .

      - name: Copy 3c stats of em3d
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-reverted/em3d
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of em3d
        uses: actions/upload-artifact@v2
        with:
          name: em3d_no_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-reverted/em3d/3c_performance_stats/
          retention-days: 5

      - name: Build converted em3d (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-reverted/em3d
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo em3d >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-reverted/failed-components-list.txt

      - name: Convert health
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-reverted/health
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --project_path .

      - name: Copy 3c stats of health
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-reverted/health
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of health
        uses: actions/upload-artifact@v2
        with:
          name: health_no_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-reverted/health/3c_performance_stats/
          retention-days: 5

      - name: Build converted health (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-reverted/health
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo health >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-reverted/failed-components-list.txt

      - name: Convert mst
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-reverted/mst
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --project_path .

      - name: Copy 3c stats of mst
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-reverted/mst
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of mst
        uses: actions/upload-artifact@v2
        with:
          name: mst_no_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-reverted/mst/3c_performance_stats/
          retention-days: 5

      - name: Build converted mst (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-reverted/mst
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo mst >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-reverted/failed-components-list.txt

      - name: Convert perimeter
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-reverted/perimeter
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --project_path .

      - name: Copy 3c stats of perimeter
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-reverted/perimeter
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of perimeter
        uses: actions/upload-artifact@v2
        with:
          name: perimeter_no_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-reverted/perimeter/3c_performance_stats/
          retention-days: 5

      - name: Build converted perimeter (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-reverted/perimeter
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo perimeter >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-reverted/failed-components-list.txt

      - name: Convert power
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-reverted/power
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --project_path .

      - name: Copy 3c stats of power
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-reverted/power
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of power
        uses: actions/upload-artifact@v2
        with:
          name: power_no_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-reverted/power/3c_performance_stats/
          retention-days: 5

      - name: Build converted power (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-reverted/power
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo power >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-reverted/failed-components-list.txt

      - name: Convert treeadd
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-reverted/treeadd
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --project_path .

      - name: Copy 3c stats of treeadd
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-reverted/treeadd
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of treeadd
        uses: actions/upload-artifact@v2
        with:
          name: treeadd_no_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-reverted/treeadd/3c_performance_stats/
          retention-days: 5

      - name: Build converted treeadd (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-reverted/treeadd
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo treeadd >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-reverted/failed-components-list.txt

      - name: Convert tsp
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-reverted/tsp
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --project_path .

      - name: Copy 3c stats of tsp
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-reverted/tsp
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of tsp
        uses: actions/upload-artifact@v2
        with:
          name: tsp_no_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-reverted/tsp/3c_performance_stats/
          retention-days: 5

      - name: Build converted tsp (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-reverted/tsp
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo tsp >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-reverted/failed-components-list.txt

      - name: Convert voronoi
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-reverted/voronoi
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --project_path .

      - name: Copy 3c stats of voronoi
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-reverted/voronoi
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of voronoi
        uses: actions/upload-artifact@v2
        with:
          name: voronoi_no_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-reverted/voronoi/3c_performance_stats/
          retention-days: 5

      - name: Build converted voronoi (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-reverted/voronoi
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo voronoi >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-reverted/failed-components-list.txt

      - name: Check for deferred post-conversion build failures
        run: |
          if [ -e ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-reverted/failed-components-list.txt ]; then
              echo 'Failed components (see previous post-conversion build steps):'
              cat ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-reverted/failed-components-list.txt
              exit 1
          fi

  test_Olden-Reverted_expand_macros_no_alltypes:
    name: Test Olden-Reverted (macro-expanded, no -alltypes)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Olden-Reverted
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes
          tar -xvzf ${{env.benchmark_tar_dir}}/Olden-reverted.tar.gz
          cd Olden-reverted
          for i in bh bisort em3d health mst perimeter power treeadd tsp voronoi ; do \
            (cd $i ; bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE") \
          done

      - name: Convert bh
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-reverted/bh
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of bh
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-reverted/bh
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of bh
        uses: actions/upload-artifact@v2
        with:
          name: bh_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-reverted/bh/3c_performance_stats/
          retention-days: 5

      - name: Build converted bh (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-reverted/bh
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" || echo bh >>${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-reverted/failed-components-list.txt

      - name: Convert bisort
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-reverted/bisort
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of bisort
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-reverted/bisort
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of bisort
        uses: actions/upload-artifact@v2
        with:
          name: bisort_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-reverted/bisort/3c_performance_stats/
          retention-days: 5

      - name: Build converted bisort (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-reverted/bisort
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" || echo bisort >>${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-reverted/failed-components-list.txt

      - name: Convert em3d
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-reverted/em3d
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of em3d
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-reverted/em3d
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of em3d
        uses: actions/upload-artifact@v2
        with:
          name: em3d_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-reverted/em3d/3c_performance_stats/
          retention-days: 5

      - name: Build converted em3d (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-reverted/em3d
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" || echo em3d >>${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-reverted/failed-components-list.txt

      - name: Convert health
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-reverted/health
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of health
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-reverted/health
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of health
        uses: actions/upload-artifact@v2
        with:
          name: health_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-reverted/health/3c_performance_stats/
          retention-days: 5

      - name: Build converted health (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-reverted/health
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" || echo health >>${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-reverted/failed-components-list.txt

      - name: Convert mst
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-reverted/mst
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of mst
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-reverted/mst
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of mst
        uses: actions/upload-artifact@v2
        with:
          name: mst_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-reverted/mst/3c_performance_stats/
          retention-days: 5

      - name: Build converted mst (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-reverted/mst
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" || echo mst >>${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-reverted/failed-components-list.txt

      - name: Convert perimeter
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-reverted/perimeter
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of perimeter
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-reverted/perimeter
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of perimeter
        uses: actions/upload-artifact@v2
        with:
          name: perimeter_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-reverted/perimeter/3c_performance_stats/
          retention-days: 5

      - name: Build converted perimeter (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-reverted/perimeter
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" || echo perimeter >>${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-reverted/failed-components-list.txt

      - name: Convert power
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-reverted/power
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of power
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-reverted/power
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of power
        uses: actions/upload-artifact@v2
        with:
          name: power_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-reverted/power/3c_performance_stats/
          retention-days: 5

      - name: Build converted power (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-reverted/power
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" || echo power >>${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-reverted/failed-components-list.txt

      - name: Convert treeadd
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-reverted/treeadd
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of treeadd
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-reverted/treeadd
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of treeadd
        uses: actions/upload-artifact@v2
        with:
          name: treeadd_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-reverted/treeadd/3c_performance_stats/
          retention-days: 5

      - name: Build converted treeadd (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-reverted/treeadd
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" || echo treeadd >>${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-reverted/failed-components-list.txt

      - name: Convert tsp
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-reverted/tsp
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of tsp
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-reverted/tsp
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of tsp
        uses: actions/upload-artifact@v2
        with:
          name: tsp_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-reverted/tsp/3c_performance_stats/
          retention-days: 5

      - name: Build converted tsp (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-reverted/tsp
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" || echo tsp >>${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-reverted/failed-components-list.txt

      - name: Convert voronoi
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-reverted/voronoi
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of voronoi
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-reverted/voronoi
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of voronoi
        uses: actions/upload-artifact@v2
        with:
          name: voronoi_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-reverted/voronoi/3c_performance_stats/
          retention-days: 5

      - name: Build converted voronoi (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-reverted/voronoi
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" || echo voronoi >>${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-reverted/failed-components-list.txt

      - name: Check for deferred post-conversion build failures
        run: |
          if [ -e ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-reverted/failed-components-list.txt ]; then
              echo 'Failed components (see previous post-conversion build steps):'
              cat ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-reverted/failed-components-list.txt
              exit 1
          fi

  test_Olden-Reverted_expand_macros_alltypes_only_g_sol:
    name: Test Olden-Reverted (macro-expanded, -alltypes, greatest solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Olden-Reverted
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol
          tar -xvzf ${{env.benchmark_tar_dir}}/Olden-reverted.tar.gz
          cd Olden-reverted
          for i in bh bisort em3d health mst perimeter power treeadd tsp voronoi ; do \
            (cd $i ; bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE") \
          done

      - name: Convert bh
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-reverted/bh
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of bh
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-reverted/bh
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of bh
        uses: actions/upload-artifact@v2
        with:
          name: bh_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-reverted/bh/3c_performance_stats/
          retention-days: 5

      - name: Build converted bh (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-reverted/bh
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo bh >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-reverted/failed-components-list.txt

      - name: Convert bisort
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-reverted/bisort
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of bisort
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-reverted/bisort
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of bisort
        uses: actions/upload-artifact@v2
        with:
          name: bisort_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-reverted/bisort/3c_performance_stats/
          retention-days: 5

      - name: Build converted bisort (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-reverted/bisort
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo bisort >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-reverted/failed-components-list.txt

      - name: Convert em3d
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-reverted/em3d
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of em3d
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-reverted/em3d
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of em3d
        uses: actions/upload-artifact@v2
        with:
          name: em3d_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-reverted/em3d/3c_performance_stats/
          retention-days: 5

      - name: Build converted em3d (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-reverted/em3d
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo em3d >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-reverted/failed-components-list.txt

      - name: Convert health
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-reverted/health
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of health
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-reverted/health
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of health
        uses: actions/upload-artifact@v2
        with:
          name: health_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-reverted/health/3c_performance_stats/
          retention-days: 5

      - name: Build converted health (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-reverted/health
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo health >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-reverted/failed-components-list.txt

      - name: Convert mst
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-reverted/mst
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of mst
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-reverted/mst
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of mst
        uses: actions/upload-artifact@v2
        with:
          name: mst_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-reverted/mst/3c_performance_stats/
          retention-days: 5

      - name: Build converted mst (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-reverted/mst
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo mst >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-reverted/failed-components-list.txt

      - name: Convert perimeter
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-reverted/perimeter
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of perimeter
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-reverted/perimeter
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of perimeter
        uses: actions/upload-artifact@v2
        with:
          name: perimeter_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-reverted/perimeter/3c_performance_stats/
          retention-days: 5

      - name: Build converted perimeter (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-reverted/perimeter
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo perimeter >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-reverted/failed-components-list.txt

      - name: Convert power
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-reverted/power
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of power
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-reverted/power
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of power
        uses: actions/upload-artifact@v2
        with:
          name: power_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-reverted/power/3c_performance_stats/
          retention-days: 5

      - name: Build converted power (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-reverted/power
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo power >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-reverted/failed-components-list.txt

      - name: Convert treeadd
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-reverted/treeadd
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of treeadd
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-reverted/treeadd
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of treeadd
        uses: actions/upload-artifact@v2
        with:
          name: treeadd_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-reverted/treeadd/3c_performance_stats/
          retention-days: 5

      - name: Build converted treeadd (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-reverted/treeadd
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo treeadd >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-reverted/failed-components-list.txt

      - name: Convert tsp
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-reverted/tsp
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of tsp
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-reverted/tsp
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of tsp
        uses: actions/upload-artifact@v2
        with:
          name: tsp_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-reverted/tsp/3c_performance_stats/
          retention-days: 5

      - name: Build converted tsp (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-reverted/tsp
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo tsp >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-reverted/failed-components-list.txt

      - name: Convert voronoi
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-reverted/voronoi
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of voronoi
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-reverted/voronoi
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of voronoi
        uses: actions/upload-artifact@v2
        with:
          name: voronoi_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-reverted/voronoi/3c_performance_stats/
          retention-days: 5

      - name: Build converted voronoi (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-reverted/voronoi
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo voronoi >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-reverted/failed-components-list.txt

      - name: Check for deferred post-conversion build failures
        run: |
          if [ -e ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-reverted/failed-components-list.txt ]; then
              echo 'Failed components (see previous post-conversion build steps):'
              cat ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-reverted/failed-components-list.txt
              exit 1
          fi

  test_Olden-Reverted_expand_macros_alltypes_only_l_sol:
    name: Test Olden-Reverted (macro-expanded, -alltypes, least solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Olden-Reverted
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol
          tar -xvzf ${{env.benchmark_tar_dir}}/Olden-reverted.tar.gz
          cd Olden-reverted
          for i in bh bisort em3d health mst perimeter power treeadd tsp voronoi ; do \
            (cd $i ; bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE") \
          done

      - name: Convert bh
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-reverted/bh
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of bh
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-reverted/bh
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of bh
        uses: actions/upload-artifact@v2
        with:
          name: bh_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-reverted/bh/3c_performance_stats/
          retention-days: 5

      - name: Build converted bh (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-reverted/bh
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo bh >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-reverted/failed-components-list.txt

      - name: Convert bisort
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-reverted/bisort
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of bisort
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-reverted/bisort
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of bisort
        uses: actions/upload-artifact@v2
        with:
          name: bisort_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-reverted/bisort/3c_performance_stats/
          retention-days: 5

      - name: Build converted bisort (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-reverted/bisort
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo bisort >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-reverted/failed-components-list.txt

      - name: Convert em3d
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-reverted/em3d
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of em3d
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-reverted/em3d
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of em3d
        uses: actions/upload-artifact@v2
        with:
          name: em3d_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-reverted/em3d/3c_performance_stats/
          retention-days: 5

      - name: Build converted em3d (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-reverted/em3d
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo em3d >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-reverted/failed-components-list.txt

      - name: Convert health
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-reverted/health
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of health
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-reverted/health
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of health
        uses: actions/upload-artifact@v2
        with:
          name: health_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-reverted/health/3c_performance_stats/
          retention-days: 5

      - name: Build converted health (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-reverted/health
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo health >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-reverted/failed-components-list.txt

      - name: Convert mst
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-reverted/mst
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of mst
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-reverted/mst
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of mst
        uses: actions/upload-artifact@v2
        with:
          name: mst_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-reverted/mst/3c_performance_stats/
          retention-days: 5

      - name: Build converted mst (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-reverted/mst
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo mst >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-reverted/failed-components-list.txt

      - name: Convert perimeter
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-reverted/perimeter
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of perimeter
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-reverted/perimeter
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of perimeter
        uses: actions/upload-artifact@v2
        with:
          name: perimeter_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-reverted/perimeter/3c_performance_stats/
          retention-days: 5

      - name: Build converted perimeter (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-reverted/perimeter
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo perimeter >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-reverted/failed-components-list.txt

      - name: Convert power
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-reverted/power
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of power
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-reverted/power
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of power
        uses: actions/upload-artifact@v2
        with:
          name: power_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-reverted/power/3c_performance_stats/
          retention-days: 5

      - name: Build converted power (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-reverted/power
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo power >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-reverted/failed-components-list.txt

      - name: Convert treeadd
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-reverted/treeadd
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of treeadd
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-reverted/treeadd
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of treeadd
        uses: actions/upload-artifact@v2
        with:
          name: treeadd_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-reverted/treeadd/3c_performance_stats/
          retention-days: 5

      - name: Build converted treeadd (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-reverted/treeadd
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo treeadd >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-reverted/failed-components-list.txt

      - name: Convert tsp
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-reverted/tsp
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of tsp
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-reverted/tsp
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of tsp
        uses: actions/upload-artifact@v2
        with:
          name: tsp_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-reverted/tsp/3c_performance_stats/
          retention-days: 5

      - name: Build converted tsp (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-reverted/tsp
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo tsp >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-reverted/failed-components-list.txt

      - name: Convert voronoi
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-reverted/voronoi
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of voronoi
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-reverted/voronoi
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of voronoi
        uses: actions/upload-artifact@v2
        with:
          name: voronoi_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-reverted/voronoi/3c_performance_stats/
          retention-days: 5

      - name: Build converted voronoi (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-reverted/voronoi
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo voronoi >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-reverted/failed-components-list.txt

      - name: Check for deferred post-conversion build failures
        run: |
          if [ -e ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-reverted/failed-components-list.txt ]; then
              echo 'Failed components (see previous post-conversion build steps):'
              cat ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-reverted/failed-components-list.txt
              exit 1
          fi

  test_Olden-Reverted_expand_macros_alltypes_disable_rds:
    name: Test Olden-Reverted (macro-expanded, -alltypes, CCured solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Olden-Reverted
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds
          tar -xvzf ${{env.benchmark_tar_dir}}/Olden-reverted.tar.gz
          cd Olden-reverted
          for i in bh bisort em3d health mst perimeter power treeadd tsp voronoi ; do \
            (cd $i ; bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE") \
          done

      - name: Convert bh
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-reverted/bh
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of bh
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-reverted/bh
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of bh
        uses: actions/upload-artifact@v2
        with:
          name: bh_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-reverted/bh/3c_performance_stats/
          retention-days: 5

      - name: Build converted bh (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-reverted/bh
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo bh >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-reverted/failed-components-list.txt

      - name: Convert bisort
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-reverted/bisort
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of bisort
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-reverted/bisort
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of bisort
        uses: actions/upload-artifact@v2
        with:
          name: bisort_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-reverted/bisort/3c_performance_stats/
          retention-days: 5

      - name: Build converted bisort (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-reverted/bisort
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo bisort >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-reverted/failed-components-list.txt

      - name: Convert em3d
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-reverted/em3d
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of em3d
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-reverted/em3d
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of em3d
        uses: actions/upload-artifact@v2
        with:
          name: em3d_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-reverted/em3d/3c_performance_stats/
          retention-days: 5

      - name: Build converted em3d (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-reverted/em3d
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo em3d >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-reverted/failed-components-list.txt

      - name: Convert health
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-reverted/health
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of health
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-reverted/health
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of health
        uses: actions/upload-artifact@v2
        with:
          name: health_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-reverted/health/3c_performance_stats/
          retention-days: 5

      - name: Build converted health (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-reverted/health
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo health >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-reverted/failed-components-list.txt

      - name: Convert mst
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-reverted/mst
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of mst
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-reverted/mst
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of mst
        uses: actions/upload-artifact@v2
        with:
          name: mst_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-reverted/mst/3c_performance_stats/
          retention-days: 5

      - name: Build converted mst (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-reverted/mst
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo mst >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-reverted/failed-components-list.txt

      - name: Convert perimeter
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-reverted/perimeter
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of perimeter
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-reverted/perimeter
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of perimeter
        uses: actions/upload-artifact@v2
        with:
          name: perimeter_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-reverted/perimeter/3c_performance_stats/
          retention-days: 5

      - name: Build converted perimeter (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-reverted/perimeter
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo perimeter >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-reverted/failed-components-list.txt

      - name: Convert power
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-reverted/power
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of power
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-reverted/power
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of power
        uses: actions/upload-artifact@v2
        with:
          name: power_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-reverted/power/3c_performance_stats/
          retention-days: 5

      - name: Build converted power (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-reverted/power
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo power >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-reverted/failed-components-list.txt

      - name: Convert treeadd
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-reverted/treeadd
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of treeadd
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-reverted/treeadd
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of treeadd
        uses: actions/upload-artifact@v2
        with:
          name: treeadd_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-reverted/treeadd/3c_performance_stats/
          retention-days: 5

      - name: Build converted treeadd (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-reverted/treeadd
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo treeadd >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-reverted/failed-components-list.txt

      - name: Convert tsp
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-reverted/tsp
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of tsp
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-reverted/tsp
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of tsp
        uses: actions/upload-artifact@v2
        with:
          name: tsp_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-reverted/tsp/3c_performance_stats/
          retention-days: 5

      - name: Build converted tsp (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-reverted/tsp
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo tsp >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-reverted/failed-components-list.txt

      - name: Convert voronoi
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-reverted/voronoi
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of voronoi
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-reverted/voronoi
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of voronoi
        uses: actions/upload-artifact@v2
        with:
          name: voronoi_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-reverted/voronoi/3c_performance_stats/
          retention-days: 5

      - name: Build converted voronoi (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-reverted/voronoi
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo voronoi >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-reverted/failed-components-list.txt

      - name: Check for deferred post-conversion build failures
        run: |
          if [ -e ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-reverted/failed-components-list.txt ]; then
              echo 'Failed components (see previous post-conversion build steps):'
              cat ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-reverted/failed-components-list.txt
              exit 1
          fi

  test_Olden-Reverted_expand_macros_alltypes_disable_fnedgs:
    name: Test Olden-Reverted (macro-expanded, -alltypes, FuncRevEdges solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Olden-Reverted
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs
          tar -xvzf ${{env.benchmark_tar_dir}}/Olden-reverted.tar.gz
          cd Olden-reverted
          for i in bh bisort em3d health mst perimeter power treeadd tsp voronoi ; do \
            (cd $i ; bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE") \
          done

      - name: Convert bh
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-reverted/bh
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of bh
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-reverted/bh
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of bh
        uses: actions/upload-artifact@v2
        with:
          name: bh_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-reverted/bh/3c_performance_stats/
          retention-days: 5

      - name: Build converted bh (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-reverted/bh
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo bh >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-reverted/failed-components-list.txt

      - name: Convert bisort
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-reverted/bisort
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of bisort
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-reverted/bisort
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of bisort
        uses: actions/upload-artifact@v2
        with:
          name: bisort_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-reverted/bisort/3c_performance_stats/
          retention-days: 5

      - name: Build converted bisort (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-reverted/bisort
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo bisort >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-reverted/failed-components-list.txt

      - name: Convert em3d
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-reverted/em3d
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of em3d
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-reverted/em3d
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of em3d
        uses: actions/upload-artifact@v2
        with:
          name: em3d_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-reverted/em3d/3c_performance_stats/
          retention-days: 5

      - name: Build converted em3d (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-reverted/em3d
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo em3d >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-reverted/failed-components-list.txt

      - name: Convert health
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-reverted/health
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of health
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-reverted/health
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of health
        uses: actions/upload-artifact@v2
        with:
          name: health_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-reverted/health/3c_performance_stats/
          retention-days: 5

      - name: Build converted health (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-reverted/health
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo health >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-reverted/failed-components-list.txt

      - name: Convert mst
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-reverted/mst
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of mst
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-reverted/mst
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of mst
        uses: actions/upload-artifact@v2
        with:
          name: mst_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-reverted/mst/3c_performance_stats/
          retention-days: 5

      - name: Build converted mst (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-reverted/mst
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo mst >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-reverted/failed-components-list.txt

      - name: Convert perimeter
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-reverted/perimeter
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of perimeter
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-reverted/perimeter
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of perimeter
        uses: actions/upload-artifact@v2
        with:
          name: perimeter_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-reverted/perimeter/3c_performance_stats/
          retention-days: 5

      - name: Build converted perimeter (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-reverted/perimeter
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo perimeter >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-reverted/failed-components-list.txt

      - name: Convert power
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-reverted/power
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of power
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-reverted/power
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of power
        uses: actions/upload-artifact@v2
        with:
          name: power_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-reverted/power/3c_performance_stats/
          retention-days: 5

      - name: Build converted power (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-reverted/power
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo power >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-reverted/failed-components-list.txt

      - name: Convert treeadd
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-reverted/treeadd
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of treeadd
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-reverted/treeadd
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of treeadd
        uses: actions/upload-artifact@v2
        with:
          name: treeadd_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-reverted/treeadd/3c_performance_stats/
          retention-days: 5

      - name: Build converted treeadd (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-reverted/treeadd
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo treeadd >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-reverted/failed-components-list.txt

      - name: Convert tsp
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-reverted/tsp
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of tsp
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-reverted/tsp
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of tsp
        uses: actions/upload-artifact@v2
        with:
          name: tsp_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-reverted/tsp/3c_performance_stats/
          retention-days: 5

      - name: Build converted tsp (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-reverted/tsp
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo tsp >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-reverted/failed-components-list.txt

      - name: Convert voronoi
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-reverted/voronoi
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of voronoi
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-reverted/voronoi
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of voronoi
        uses: actions/upload-artifact@v2
        with:
          name: voronoi_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-reverted/voronoi/3c_performance_stats/
          retention-days: 5

      - name: Build converted voronoi (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-reverted/voronoi
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo voronoi >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-reverted/failed-components-list.txt

      - name: Check for deferred post-conversion build failures
        run: |
          if [ -e ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-reverted/failed-components-list.txt ]; then
              echo 'Failed components (see previous post-conversion build steps):'
              cat ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-reverted/failed-components-list.txt
              exit 1
          fi

  test_Olden-Reverted_expand_macros_alltypes:
    name: Test Olden-Reverted (macro-expanded, -alltypes)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Olden-Reverted
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/expand_macros_alltypes
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes
          tar -xvzf ${{env.benchmark_tar_dir}}/Olden-reverted.tar.gz
          cd Olden-reverted
          for i in bh bisort em3d health mst perimeter power treeadd tsp voronoi ; do \
            (cd $i ; bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE") \
          done

      - name: Convert bh
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-reverted/bh
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of bh
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-reverted/bh
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of bh
        uses: actions/upload-artifact@v2
        with:
          name: bh_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-reverted/bh/3c_performance_stats/
          retention-days: 5

      - name: Build converted bh (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-reverted/bh
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo bh >>${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-reverted/failed-components-list.txt

      - name: Convert bisort
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-reverted/bisort
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of bisort
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-reverted/bisort
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of bisort
        uses: actions/upload-artifact@v2
        with:
          name: bisort_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-reverted/bisort/3c_performance_stats/
          retention-days: 5

      - name: Build converted bisort (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-reverted/bisort
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo bisort >>${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-reverted/failed-components-list.txt

      - name: Convert em3d
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-reverted/em3d
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of em3d
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-reverted/em3d
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of em3d
        uses: actions/upload-artifact@v2
        with:
          name: em3d_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-reverted/em3d/3c_performance_stats/
          retention-days: 5

      - name: Build converted em3d (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-reverted/em3d
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo em3d >>${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-reverted/failed-components-list.txt

      - name: Convert health
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-reverted/health
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of health
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-reverted/health
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of health
        uses: actions/upload-artifact@v2
        with:
          name: health_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-reverted/health/3c_performance_stats/
          retention-days: 5

      - name: Build converted health (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-reverted/health
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo health >>${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-reverted/failed-components-list.txt

      - name: Convert mst
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-reverted/mst
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of mst
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-reverted/mst
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of mst
        uses: actions/upload-artifact@v2
        with:
          name: mst_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-reverted/mst/3c_performance_stats/
          retention-days: 5

      - name: Build converted mst (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-reverted/mst
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo mst >>${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-reverted/failed-components-list.txt

      - name: Convert perimeter
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-reverted/perimeter
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of perimeter
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-reverted/perimeter
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of perimeter
        uses: actions/upload-artifact@v2
        with:
          name: perimeter_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-reverted/perimeter/3c_performance_stats/
          retention-days: 5

      - name: Build converted perimeter (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-reverted/perimeter
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo perimeter >>${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-reverted/failed-components-list.txt

      - name: Convert power
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-reverted/power
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of power
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-reverted/power
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of power
        uses: actions/upload-artifact@v2
        with:
          name: power_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-reverted/power/3c_performance_stats/
          retention-days: 5

      - name: Build converted power (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-reverted/power
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo power >>${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-reverted/failed-components-list.txt

      - name: Convert treeadd
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-reverted/treeadd
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of treeadd
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-reverted/treeadd
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of treeadd
        uses: actions/upload-artifact@v2
        with:
          name: treeadd_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-reverted/treeadd/3c_performance_stats/
          retention-days: 5

      - name: Build converted treeadd (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-reverted/treeadd
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo treeadd >>${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-reverted/failed-components-list.txt

      - name: Convert tsp
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-reverted/tsp
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of tsp
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-reverted/tsp
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of tsp
        uses: actions/upload-artifact@v2
        with:
          name: tsp_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-reverted/tsp/3c_performance_stats/
          retention-days: 5

      - name: Build converted tsp (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-reverted/tsp
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo tsp >>${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-reverted/failed-components-list.txt

      - name: Convert voronoi
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-reverted/voronoi
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of voronoi
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-reverted/voronoi
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of voronoi
        uses: actions/upload-artifact@v2
        with:
          name: voronoi_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-reverted/voronoi/3c_performance_stats/
          retention-days: 5

      - name: Build converted voronoi (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-reverted/voronoi
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo voronoi >>${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-reverted/failed-components-list.txt

      - name: Check for deferred post-conversion build failures
        run: |
          if [ -e ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-reverted/failed-components-list.txt ]; then
              echo 'Failed components (see previous post-conversion build steps):'
              cat ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-reverted/failed-components-list.txt
              exit 1
          fi

  test_Olden-Manual_no_expand_macros_no_alltypes:
    name: Test Olden-Manual (not macro-expanded, no -alltypes)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Olden-Manual
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes
          tar -xvzf ${{env.benchmark_tar_dir}}/Olden-manual.tar.gz
          cd Olden-manual
          for i in bh bisort em3d health mst perimeter power treeadd tsp voronoi ; do \
            (cd $i ; bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE") \
          done

      - name: Convert bh
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-manual/bh
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --project_path .

      - name: Copy 3c stats of bh
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-manual/bh
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of bh
        uses: actions/upload-artifact@v2
        with:
          name: bh_no_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-manual/bh/3c_performance_stats/
          retention-days: 5

      - name: Build converted bh (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-manual/bh
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" || echo bh >>${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-manual/failed-components-list.txt

      - name: Convert bisort
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-manual/bisort
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --project_path .

      - name: Copy 3c stats of bisort
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-manual/bisort
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of bisort
        uses: actions/upload-artifact@v2
        with:
          name: bisort_no_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-manual/bisort/3c_performance_stats/
          retention-days: 5

      - name: Build converted bisort (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-manual/bisort
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" || echo bisort >>${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-manual/failed-components-list.txt

      - name: Convert em3d
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-manual/em3d
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --project_path .

      - name: Copy 3c stats of em3d
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-manual/em3d
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of em3d
        uses: actions/upload-artifact@v2
        with:
          name: em3d_no_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-manual/em3d/3c_performance_stats/
          retention-days: 5

      - name: Build converted em3d (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-manual/em3d
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" || echo em3d >>${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-manual/failed-components-list.txt

      - name: Convert health
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-manual/health
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --project_path .

      - name: Copy 3c stats of health
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-manual/health
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of health
        uses: actions/upload-artifact@v2
        with:
          name: health_no_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-manual/health/3c_performance_stats/
          retention-days: 5

      - name: Build converted health (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-manual/health
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" || echo health >>${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-manual/failed-components-list.txt

      - name: Convert mst
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-manual/mst
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --project_path .

      - name: Copy 3c stats of mst
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-manual/mst
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of mst
        uses: actions/upload-artifact@v2
        with:
          name: mst_no_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-manual/mst/3c_performance_stats/
          retention-days: 5

      - name: Build converted mst (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-manual/mst
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" || echo mst >>${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-manual/failed-components-list.txt

      - name: Convert perimeter
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-manual/perimeter
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --project_path .

      - name: Copy 3c stats of perimeter
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-manual/perimeter
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of perimeter
        uses: actions/upload-artifact@v2
        with:
          name: perimeter_no_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-manual/perimeter/3c_performance_stats/
          retention-days: 5

      - name: Build converted perimeter (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-manual/perimeter
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" || echo perimeter >>${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-manual/failed-components-list.txt

      - name: Convert power
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-manual/power
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --project_path .

      - name: Copy 3c stats of power
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-manual/power
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of power
        uses: actions/upload-artifact@v2
        with:
          name: power_no_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-manual/power/3c_performance_stats/
          retention-days: 5

      - name: Build converted power (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-manual/power
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" || echo power >>${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-manual/failed-components-list.txt

      - name: Convert treeadd
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-manual/treeadd
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --project_path .

      - name: Copy 3c stats of treeadd
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-manual/treeadd
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of treeadd
        uses: actions/upload-artifact@v2
        with:
          name: treeadd_no_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-manual/treeadd/3c_performance_stats/
          retention-days: 5

      - name: Build converted treeadd (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-manual/treeadd
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" || echo treeadd >>${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-manual/failed-components-list.txt

      - name: Convert tsp
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-manual/tsp
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --project_path .

      - name: Copy 3c stats of tsp
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-manual/tsp
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of tsp
        uses: actions/upload-artifact@v2
        with:
          name: tsp_no_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-manual/tsp/3c_performance_stats/
          retention-days: 5

      - name: Build converted tsp (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-manual/tsp
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" || echo tsp >>${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-manual/failed-components-list.txt

      - name: Convert voronoi
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-manual/voronoi
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --project_path .

      - name: Copy 3c stats of voronoi
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-manual/voronoi
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of voronoi
        uses: actions/upload-artifact@v2
        with:
          name: voronoi_no_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-manual/voronoi/3c_performance_stats/
          retention-days: 5

      - name: Build converted voronoi (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-manual/voronoi
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" || echo voronoi >>${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-manual/failed-components-list.txt

      - name: Check for deferred post-conversion build failures
        run: |
          if [ -e ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-manual/failed-components-list.txt ]; then
              echo 'Failed components (see previous post-conversion build steps):'
              cat ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/Olden-manual/failed-components-list.txt
              exit 1
          fi

  test_Olden-Manual_no_expand_macros_alltypes_only_g_sol:
    name: Test Olden-Manual (not macro-expanded, -alltypes, greatest solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Olden-Manual
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol
          tar -xvzf ${{env.benchmark_tar_dir}}/Olden-manual.tar.gz
          cd Olden-manual
          for i in bh bisort em3d health mst perimeter power treeadd tsp voronoi ; do \
            (cd $i ; bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE") \
          done

      - name: Convert bh
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-manual/bh
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of bh
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-manual/bh
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of bh
        uses: actions/upload-artifact@v2
        with:
          name: bh_no_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-manual/bh/3c_performance_stats/
          retention-days: 5

      - name: Build converted bh (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-manual/bh
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo bh >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-manual/failed-components-list.txt

      - name: Convert bisort
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-manual/bisort
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of bisort
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-manual/bisort
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of bisort
        uses: actions/upload-artifact@v2
        with:
          name: bisort_no_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-manual/bisort/3c_performance_stats/
          retention-days: 5

      - name: Build converted bisort (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-manual/bisort
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo bisort >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-manual/failed-components-list.txt

      - name: Convert em3d
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-manual/em3d
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of em3d
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-manual/em3d
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of em3d
        uses: actions/upload-artifact@v2
        with:
          name: em3d_no_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-manual/em3d/3c_performance_stats/
          retention-days: 5

      - name: Build converted em3d (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-manual/em3d
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo em3d >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-manual/failed-components-list.txt

      - name: Convert health
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-manual/health
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of health
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-manual/health
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of health
        uses: actions/upload-artifact@v2
        with:
          name: health_no_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-manual/health/3c_performance_stats/
          retention-days: 5

      - name: Build converted health (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-manual/health
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo health >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-manual/failed-components-list.txt

      - name: Convert mst
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-manual/mst
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of mst
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-manual/mst
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of mst
        uses: actions/upload-artifact@v2
        with:
          name: mst_no_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-manual/mst/3c_performance_stats/
          retention-days: 5

      - name: Build converted mst (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-manual/mst
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo mst >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-manual/failed-components-list.txt

      - name: Convert perimeter
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-manual/perimeter
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of perimeter
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-manual/perimeter
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of perimeter
        uses: actions/upload-artifact@v2
        with:
          name: perimeter_no_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-manual/perimeter/3c_performance_stats/
          retention-days: 5

      - name: Build converted perimeter (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-manual/perimeter
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo perimeter >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-manual/failed-components-list.txt

      - name: Convert power
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-manual/power
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of power
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-manual/power
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of power
        uses: actions/upload-artifact@v2
        with:
          name: power_no_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-manual/power/3c_performance_stats/
          retention-days: 5

      - name: Build converted power (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-manual/power
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo power >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-manual/failed-components-list.txt

      - name: Convert treeadd
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-manual/treeadd
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of treeadd
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-manual/treeadd
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of treeadd
        uses: actions/upload-artifact@v2
        with:
          name: treeadd_no_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-manual/treeadd/3c_performance_stats/
          retention-days: 5

      - name: Build converted treeadd (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-manual/treeadd
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo treeadd >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-manual/failed-components-list.txt

      - name: Convert tsp
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-manual/tsp
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of tsp
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-manual/tsp
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of tsp
        uses: actions/upload-artifact@v2
        with:
          name: tsp_no_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-manual/tsp/3c_performance_stats/
          retention-days: 5

      - name: Build converted tsp (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-manual/tsp
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo tsp >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-manual/failed-components-list.txt

      - name: Convert voronoi
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-manual/voronoi
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of voronoi
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-manual/voronoi
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of voronoi
        uses: actions/upload-artifact@v2
        with:
          name: voronoi_no_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-manual/voronoi/3c_performance_stats/
          retention-days: 5

      - name: Build converted voronoi (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-manual/voronoi
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo voronoi >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-manual/failed-components-list.txt

      - name: Check for deferred post-conversion build failures
        run: |
          if [ -e ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-manual/failed-components-list.txt ]; then
              echo 'Failed components (see previous post-conversion build steps):'
              cat ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/Olden-manual/failed-components-list.txt
              exit 1
          fi

  test_Olden-Manual_no_expand_macros_alltypes_only_l_sol:
    name: Test Olden-Manual (not macro-expanded, -alltypes, least solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Olden-Manual
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol
          tar -xvzf ${{env.benchmark_tar_dir}}/Olden-manual.tar.gz
          cd Olden-manual
          for i in bh bisort em3d health mst perimeter power treeadd tsp voronoi ; do \
            (cd $i ; bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE") \
          done

      - name: Convert bh
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-manual/bh
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of bh
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-manual/bh
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of bh
        uses: actions/upload-artifact@v2
        with:
          name: bh_no_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-manual/bh/3c_performance_stats/
          retention-days: 5

      - name: Build converted bh (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-manual/bh
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo bh >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-manual/failed-components-list.txt

      - name: Convert bisort
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-manual/bisort
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of bisort
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-manual/bisort
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of bisort
        uses: actions/upload-artifact@v2
        with:
          name: bisort_no_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-manual/bisort/3c_performance_stats/
          retention-days: 5

      - name: Build converted bisort (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-manual/bisort
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo bisort >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-manual/failed-components-list.txt

      - name: Convert em3d
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-manual/em3d
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of em3d
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-manual/em3d
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of em3d
        uses: actions/upload-artifact@v2
        with:
          name: em3d_no_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-manual/em3d/3c_performance_stats/
          retention-days: 5

      - name: Build converted em3d (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-manual/em3d
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo em3d >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-manual/failed-components-list.txt

      - name: Convert health
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-manual/health
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of health
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-manual/health
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of health
        uses: actions/upload-artifact@v2
        with:
          name: health_no_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-manual/health/3c_performance_stats/
          retention-days: 5

      - name: Build converted health (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-manual/health
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo health >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-manual/failed-components-list.txt

      - name: Convert mst
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-manual/mst
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of mst
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-manual/mst
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of mst
        uses: actions/upload-artifact@v2
        with:
          name: mst_no_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-manual/mst/3c_performance_stats/
          retention-days: 5

      - name: Build converted mst (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-manual/mst
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo mst >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-manual/failed-components-list.txt

      - name: Convert perimeter
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-manual/perimeter
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of perimeter
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-manual/perimeter
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of perimeter
        uses: actions/upload-artifact@v2
        with:
          name: perimeter_no_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-manual/perimeter/3c_performance_stats/
          retention-days: 5

      - name: Build converted perimeter (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-manual/perimeter
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo perimeter >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-manual/failed-components-list.txt

      - name: Convert power
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-manual/power
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of power
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-manual/power
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of power
        uses: actions/upload-artifact@v2
        with:
          name: power_no_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-manual/power/3c_performance_stats/
          retention-days: 5

      - name: Build converted power (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-manual/power
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo power >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-manual/failed-components-list.txt

      - name: Convert treeadd
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-manual/treeadd
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of treeadd
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-manual/treeadd
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of treeadd
        uses: actions/upload-artifact@v2
        with:
          name: treeadd_no_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-manual/treeadd/3c_performance_stats/
          retention-days: 5

      - name: Build converted treeadd (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-manual/treeadd
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo treeadd >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-manual/failed-components-list.txt

      - name: Convert tsp
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-manual/tsp
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of tsp
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-manual/tsp
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of tsp
        uses: actions/upload-artifact@v2
        with:
          name: tsp_no_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-manual/tsp/3c_performance_stats/
          retention-days: 5

      - name: Build converted tsp (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-manual/tsp
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo tsp >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-manual/failed-components-list.txt

      - name: Convert voronoi
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-manual/voronoi
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of voronoi
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-manual/voronoi
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of voronoi
        uses: actions/upload-artifact@v2
        with:
          name: voronoi_no_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-manual/voronoi/3c_performance_stats/
          retention-days: 5

      - name: Build converted voronoi (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-manual/voronoi
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo voronoi >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-manual/failed-components-list.txt

      - name: Check for deferred post-conversion build failures
        run: |
          if [ -e ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-manual/failed-components-list.txt ]; then
              echo 'Failed components (see previous post-conversion build steps):'
              cat ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/Olden-manual/failed-components-list.txt
              exit 1
          fi

  test_Olden-Manual_no_expand_macros_alltypes_disable_rds:
    name: Test Olden-Manual (not macro-expanded, -alltypes, CCured solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Olden-Manual
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds
          tar -xvzf ${{env.benchmark_tar_dir}}/Olden-manual.tar.gz
          cd Olden-manual
          for i in bh bisort em3d health mst perimeter power treeadd tsp voronoi ; do \
            (cd $i ; bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE") \
          done

      - name: Convert bh
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-manual/bh
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of bh
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-manual/bh
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of bh
        uses: actions/upload-artifact@v2
        with:
          name: bh_no_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-manual/bh/3c_performance_stats/
          retention-days: 5

      - name: Build converted bh (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-manual/bh
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo bh >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-manual/failed-components-list.txt

      - name: Convert bisort
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-manual/bisort
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of bisort
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-manual/bisort
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of bisort
        uses: actions/upload-artifact@v2
        with:
          name: bisort_no_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-manual/bisort/3c_performance_stats/
          retention-days: 5

      - name: Build converted bisort (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-manual/bisort
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo bisort >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-manual/failed-components-list.txt

      - name: Convert em3d
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-manual/em3d
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of em3d
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-manual/em3d
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of em3d
        uses: actions/upload-artifact@v2
        with:
          name: em3d_no_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-manual/em3d/3c_performance_stats/
          retention-days: 5

      - name: Build converted em3d (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-manual/em3d
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo em3d >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-manual/failed-components-list.txt

      - name: Convert health
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-manual/health
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of health
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-manual/health
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of health
        uses: actions/upload-artifact@v2
        with:
          name: health_no_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-manual/health/3c_performance_stats/
          retention-days: 5

      - name: Build converted health (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-manual/health
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo health >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-manual/failed-components-list.txt

      - name: Convert mst
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-manual/mst
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of mst
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-manual/mst
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of mst
        uses: actions/upload-artifact@v2
        with:
          name: mst_no_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-manual/mst/3c_performance_stats/
          retention-days: 5

      - name: Build converted mst (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-manual/mst
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo mst >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-manual/failed-components-list.txt

      - name: Convert perimeter
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-manual/perimeter
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of perimeter
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-manual/perimeter
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of perimeter
        uses: actions/upload-artifact@v2
        with:
          name: perimeter_no_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-manual/perimeter/3c_performance_stats/
          retention-days: 5

      - name: Build converted perimeter (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-manual/perimeter
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo perimeter >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-manual/failed-components-list.txt

      - name: Convert power
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-manual/power
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of power
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-manual/power
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of power
        uses: actions/upload-artifact@v2
        with:
          name: power_no_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-manual/power/3c_performance_stats/
          retention-days: 5

      - name: Build converted power (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-manual/power
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo power >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-manual/failed-components-list.txt

      - name: Convert treeadd
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-manual/treeadd
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of treeadd
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-manual/treeadd
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of treeadd
        uses: actions/upload-artifact@v2
        with:
          name: treeadd_no_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-manual/treeadd/3c_performance_stats/
          retention-days: 5

      - name: Build converted treeadd (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-manual/treeadd
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo treeadd >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-manual/failed-components-list.txt

      - name: Convert tsp
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-manual/tsp
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of tsp
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-manual/tsp
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of tsp
        uses: actions/upload-artifact@v2
        with:
          name: tsp_no_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-manual/tsp/3c_performance_stats/
          retention-days: 5

      - name: Build converted tsp (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-manual/tsp
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo tsp >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-manual/failed-components-list.txt

      - name: Convert voronoi
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-manual/voronoi
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of voronoi
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-manual/voronoi
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of voronoi
        uses: actions/upload-artifact@v2
        with:
          name: voronoi_no_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-manual/voronoi/3c_performance_stats/
          retention-days: 5

      - name: Build converted voronoi (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-manual/voronoi
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo voronoi >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-manual/failed-components-list.txt

      - name: Check for deferred post-conversion build failures
        run: |
          if [ -e ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-manual/failed-components-list.txt ]; then
              echo 'Failed components (see previous post-conversion build steps):'
              cat ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/Olden-manual/failed-components-list.txt
              exit 1
          fi

  test_Olden-Manual_no_expand_macros_alltypes_disable_fnedgs:
    name: Test Olden-Manual (not macro-expanded, -alltypes, FuncRevEdges solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Olden-Manual
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs
          tar -xvzf ${{env.benchmark_tar_dir}}/Olden-manual.tar.gz
          cd Olden-manual
          for i in bh bisort em3d health mst perimeter power treeadd tsp voronoi ; do \
            (cd $i ; bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE") \
          done

      - name: Convert bh
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-manual/bh
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of bh
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-manual/bh
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of bh
        uses: actions/upload-artifact@v2
        with:
          name: bh_no_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-manual/bh/3c_performance_stats/
          retention-days: 5

      - name: Build converted bh (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-manual/bh
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo bh >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-manual/failed-components-list.txt

      - name: Convert bisort
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-manual/bisort
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of bisort
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-manual/bisort
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of bisort
        uses: actions/upload-artifact@v2
        with:
          name: bisort_no_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-manual/bisort/3c_performance_stats/
          retention-days: 5

      - name: Build converted bisort (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-manual/bisort
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo bisort >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-manual/failed-components-list.txt

      - name: Convert em3d
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-manual/em3d
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of em3d
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-manual/em3d
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of em3d
        uses: actions/upload-artifact@v2
        with:
          name: em3d_no_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-manual/em3d/3c_performance_stats/
          retention-days: 5

      - name: Build converted em3d (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-manual/em3d
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo em3d >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-manual/failed-components-list.txt

      - name: Convert health
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-manual/health
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of health
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-manual/health
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of health
        uses: actions/upload-artifact@v2
        with:
          name: health_no_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-manual/health/3c_performance_stats/
          retention-days: 5

      - name: Build converted health (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-manual/health
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo health >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-manual/failed-components-list.txt

      - name: Convert mst
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-manual/mst
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of mst
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-manual/mst
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of mst
        uses: actions/upload-artifact@v2
        with:
          name: mst_no_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-manual/mst/3c_performance_stats/
          retention-days: 5

      - name: Build converted mst (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-manual/mst
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo mst >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-manual/failed-components-list.txt

      - name: Convert perimeter
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-manual/perimeter
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of perimeter
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-manual/perimeter
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of perimeter
        uses: actions/upload-artifact@v2
        with:
          name: perimeter_no_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-manual/perimeter/3c_performance_stats/
          retention-days: 5

      - name: Build converted perimeter (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-manual/perimeter
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo perimeter >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-manual/failed-components-list.txt

      - name: Convert power
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-manual/power
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of power
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-manual/power
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of power
        uses: actions/upload-artifact@v2
        with:
          name: power_no_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-manual/power/3c_performance_stats/
          retention-days: 5

      - name: Build converted power (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-manual/power
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo power >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-manual/failed-components-list.txt

      - name: Convert treeadd
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-manual/treeadd
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of treeadd
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-manual/treeadd
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of treeadd
        uses: actions/upload-artifact@v2
        with:
          name: treeadd_no_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-manual/treeadd/3c_performance_stats/
          retention-days: 5

      - name: Build converted treeadd (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-manual/treeadd
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo treeadd >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-manual/failed-components-list.txt

      - name: Convert tsp
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-manual/tsp
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of tsp
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-manual/tsp
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of tsp
        uses: actions/upload-artifact@v2
        with:
          name: tsp_no_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-manual/tsp/3c_performance_stats/
          retention-days: 5

      - name: Build converted tsp (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-manual/tsp
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo tsp >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-manual/failed-components-list.txt

      - name: Convert voronoi
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-manual/voronoi
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of voronoi
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-manual/voronoi
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of voronoi
        uses: actions/upload-artifact@v2
        with:
          name: voronoi_no_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-manual/voronoi/3c_performance_stats/
          retention-days: 5

      - name: Build converted voronoi (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-manual/voronoi
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo voronoi >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-manual/failed-components-list.txt

      - name: Check for deferred post-conversion build failures
        run: |
          if [ -e ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-manual/failed-components-list.txt ]; then
              echo 'Failed components (see previous post-conversion build steps):'
              cat ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/Olden-manual/failed-components-list.txt
              exit 1
          fi

  test_Olden-Manual_no_expand_macros_alltypes:
    name: Test Olden-Manual (not macro-expanded, -alltypes)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Olden-Manual
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes
          tar -xvzf ${{env.benchmark_tar_dir}}/Olden-manual.tar.gz
          cd Olden-manual
          for i in bh bisort em3d health mst perimeter power treeadd tsp voronoi ; do \
            (cd $i ; bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE") \
          done

      - name: Convert bh
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-manual/bh
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --project_path .

      - name: Copy 3c stats of bh
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-manual/bh
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of bh
        uses: actions/upload-artifact@v2
        with:
          name: bh_no_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-manual/bh/3c_performance_stats/
          retention-days: 5

      - name: Build converted bh (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-manual/bh
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo bh >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-manual/failed-components-list.txt

      - name: Convert bisort
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-manual/bisort
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --project_path .

      - name: Copy 3c stats of bisort
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-manual/bisort
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of bisort
        uses: actions/upload-artifact@v2
        with:
          name: bisort_no_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-manual/bisort/3c_performance_stats/
          retention-days: 5

      - name: Build converted bisort (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-manual/bisort
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo bisort >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-manual/failed-components-list.txt

      - name: Convert em3d
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-manual/em3d
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --project_path .

      - name: Copy 3c stats of em3d
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-manual/em3d
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of em3d
        uses: actions/upload-artifact@v2
        with:
          name: em3d_no_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-manual/em3d/3c_performance_stats/
          retention-days: 5

      - name: Build converted em3d (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-manual/em3d
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo em3d >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-manual/failed-components-list.txt

      - name: Convert health
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-manual/health
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --project_path .

      - name: Copy 3c stats of health
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-manual/health
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of health
        uses: actions/upload-artifact@v2
        with:
          name: health_no_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-manual/health/3c_performance_stats/
          retention-days: 5

      - name: Build converted health (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-manual/health
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo health >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-manual/failed-components-list.txt

      - name: Convert mst
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-manual/mst
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --project_path .

      - name: Copy 3c stats of mst
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-manual/mst
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of mst
        uses: actions/upload-artifact@v2
        with:
          name: mst_no_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-manual/mst/3c_performance_stats/
          retention-days: 5

      - name: Build converted mst (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-manual/mst
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo mst >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-manual/failed-components-list.txt

      - name: Convert perimeter
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-manual/perimeter
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --project_path .

      - name: Copy 3c stats of perimeter
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-manual/perimeter
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of perimeter
        uses: actions/upload-artifact@v2
        with:
          name: perimeter_no_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-manual/perimeter/3c_performance_stats/
          retention-days: 5

      - name: Build converted perimeter (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-manual/perimeter
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo perimeter >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-manual/failed-components-list.txt

      - name: Convert power
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-manual/power
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --project_path .

      - name: Copy 3c stats of power
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-manual/power
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of power
        uses: actions/upload-artifact@v2
        with:
          name: power_no_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-manual/power/3c_performance_stats/
          retention-days: 5

      - name: Build converted power (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-manual/power
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo power >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-manual/failed-components-list.txt

      - name: Convert treeadd
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-manual/treeadd
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --project_path .

      - name: Copy 3c stats of treeadd
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-manual/treeadd
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of treeadd
        uses: actions/upload-artifact@v2
        with:
          name: treeadd_no_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-manual/treeadd/3c_performance_stats/
          retention-days: 5

      - name: Build converted treeadd (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-manual/treeadd
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo treeadd >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-manual/failed-components-list.txt

      - name: Convert tsp
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-manual/tsp
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --project_path .

      - name: Copy 3c stats of tsp
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-manual/tsp
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of tsp
        uses: actions/upload-artifact@v2
        with:
          name: tsp_no_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-manual/tsp/3c_performance_stats/
          retention-days: 5

      - name: Build converted tsp (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-manual/tsp
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo tsp >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-manual/failed-components-list.txt

      - name: Convert voronoi
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-manual/voronoi
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --project_path .

      - name: Copy 3c stats of voronoi
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-manual/voronoi
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of voronoi
        uses: actions/upload-artifact@v2
        with:
          name: voronoi_no_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-manual/voronoi/3c_performance_stats/
          retention-days: 5

      - name: Build converted voronoi (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-manual/voronoi
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo voronoi >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-manual/failed-components-list.txt

      - name: Check for deferred post-conversion build failures
        run: |
          if [ -e ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-manual/failed-components-list.txt ]; then
              echo 'Failed components (see previous post-conversion build steps):'
              cat ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/Olden-manual/failed-components-list.txt
              exit 1
          fi

  test_Olden-Manual_expand_macros_no_alltypes:
    name: Test Olden-Manual (macro-expanded, no -alltypes)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Olden-Manual
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes
          tar -xvzf ${{env.benchmark_tar_dir}}/Olden-manual.tar.gz
          cd Olden-manual
          for i in bh bisort em3d health mst perimeter power treeadd tsp voronoi ; do \
            (cd $i ; bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE") \
          done

      - name: Convert bh
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-manual/bh
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of bh
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-manual/bh
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of bh
        uses: actions/upload-artifact@v2
        with:
          name: bh_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-manual/bh/3c_performance_stats/
          retention-days: 5

      - name: Build converted bh (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-manual/bh
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" || echo bh >>${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-manual/failed-components-list.txt

      - name: Convert bisort
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-manual/bisort
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of bisort
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-manual/bisort
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of bisort
        uses: actions/upload-artifact@v2
        with:
          name: bisort_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-manual/bisort/3c_performance_stats/
          retention-days: 5

      - name: Build converted bisort (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-manual/bisort
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" || echo bisort >>${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-manual/failed-components-list.txt

      - name: Convert em3d
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-manual/em3d
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of em3d
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-manual/em3d
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of em3d
        uses: actions/upload-artifact@v2
        with:
          name: em3d_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-manual/em3d/3c_performance_stats/
          retention-days: 5

      - name: Build converted em3d (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-manual/em3d
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" || echo em3d >>${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-manual/failed-components-list.txt

      - name: Convert health
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-manual/health
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of health
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-manual/health
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of health
        uses: actions/upload-artifact@v2
        with:
          name: health_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-manual/health/3c_performance_stats/
          retention-days: 5

      - name: Build converted health (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-manual/health
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" || echo health >>${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-manual/failed-components-list.txt

      - name: Convert mst
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-manual/mst
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of mst
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-manual/mst
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of mst
        uses: actions/upload-artifact@v2
        with:
          name: mst_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-manual/mst/3c_performance_stats/
          retention-days: 5

      - name: Build converted mst (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-manual/mst
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" || echo mst >>${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-manual/failed-components-list.txt

      - name: Convert perimeter
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-manual/perimeter
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of perimeter
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-manual/perimeter
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of perimeter
        uses: actions/upload-artifact@v2
        with:
          name: perimeter_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-manual/perimeter/3c_performance_stats/
          retention-days: 5

      - name: Build converted perimeter (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-manual/perimeter
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" || echo perimeter >>${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-manual/failed-components-list.txt

      - name: Convert power
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-manual/power
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of power
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-manual/power
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of power
        uses: actions/upload-artifact@v2
        with:
          name: power_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-manual/power/3c_performance_stats/
          retention-days: 5

      - name: Build converted power (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-manual/power
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" || echo power >>${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-manual/failed-components-list.txt

      - name: Convert treeadd
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-manual/treeadd
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of treeadd
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-manual/treeadd
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of treeadd
        uses: actions/upload-artifact@v2
        with:
          name: treeadd_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-manual/treeadd/3c_performance_stats/
          retention-days: 5

      - name: Build converted treeadd (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-manual/treeadd
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" || echo treeadd >>${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-manual/failed-components-list.txt

      - name: Convert tsp
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-manual/tsp
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of tsp
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-manual/tsp
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of tsp
        uses: actions/upload-artifact@v2
        with:
          name: tsp_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-manual/tsp/3c_performance_stats/
          retention-days: 5

      - name: Build converted tsp (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-manual/tsp
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" || echo tsp >>${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-manual/failed-components-list.txt

      - name: Convert voronoi
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-manual/voronoi
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of voronoi
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-manual/voronoi
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of voronoi
        uses: actions/upload-artifact@v2
        with:
          name: voronoi_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-manual/voronoi/3c_performance_stats/
          retention-days: 5

      - name: Build converted voronoi (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-manual/voronoi
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" || echo voronoi >>${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-manual/failed-components-list.txt

      - name: Check for deferred post-conversion build failures
        run: |
          if [ -e ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-manual/failed-components-list.txt ]; then
              echo 'Failed components (see previous post-conversion build steps):'
              cat ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/Olden-manual/failed-components-list.txt
              exit 1
          fi

  test_Olden-Manual_expand_macros_alltypes_only_g_sol:
    name: Test Olden-Manual (macro-expanded, -alltypes, greatest solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Olden-Manual
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol
          tar -xvzf ${{env.benchmark_tar_dir}}/Olden-manual.tar.gz
          cd Olden-manual
          for i in bh bisort em3d health mst perimeter power treeadd tsp voronoi ; do \
            (cd $i ; bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE") \
          done

      - name: Convert bh
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-manual/bh
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of bh
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-manual/bh
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of bh
        uses: actions/upload-artifact@v2
        with:
          name: bh_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-manual/bh/3c_performance_stats/
          retention-days: 5

      - name: Build converted bh (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-manual/bh
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo bh >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-manual/failed-components-list.txt

      - name: Convert bisort
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-manual/bisort
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of bisort
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-manual/bisort
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of bisort
        uses: actions/upload-artifact@v2
        with:
          name: bisort_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-manual/bisort/3c_performance_stats/
          retention-days: 5

      - name: Build converted bisort (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-manual/bisort
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo bisort >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-manual/failed-components-list.txt

      - name: Convert em3d
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-manual/em3d
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of em3d
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-manual/em3d
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of em3d
        uses: actions/upload-artifact@v2
        with:
          name: em3d_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-manual/em3d/3c_performance_stats/
          retention-days: 5

      - name: Build converted em3d (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-manual/em3d
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo em3d >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-manual/failed-components-list.txt

      - name: Convert health
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-manual/health
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of health
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-manual/health
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of health
        uses: actions/upload-artifact@v2
        with:
          name: health_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-manual/health/3c_performance_stats/
          retention-days: 5

      - name: Build converted health (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-manual/health
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo health >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-manual/failed-components-list.txt

      - name: Convert mst
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-manual/mst
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of mst
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-manual/mst
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of mst
        uses: actions/upload-artifact@v2
        with:
          name: mst_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-manual/mst/3c_performance_stats/
          retention-days: 5

      - name: Build converted mst (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-manual/mst
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo mst >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-manual/failed-components-list.txt

      - name: Convert perimeter
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-manual/perimeter
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of perimeter
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-manual/perimeter
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of perimeter
        uses: actions/upload-artifact@v2
        with:
          name: perimeter_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-manual/perimeter/3c_performance_stats/
          retention-days: 5

      - name: Build converted perimeter (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-manual/perimeter
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo perimeter >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-manual/failed-components-list.txt

      - name: Convert power
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-manual/power
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of power
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-manual/power
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of power
        uses: actions/upload-artifact@v2
        with:
          name: power_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-manual/power/3c_performance_stats/
          retention-days: 5

      - name: Build converted power (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-manual/power
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo power >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-manual/failed-components-list.txt

      - name: Convert treeadd
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-manual/treeadd
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of treeadd
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-manual/treeadd
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of treeadd
        uses: actions/upload-artifact@v2
        with:
          name: treeadd_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-manual/treeadd/3c_performance_stats/
          retention-days: 5

      - name: Build converted treeadd (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-manual/treeadd
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo treeadd >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-manual/failed-components-list.txt

      - name: Convert tsp
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-manual/tsp
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of tsp
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-manual/tsp
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of tsp
        uses: actions/upload-artifact@v2
        with:
          name: tsp_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-manual/tsp/3c_performance_stats/
          retention-days: 5

      - name: Build converted tsp (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-manual/tsp
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo tsp >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-manual/failed-components-list.txt

      - name: Convert voronoi
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-manual/voronoi
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of voronoi
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-manual/voronoi
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of voronoi
        uses: actions/upload-artifact@v2
        with:
          name: voronoi_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-manual/voronoi/3c_performance_stats/
          retention-days: 5

      - name: Build converted voronoi (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-manual/voronoi
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo voronoi >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-manual/failed-components-list.txt

      - name: Check for deferred post-conversion build failures
        run: |
          if [ -e ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-manual/failed-components-list.txt ]; then
              echo 'Failed components (see previous post-conversion build steps):'
              cat ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/Olden-manual/failed-components-list.txt
              exit 1
          fi

  test_Olden-Manual_expand_macros_alltypes_only_l_sol:
    name: Test Olden-Manual (macro-expanded, -alltypes, least solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Olden-Manual
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol
          tar -xvzf ${{env.benchmark_tar_dir}}/Olden-manual.tar.gz
          cd Olden-manual
          for i in bh bisort em3d health mst perimeter power treeadd tsp voronoi ; do \
            (cd $i ; bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE") \
          done

      - name: Convert bh
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-manual/bh
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of bh
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-manual/bh
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of bh
        uses: actions/upload-artifact@v2
        with:
          name: bh_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-manual/bh/3c_performance_stats/
          retention-days: 5

      - name: Build converted bh (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-manual/bh
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo bh >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-manual/failed-components-list.txt

      - name: Convert bisort
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-manual/bisort
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of bisort
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-manual/bisort
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of bisort
        uses: actions/upload-artifact@v2
        with:
          name: bisort_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-manual/bisort/3c_performance_stats/
          retention-days: 5

      - name: Build converted bisort (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-manual/bisort
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo bisort >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-manual/failed-components-list.txt

      - name: Convert em3d
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-manual/em3d
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of em3d
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-manual/em3d
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of em3d
        uses: actions/upload-artifact@v2
        with:
          name: em3d_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-manual/em3d/3c_performance_stats/
          retention-days: 5

      - name: Build converted em3d (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-manual/em3d
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo em3d >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-manual/failed-components-list.txt

      - name: Convert health
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-manual/health
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of health
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-manual/health
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of health
        uses: actions/upload-artifact@v2
        with:
          name: health_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-manual/health/3c_performance_stats/
          retention-days: 5

      - name: Build converted health (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-manual/health
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo health >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-manual/failed-components-list.txt

      - name: Convert mst
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-manual/mst
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of mst
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-manual/mst
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of mst
        uses: actions/upload-artifact@v2
        with:
          name: mst_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-manual/mst/3c_performance_stats/
          retention-days: 5

      - name: Build converted mst (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-manual/mst
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo mst >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-manual/failed-components-list.txt

      - name: Convert perimeter
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-manual/perimeter
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of perimeter
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-manual/perimeter
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of perimeter
        uses: actions/upload-artifact@v2
        with:
          name: perimeter_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-manual/perimeter/3c_performance_stats/
          retention-days: 5

      - name: Build converted perimeter (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-manual/perimeter
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo perimeter >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-manual/failed-components-list.txt

      - name: Convert power
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-manual/power
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of power
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-manual/power
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of power
        uses: actions/upload-artifact@v2
        with:
          name: power_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-manual/power/3c_performance_stats/
          retention-days: 5

      - name: Build converted power (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-manual/power
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo power >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-manual/failed-components-list.txt

      - name: Convert treeadd
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-manual/treeadd
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of treeadd
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-manual/treeadd
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of treeadd
        uses: actions/upload-artifact@v2
        with:
          name: treeadd_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-manual/treeadd/3c_performance_stats/
          retention-days: 5

      - name: Build converted treeadd (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-manual/treeadd
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo treeadd >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-manual/failed-components-list.txt

      - name: Convert tsp
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-manual/tsp
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of tsp
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-manual/tsp
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of tsp
        uses: actions/upload-artifact@v2
        with:
          name: tsp_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-manual/tsp/3c_performance_stats/
          retention-days: 5

      - name: Build converted tsp (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-manual/tsp
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo tsp >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-manual/failed-components-list.txt

      - name: Convert voronoi
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-manual/voronoi
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of voronoi
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-manual/voronoi
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of voronoi
        uses: actions/upload-artifact@v2
        with:
          name: voronoi_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-manual/voronoi/3c_performance_stats/
          retention-days: 5

      - name: Build converted voronoi (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-manual/voronoi
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo voronoi >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-manual/failed-components-list.txt

      - name: Check for deferred post-conversion build failures
        run: |
          if [ -e ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-manual/failed-components-list.txt ]; then
              echo 'Failed components (see previous post-conversion build steps):'
              cat ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/Olden-manual/failed-components-list.txt
              exit 1
          fi

  test_Olden-Manual_expand_macros_alltypes_disable_rds:
    name: Test Olden-Manual (macro-expanded, -alltypes, CCured solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Olden-Manual
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds
          tar -xvzf ${{env.benchmark_tar_dir}}/Olden-manual.tar.gz
          cd Olden-manual
          for i in bh bisort em3d health mst perimeter power treeadd tsp voronoi ; do \
            (cd $i ; bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE") \
          done

      - name: Convert bh
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-manual/bh
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of bh
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-manual/bh
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of bh
        uses: actions/upload-artifact@v2
        with:
          name: bh_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-manual/bh/3c_performance_stats/
          retention-days: 5

      - name: Build converted bh (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-manual/bh
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo bh >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-manual/failed-components-list.txt

      - name: Convert bisort
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-manual/bisort
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of bisort
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-manual/bisort
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of bisort
        uses: actions/upload-artifact@v2
        with:
          name: bisort_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-manual/bisort/3c_performance_stats/
          retention-days: 5

      - name: Build converted bisort (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-manual/bisort
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo bisort >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-manual/failed-components-list.txt

      - name: Convert em3d
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-manual/em3d
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of em3d
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-manual/em3d
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of em3d
        uses: actions/upload-artifact@v2
        with:
          name: em3d_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-manual/em3d/3c_performance_stats/
          retention-days: 5

      - name: Build converted em3d (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-manual/em3d
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo em3d >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-manual/failed-components-list.txt

      - name: Convert health
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-manual/health
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of health
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-manual/health
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of health
        uses: actions/upload-artifact@v2
        with:
          name: health_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-manual/health/3c_performance_stats/
          retention-days: 5

      - name: Build converted health (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-manual/health
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo health >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-manual/failed-components-list.txt

      - name: Convert mst
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-manual/mst
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of mst
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-manual/mst
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of mst
        uses: actions/upload-artifact@v2
        with:
          name: mst_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-manual/mst/3c_performance_stats/
          retention-days: 5

      - name: Build converted mst (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-manual/mst
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo mst >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-manual/failed-components-list.txt

      - name: Convert perimeter
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-manual/perimeter
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of perimeter
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-manual/perimeter
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of perimeter
        uses: actions/upload-artifact@v2
        with:
          name: perimeter_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-manual/perimeter/3c_performance_stats/
          retention-days: 5

      - name: Build converted perimeter (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-manual/perimeter
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo perimeter >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-manual/failed-components-list.txt

      - name: Convert power
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-manual/power
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of power
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-manual/power
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of power
        uses: actions/upload-artifact@v2
        with:
          name: power_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-manual/power/3c_performance_stats/
          retention-days: 5

      - name: Build converted power (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-manual/power
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo power >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-manual/failed-components-list.txt

      - name: Convert treeadd
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-manual/treeadd
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of treeadd
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-manual/treeadd
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of treeadd
        uses: actions/upload-artifact@v2
        with:
          name: treeadd_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-manual/treeadd/3c_performance_stats/
          retention-days: 5

      - name: Build converted treeadd (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-manual/treeadd
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo treeadd >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-manual/failed-components-list.txt

      - name: Convert tsp
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-manual/tsp
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of tsp
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-manual/tsp
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of tsp
        uses: actions/upload-artifact@v2
        with:
          name: tsp_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-manual/tsp/3c_performance_stats/
          retention-days: 5

      - name: Build converted tsp (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-manual/tsp
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo tsp >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-manual/failed-components-list.txt

      - name: Convert voronoi
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-manual/voronoi
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of voronoi
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-manual/voronoi
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of voronoi
        uses: actions/upload-artifact@v2
        with:
          name: voronoi_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-manual/voronoi/3c_performance_stats/
          retention-days: 5

      - name: Build converted voronoi (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-manual/voronoi
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo voronoi >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-manual/failed-components-list.txt

      - name: Check for deferred post-conversion build failures
        run: |
          if [ -e ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-manual/failed-components-list.txt ]; then
              echo 'Failed components (see previous post-conversion build steps):'
              cat ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/Olden-manual/failed-components-list.txt
              exit 1
          fi

  test_Olden-Manual_expand_macros_alltypes_disable_fnedgs:
    name: Test Olden-Manual (macro-expanded, -alltypes, FuncRevEdges solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Olden-Manual
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs
          tar -xvzf ${{env.benchmark_tar_dir}}/Olden-manual.tar.gz
          cd Olden-manual
          for i in bh bisort em3d health mst perimeter power treeadd tsp voronoi ; do \
            (cd $i ; bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE") \
          done

      - name: Convert bh
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-manual/bh
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of bh
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-manual/bh
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of bh
        uses: actions/upload-artifact@v2
        with:
          name: bh_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-manual/bh/3c_performance_stats/
          retention-days: 5

      - name: Build converted bh (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-manual/bh
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo bh >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-manual/failed-components-list.txt

      - name: Convert bisort
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-manual/bisort
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of bisort
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-manual/bisort
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of bisort
        uses: actions/upload-artifact@v2
        with:
          name: bisort_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-manual/bisort/3c_performance_stats/
          retention-days: 5

      - name: Build converted bisort (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-manual/bisort
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo bisort >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-manual/failed-components-list.txt

      - name: Convert em3d
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-manual/em3d
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of em3d
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-manual/em3d
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of em3d
        uses: actions/upload-artifact@v2
        with:
          name: em3d_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-manual/em3d/3c_performance_stats/
          retention-days: 5

      - name: Build converted em3d (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-manual/em3d
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo em3d >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-manual/failed-components-list.txt

      - name: Convert health
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-manual/health
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of health
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-manual/health
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of health
        uses: actions/upload-artifact@v2
        with:
          name: health_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-manual/health/3c_performance_stats/
          retention-days: 5

      - name: Build converted health (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-manual/health
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo health >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-manual/failed-components-list.txt

      - name: Convert mst
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-manual/mst
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of mst
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-manual/mst
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of mst
        uses: actions/upload-artifact@v2
        with:
          name: mst_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-manual/mst/3c_performance_stats/
          retention-days: 5

      - name: Build converted mst (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-manual/mst
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo mst >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-manual/failed-components-list.txt

      - name: Convert perimeter
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-manual/perimeter
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of perimeter
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-manual/perimeter
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of perimeter
        uses: actions/upload-artifact@v2
        with:
          name: perimeter_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-manual/perimeter/3c_performance_stats/
          retention-days: 5

      - name: Build converted perimeter (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-manual/perimeter
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo perimeter >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-manual/failed-components-list.txt

      - name: Convert power
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-manual/power
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of power
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-manual/power
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of power
        uses: actions/upload-artifact@v2
        with:
          name: power_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-manual/power/3c_performance_stats/
          retention-days: 5

      - name: Build converted power (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-manual/power
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo power >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-manual/failed-components-list.txt

      - name: Convert treeadd
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-manual/treeadd
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of treeadd
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-manual/treeadd
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of treeadd
        uses: actions/upload-artifact@v2
        with:
          name: treeadd_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-manual/treeadd/3c_performance_stats/
          retention-days: 5

      - name: Build converted treeadd (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-manual/treeadd
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo treeadd >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-manual/failed-components-list.txt

      - name: Convert tsp
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-manual/tsp
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of tsp
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-manual/tsp
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of tsp
        uses: actions/upload-artifact@v2
        with:
          name: tsp_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-manual/tsp/3c_performance_stats/
          retention-days: 5

      - name: Build converted tsp (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-manual/tsp
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo tsp >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-manual/failed-components-list.txt

      - name: Convert voronoi
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-manual/voronoi
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of voronoi
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-manual/voronoi
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of voronoi
        uses: actions/upload-artifact@v2
        with:
          name: voronoi_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-manual/voronoi/3c_performance_stats/
          retention-days: 5

      - name: Build converted voronoi (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-manual/voronoi
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo voronoi >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-manual/failed-components-list.txt

      - name: Check for deferred post-conversion build failures
        run: |
          if [ -e ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-manual/failed-components-list.txt ]; then
              echo 'Failed components (see previous post-conversion build steps):'
              cat ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/Olden-manual/failed-components-list.txt
              exit 1
          fi

  test_Olden-Manual_expand_macros_alltypes:
    name: Test Olden-Manual (macro-expanded, -alltypes)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Olden-Manual
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/expand_macros_alltypes
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes
          tar -xvzf ${{env.benchmark_tar_dir}}/Olden-manual.tar.gz
          cd Olden-manual
          for i in bh bisort em3d health mst perimeter power treeadd tsp voronoi ; do \
            (cd $i ; bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE") \
          done

      - name: Convert bh
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-manual/bh
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of bh
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-manual/bh
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of bh
        uses: actions/upload-artifact@v2
        with:
          name: bh_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-manual/bh/3c_performance_stats/
          retention-days: 5

      - name: Build converted bh (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-manual/bh
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo bh >>${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-manual/failed-components-list.txt

      - name: Convert bisort
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-manual/bisort
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of bisort
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-manual/bisort
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of bisort
        uses: actions/upload-artifact@v2
        with:
          name: bisort_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-manual/bisort/3c_performance_stats/
          retention-days: 5

      - name: Build converted bisort (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-manual/bisort
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo bisort >>${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-manual/failed-components-list.txt

      - name: Convert em3d
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-manual/em3d
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of em3d
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-manual/em3d
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of em3d
        uses: actions/upload-artifact@v2
        with:
          name: em3d_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-manual/em3d/3c_performance_stats/
          retention-days: 5

      - name: Build converted em3d (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-manual/em3d
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo em3d >>${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-manual/failed-components-list.txt

      - name: Convert health
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-manual/health
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of health
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-manual/health
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of health
        uses: actions/upload-artifact@v2
        with:
          name: health_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-manual/health/3c_performance_stats/
          retention-days: 5

      - name: Build converted health (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-manual/health
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo health >>${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-manual/failed-components-list.txt

      - name: Convert mst
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-manual/mst
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of mst
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-manual/mst
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of mst
        uses: actions/upload-artifact@v2
        with:
          name: mst_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-manual/mst/3c_performance_stats/
          retention-days: 5

      - name: Build converted mst (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-manual/mst
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo mst >>${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-manual/failed-components-list.txt

      - name: Convert perimeter
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-manual/perimeter
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of perimeter
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-manual/perimeter
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of perimeter
        uses: actions/upload-artifact@v2
        with:
          name: perimeter_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-manual/perimeter/3c_performance_stats/
          retention-days: 5

      - name: Build converted perimeter (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-manual/perimeter
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo perimeter >>${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-manual/failed-components-list.txt

      - name: Convert power
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-manual/power
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of power
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-manual/power
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of power
        uses: actions/upload-artifact@v2
        with:
          name: power_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-manual/power/3c_performance_stats/
          retention-days: 5

      - name: Build converted power (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-manual/power
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo power >>${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-manual/failed-components-list.txt

      - name: Convert treeadd
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-manual/treeadd
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of treeadd
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-manual/treeadd
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of treeadd
        uses: actions/upload-artifact@v2
        with:
          name: treeadd_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-manual/treeadd/3c_performance_stats/
          retention-days: 5

      - name: Build converted treeadd (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-manual/treeadd
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo treeadd >>${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-manual/failed-components-list.txt

      - name: Convert tsp
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-manual/tsp
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of tsp
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-manual/tsp
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of tsp
        uses: actions/upload-artifact@v2
        with:
          name: tsp_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-manual/tsp/3c_performance_stats/
          retention-days: 5

      - name: Build converted tsp (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-manual/tsp
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo tsp >>${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-manual/failed-components-list.txt

      - name: Convert voronoi
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-manual/voronoi
          ${{env.port_tools}}/convert_project.py \
            --extra-3c-arg=-allow-unwritable-changes \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of voronoi
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-manual/voronoi
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of voronoi
        uses: actions/upload-artifact@v2
        with:
          name: voronoi_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-manual/voronoi/3c_performance_stats/
          retention-days: 5

      - name: Build converted voronoi (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-manual/voronoi
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo voronoi >>${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-manual/failed-components-list.txt

      - name: Check for deferred post-conversion build failures
        run: |
          if [ -e ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-manual/failed-components-list.txt ]; then
              echo 'Failed components (see previous post-conversion build steps):'
              cat ${{env.benchmark_conv_dir}}/expand_macros_alltypes/Olden-manual/failed-components-list.txt
              exit 1
          fi

  test_ptrdist_no_expand_macros_no_alltypes:
    name: Test PtrDist (not macro-expanded, no -alltypes)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build PtrDist
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes
          tar -xvzf ${{env.benchmark_tar_dir}}/ptrdist-1.1.tar.gz
          cd ptrdist-1.1
          ( cd yacr2 ; \
            sed -Ei 's/^long (.*costMatrix)/ulong \1/' assign.h
            for header in *.h  ; do
              src="$(basename "$header" .h).c"
              new_header="$(basename "$header" .h)_code.h"
              test -e "$src" || continue
              sed -ne '/^#ifdef.*CODE/,/#else.*CODE/{ /^#/!p; }' "$header" >"$new_header"
              sed -i "/#define.*_CODE/d; /#include \"$header\"/a#include \"$new_header\"" "$src"
            done )
          for i in anagram bc ft ks yacr2 ; do \
            (cd $i ; bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE") \
          done

      - name: Convert anagram
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/ptrdist-1.1/anagram
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --project_path .

      - name: Copy 3c stats of anagram
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/ptrdist-1.1/anagram
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of anagram
        uses: actions/upload-artifact@v2
        with:
          name: anagram_no_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/ptrdist-1.1/anagram/3c_performance_stats/
          retention-days: 5

      - name: Build converted anagram (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/ptrdist-1.1/anagram
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" || echo anagram >>${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/ptrdist-1.1/failed-components-list.txt

      - name: Convert bc
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/ptrdist-1.1/bc
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --project_path .

      - name: Copy 3c stats of bc
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/ptrdist-1.1/bc
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of bc
        uses: actions/upload-artifact@v2
        with:
          name: bc_no_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/ptrdist-1.1/bc/3c_performance_stats/
          retention-days: 5

      - name: Build converted bc (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/ptrdist-1.1/bc
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" || echo bc >>${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/ptrdist-1.1/failed-components-list.txt

      - name: Convert ft
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/ptrdist-1.1/ft
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --project_path .

      - name: Copy 3c stats of ft
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/ptrdist-1.1/ft
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of ft
        uses: actions/upload-artifact@v2
        with:
          name: ft_no_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/ptrdist-1.1/ft/3c_performance_stats/
          retention-days: 5

      - name: Build converted ft (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/ptrdist-1.1/ft
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" || echo ft >>${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/ptrdist-1.1/failed-components-list.txt

      - name: Convert ks
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/ptrdist-1.1/ks
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --project_path .

      - name: Copy 3c stats of ks
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/ptrdist-1.1/ks
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of ks
        uses: actions/upload-artifact@v2
        with:
          name: ks_no_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/ptrdist-1.1/ks/3c_performance_stats/
          retention-days: 5

      - name: Build converted ks (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/ptrdist-1.1/ks
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" || echo ks >>${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/ptrdist-1.1/failed-components-list.txt

      - name: Convert yacr2
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/ptrdist-1.1/yacr2
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --project_path .

      - name: Copy 3c stats of yacr2
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/ptrdist-1.1/yacr2
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of yacr2
        uses: actions/upload-artifact@v2
        with:
          name: yacr2_no_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/ptrdist-1.1/yacr2/3c_performance_stats/
          retention-days: 5

      - name: Build converted yacr2 (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/ptrdist-1.1/yacr2
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" || echo yacr2 >>${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/ptrdist-1.1/failed-components-list.txt

      - name: Check for deferred post-conversion build failures
        run: |
          if [ -e ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/ptrdist-1.1/failed-components-list.txt ]; then
              echo 'Failed components (see previous post-conversion build steps):'
              cat ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/ptrdist-1.1/failed-components-list.txt
              exit 1
          fi

  test_ptrdist_no_expand_macros_alltypes_only_g_sol:
    name: Test PtrDist (not macro-expanded, -alltypes, greatest solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build PtrDist
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol
          tar -xvzf ${{env.benchmark_tar_dir}}/ptrdist-1.1.tar.gz
          cd ptrdist-1.1
          ( cd yacr2 ; \
            sed -Ei 's/^long (.*costMatrix)/ulong \1/' assign.h
            for header in *.h  ; do
              src="$(basename "$header" .h).c"
              new_header="$(basename "$header" .h)_code.h"
              test -e "$src" || continue
              sed -ne '/^#ifdef.*CODE/,/#else.*CODE/{ /^#/!p; }' "$header" >"$new_header"
              sed -i "/#define.*_CODE/d; /#include \"$header\"/a#include \"$new_header\"" "$src"
            done )
          for i in anagram bc ft ks yacr2 ; do \
            (cd $i ; bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE") \
          done

      - name: Convert anagram
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/ptrdist-1.1/anagram
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of anagram
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/ptrdist-1.1/anagram
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of anagram
        uses: actions/upload-artifact@v2
        with:
          name: anagram_no_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/ptrdist-1.1/anagram/3c_performance_stats/
          retention-days: 5

      - name: Build converted anagram (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/ptrdist-1.1/anagram
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo anagram >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/ptrdist-1.1/failed-components-list.txt

      - name: Convert bc
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/ptrdist-1.1/bc
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of bc
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/ptrdist-1.1/bc
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of bc
        uses: actions/upload-artifact@v2
        with:
          name: bc_no_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/ptrdist-1.1/bc/3c_performance_stats/
          retention-days: 5

      - name: Build converted bc (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/ptrdist-1.1/bc
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo bc >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/ptrdist-1.1/failed-components-list.txt

      - name: Convert ft
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/ptrdist-1.1/ft
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of ft
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/ptrdist-1.1/ft
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of ft
        uses: actions/upload-artifact@v2
        with:
          name: ft_no_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/ptrdist-1.1/ft/3c_performance_stats/
          retention-days: 5

      - name: Build converted ft (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/ptrdist-1.1/ft
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo ft >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/ptrdist-1.1/failed-components-list.txt

      - name: Convert ks
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/ptrdist-1.1/ks
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of ks
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/ptrdist-1.1/ks
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of ks
        uses: actions/upload-artifact@v2
        with:
          name: ks_no_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/ptrdist-1.1/ks/3c_performance_stats/
          retention-days: 5

      - name: Build converted ks (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/ptrdist-1.1/ks
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo ks >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/ptrdist-1.1/failed-components-list.txt

      - name: Convert yacr2
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/ptrdist-1.1/yacr2
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of yacr2
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/ptrdist-1.1/yacr2
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of yacr2
        uses: actions/upload-artifact@v2
        with:
          name: yacr2_no_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/ptrdist-1.1/yacr2/3c_performance_stats/
          retention-days: 5

      - name: Build converted yacr2 (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/ptrdist-1.1/yacr2
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo yacr2 >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/ptrdist-1.1/failed-components-list.txt

      - name: Check for deferred post-conversion build failures
        run: |
          if [ -e ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/ptrdist-1.1/failed-components-list.txt ]; then
              echo 'Failed components (see previous post-conversion build steps):'
              cat ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/ptrdist-1.1/failed-components-list.txt
              exit 1
          fi

  test_ptrdist_no_expand_macros_alltypes_only_l_sol:
    name: Test PtrDist (not macro-expanded, -alltypes, least solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build PtrDist
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol
          tar -xvzf ${{env.benchmark_tar_dir}}/ptrdist-1.1.tar.gz
          cd ptrdist-1.1
          ( cd yacr2 ; \
            sed -Ei 's/^long (.*costMatrix)/ulong \1/' assign.h
            for header in *.h  ; do
              src="$(basename "$header" .h).c"
              new_header="$(basename "$header" .h)_code.h"
              test -e "$src" || continue
              sed -ne '/^#ifdef.*CODE/,/#else.*CODE/{ /^#/!p; }' "$header" >"$new_header"
              sed -i "/#define.*_CODE/d; /#include \"$header\"/a#include \"$new_header\"" "$src"
            done )
          for i in anagram bc ft ks yacr2 ; do \
            (cd $i ; bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE") \
          done

      - name: Convert anagram
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/ptrdist-1.1/anagram
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of anagram
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/ptrdist-1.1/anagram
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of anagram
        uses: actions/upload-artifact@v2
        with:
          name: anagram_no_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/ptrdist-1.1/anagram/3c_performance_stats/
          retention-days: 5

      - name: Build converted anagram (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/ptrdist-1.1/anagram
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo anagram >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/ptrdist-1.1/failed-components-list.txt

      - name: Convert bc
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/ptrdist-1.1/bc
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of bc
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/ptrdist-1.1/bc
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of bc
        uses: actions/upload-artifact@v2
        with:
          name: bc_no_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/ptrdist-1.1/bc/3c_performance_stats/
          retention-days: 5

      - name: Build converted bc (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/ptrdist-1.1/bc
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo bc >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/ptrdist-1.1/failed-components-list.txt

      - name: Convert ft
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/ptrdist-1.1/ft
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of ft
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/ptrdist-1.1/ft
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of ft
        uses: actions/upload-artifact@v2
        with:
          name: ft_no_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/ptrdist-1.1/ft/3c_performance_stats/
          retention-days: 5

      - name: Build converted ft (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/ptrdist-1.1/ft
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo ft >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/ptrdist-1.1/failed-components-list.txt

      - name: Convert ks
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/ptrdist-1.1/ks
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of ks
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/ptrdist-1.1/ks
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of ks
        uses: actions/upload-artifact@v2
        with:
          name: ks_no_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/ptrdist-1.1/ks/3c_performance_stats/
          retention-days: 5

      - name: Build converted ks (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/ptrdist-1.1/ks
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo ks >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/ptrdist-1.1/failed-components-list.txt

      - name: Convert yacr2
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/ptrdist-1.1/yacr2
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of yacr2
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/ptrdist-1.1/yacr2
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of yacr2
        uses: actions/upload-artifact@v2
        with:
          name: yacr2_no_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/ptrdist-1.1/yacr2/3c_performance_stats/
          retention-days: 5

      - name: Build converted yacr2 (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/ptrdist-1.1/yacr2
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo yacr2 >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/ptrdist-1.1/failed-components-list.txt

      - name: Check for deferred post-conversion build failures
        run: |
          if [ -e ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/ptrdist-1.1/failed-components-list.txt ]; then
              echo 'Failed components (see previous post-conversion build steps):'
              cat ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/ptrdist-1.1/failed-components-list.txt
              exit 1
          fi

  test_ptrdist_no_expand_macros_alltypes_disable_rds:
    name: Test PtrDist (not macro-expanded, -alltypes, CCured solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build PtrDist
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds
          tar -xvzf ${{env.benchmark_tar_dir}}/ptrdist-1.1.tar.gz
          cd ptrdist-1.1
          ( cd yacr2 ; \
            sed -Ei 's/^long (.*costMatrix)/ulong \1/' assign.h
            for header in *.h  ; do
              src="$(basename "$header" .h).c"
              new_header="$(basename "$header" .h)_code.h"
              test -e "$src" || continue
              sed -ne '/^#ifdef.*CODE/,/#else.*CODE/{ /^#/!p; }' "$header" >"$new_header"
              sed -i "/#define.*_CODE/d; /#include \"$header\"/a#include \"$new_header\"" "$src"
            done )
          for i in anagram bc ft ks yacr2 ; do \
            (cd $i ; bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE") \
          done

      - name: Convert anagram
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/ptrdist-1.1/anagram
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of anagram
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/ptrdist-1.1/anagram
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of anagram
        uses: actions/upload-artifact@v2
        with:
          name: anagram_no_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/ptrdist-1.1/anagram/3c_performance_stats/
          retention-days: 5

      - name: Build converted anagram (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/ptrdist-1.1/anagram
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo anagram >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/ptrdist-1.1/failed-components-list.txt

      - name: Convert bc
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/ptrdist-1.1/bc
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of bc
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/ptrdist-1.1/bc
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of bc
        uses: actions/upload-artifact@v2
        with:
          name: bc_no_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/ptrdist-1.1/bc/3c_performance_stats/
          retention-days: 5

      - name: Build converted bc (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/ptrdist-1.1/bc
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo bc >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/ptrdist-1.1/failed-components-list.txt

      - name: Convert ft
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/ptrdist-1.1/ft
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of ft
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/ptrdist-1.1/ft
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of ft
        uses: actions/upload-artifact@v2
        with:
          name: ft_no_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/ptrdist-1.1/ft/3c_performance_stats/
          retention-days: 5

      - name: Build converted ft (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/ptrdist-1.1/ft
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo ft >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/ptrdist-1.1/failed-components-list.txt

      - name: Convert ks
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/ptrdist-1.1/ks
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of ks
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/ptrdist-1.1/ks
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of ks
        uses: actions/upload-artifact@v2
        with:
          name: ks_no_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/ptrdist-1.1/ks/3c_performance_stats/
          retention-days: 5

      - name: Build converted ks (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/ptrdist-1.1/ks
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo ks >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/ptrdist-1.1/failed-components-list.txt

      - name: Convert yacr2
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/ptrdist-1.1/yacr2
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of yacr2
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/ptrdist-1.1/yacr2
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of yacr2
        uses: actions/upload-artifact@v2
        with:
          name: yacr2_no_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/ptrdist-1.1/yacr2/3c_performance_stats/
          retention-days: 5

      - name: Build converted yacr2 (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/ptrdist-1.1/yacr2
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo yacr2 >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/ptrdist-1.1/failed-components-list.txt

      - name: Check for deferred post-conversion build failures
        run: |
          if [ -e ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/ptrdist-1.1/failed-components-list.txt ]; then
              echo 'Failed components (see previous post-conversion build steps):'
              cat ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/ptrdist-1.1/failed-components-list.txt
              exit 1
          fi

  test_ptrdist_no_expand_macros_alltypes_disable_fnedgs:
    name: Test PtrDist (not macro-expanded, -alltypes, FuncRevEdges solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build PtrDist
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs
          tar -xvzf ${{env.benchmark_tar_dir}}/ptrdist-1.1.tar.gz
          cd ptrdist-1.1
          ( cd yacr2 ; \
            sed -Ei 's/^long (.*costMatrix)/ulong \1/' assign.h
            for header in *.h  ; do
              src="$(basename "$header" .h).c"
              new_header="$(basename "$header" .h)_code.h"
              test -e "$src" || continue
              sed -ne '/^#ifdef.*CODE/,/#else.*CODE/{ /^#/!p; }' "$header" >"$new_header"
              sed -i "/#define.*_CODE/d; /#include \"$header\"/a#include \"$new_header\"" "$src"
            done )
          for i in anagram bc ft ks yacr2 ; do \
            (cd $i ; bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE") \
          done

      - name: Convert anagram
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/ptrdist-1.1/anagram
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of anagram
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/ptrdist-1.1/anagram
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of anagram
        uses: actions/upload-artifact@v2
        with:
          name: anagram_no_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/ptrdist-1.1/anagram/3c_performance_stats/
          retention-days: 5

      - name: Build converted anagram (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/ptrdist-1.1/anagram
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo anagram >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/ptrdist-1.1/failed-components-list.txt

      - name: Convert bc
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/ptrdist-1.1/bc
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of bc
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/ptrdist-1.1/bc
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of bc
        uses: actions/upload-artifact@v2
        with:
          name: bc_no_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/ptrdist-1.1/bc/3c_performance_stats/
          retention-days: 5

      - name: Build converted bc (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/ptrdist-1.1/bc
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo bc >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/ptrdist-1.1/failed-components-list.txt

      - name: Convert ft
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/ptrdist-1.1/ft
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of ft
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/ptrdist-1.1/ft
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of ft
        uses: actions/upload-artifact@v2
        with:
          name: ft_no_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/ptrdist-1.1/ft/3c_performance_stats/
          retention-days: 5

      - name: Build converted ft (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/ptrdist-1.1/ft
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo ft >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/ptrdist-1.1/failed-components-list.txt

      - name: Convert ks
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/ptrdist-1.1/ks
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of ks
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/ptrdist-1.1/ks
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of ks
        uses: actions/upload-artifact@v2
        with:
          name: ks_no_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/ptrdist-1.1/ks/3c_performance_stats/
          retention-days: 5

      - name: Build converted ks (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/ptrdist-1.1/ks
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo ks >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/ptrdist-1.1/failed-components-list.txt

      - name: Convert yacr2
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/ptrdist-1.1/yacr2
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of yacr2
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/ptrdist-1.1/yacr2
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of yacr2
        uses: actions/upload-artifact@v2
        with:
          name: yacr2_no_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/ptrdist-1.1/yacr2/3c_performance_stats/
          retention-days: 5

      - name: Build converted yacr2 (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/ptrdist-1.1/yacr2
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo yacr2 >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/ptrdist-1.1/failed-components-list.txt

      - name: Check for deferred post-conversion build failures
        run: |
          if [ -e ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/ptrdist-1.1/failed-components-list.txt ]; then
              echo 'Failed components (see previous post-conversion build steps):'
              cat ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/ptrdist-1.1/failed-components-list.txt
              exit 1
          fi

  test_ptrdist_no_expand_macros_alltypes:
    name: Test PtrDist (not macro-expanded, -alltypes)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build PtrDist
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes
          tar -xvzf ${{env.benchmark_tar_dir}}/ptrdist-1.1.tar.gz
          cd ptrdist-1.1
          ( cd yacr2 ; \
            sed -Ei 's/^long (.*costMatrix)/ulong \1/' assign.h
            for header in *.h  ; do
              src="$(basename "$header" .h).c"
              new_header="$(basename "$header" .h)_code.h"
              test -e "$src" || continue
              sed -ne '/^#ifdef.*CODE/,/#else.*CODE/{ /^#/!p; }' "$header" >"$new_header"
              sed -i "/#define.*_CODE/d; /#include \"$header\"/a#include \"$new_header\"" "$src"
            done )
          for i in anagram bc ft ks yacr2 ; do \
            (cd $i ; bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE") \
          done

      - name: Convert anagram
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/ptrdist-1.1/anagram
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --project_path .

      - name: Copy 3c stats of anagram
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/ptrdist-1.1/anagram
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of anagram
        uses: actions/upload-artifact@v2
        with:
          name: anagram_no_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/ptrdist-1.1/anagram/3c_performance_stats/
          retention-days: 5

      - name: Build converted anagram (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/ptrdist-1.1/anagram
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo anagram >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/ptrdist-1.1/failed-components-list.txt

      - name: Convert bc
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/ptrdist-1.1/bc
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --project_path .

      - name: Copy 3c stats of bc
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/ptrdist-1.1/bc
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of bc
        uses: actions/upload-artifact@v2
        with:
          name: bc_no_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/ptrdist-1.1/bc/3c_performance_stats/
          retention-days: 5

      - name: Build converted bc (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/ptrdist-1.1/bc
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo bc >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/ptrdist-1.1/failed-components-list.txt

      - name: Convert ft
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/ptrdist-1.1/ft
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --project_path .

      - name: Copy 3c stats of ft
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/ptrdist-1.1/ft
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of ft
        uses: actions/upload-artifact@v2
        with:
          name: ft_no_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/ptrdist-1.1/ft/3c_performance_stats/
          retention-days: 5

      - name: Build converted ft (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/ptrdist-1.1/ft
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo ft >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/ptrdist-1.1/failed-components-list.txt

      - name: Convert ks
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/ptrdist-1.1/ks
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --project_path .

      - name: Copy 3c stats of ks
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/ptrdist-1.1/ks
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of ks
        uses: actions/upload-artifact@v2
        with:
          name: ks_no_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/ptrdist-1.1/ks/3c_performance_stats/
          retention-days: 5

      - name: Build converted ks (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/ptrdist-1.1/ks
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo ks >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/ptrdist-1.1/failed-components-list.txt

      - name: Convert yacr2
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/ptrdist-1.1/yacr2
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --project_path .

      - name: Copy 3c stats of yacr2
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/ptrdist-1.1/yacr2
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of yacr2
        uses: actions/upload-artifact@v2
        with:
          name: yacr2_no_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/ptrdist-1.1/yacr2/3c_performance_stats/
          retention-days: 5

      - name: Build converted yacr2 (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/ptrdist-1.1/yacr2
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo yacr2 >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/ptrdist-1.1/failed-components-list.txt

      - name: Check for deferred post-conversion build failures
        run: |
          if [ -e ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/ptrdist-1.1/failed-components-list.txt ]; then
              echo 'Failed components (see previous post-conversion build steps):'
              cat ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/ptrdist-1.1/failed-components-list.txt
              exit 1
          fi

  test_ptrdist_expand_macros_no_alltypes:
    name: Test PtrDist (macro-expanded, no -alltypes)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build PtrDist
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes
          tar -xvzf ${{env.benchmark_tar_dir}}/ptrdist-1.1.tar.gz
          cd ptrdist-1.1
          ( cd yacr2 ; \
            sed -Ei 's/^long (.*costMatrix)/ulong \1/' assign.h
            for header in *.h  ; do
              src="$(basename "$header" .h).c"
              new_header="$(basename "$header" .h)_code.h"
              test -e "$src" || continue
              sed -ne '/^#ifdef.*CODE/,/#else.*CODE/{ /^#/!p; }' "$header" >"$new_header"
              sed -i "/#define.*_CODE/d; /#include \"$header\"/a#include \"$new_header\"" "$src"
            done )
          for i in anagram bc ft ks yacr2 ; do \
            (cd $i ; bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE") \
          done

      - name: Convert anagram
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/ptrdist-1.1/anagram
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of anagram
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/ptrdist-1.1/anagram
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of anagram
        uses: actions/upload-artifact@v2
        with:
          name: anagram_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/ptrdist-1.1/anagram/3c_performance_stats/
          retention-days: 5

      - name: Build converted anagram (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/ptrdist-1.1/anagram
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" || echo anagram >>${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/ptrdist-1.1/failed-components-list.txt

      - name: Convert bc
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/ptrdist-1.1/bc
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of bc
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/ptrdist-1.1/bc
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of bc
        uses: actions/upload-artifact@v2
        with:
          name: bc_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/ptrdist-1.1/bc/3c_performance_stats/
          retention-days: 5

      - name: Build converted bc (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/ptrdist-1.1/bc
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" || echo bc >>${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/ptrdist-1.1/failed-components-list.txt

      - name: Convert ft
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/ptrdist-1.1/ft
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of ft
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/ptrdist-1.1/ft
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of ft
        uses: actions/upload-artifact@v2
        with:
          name: ft_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/ptrdist-1.1/ft/3c_performance_stats/
          retention-days: 5

      - name: Build converted ft (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/ptrdist-1.1/ft
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" || echo ft >>${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/ptrdist-1.1/failed-components-list.txt

      - name: Convert ks
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/ptrdist-1.1/ks
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of ks
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/ptrdist-1.1/ks
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of ks
        uses: actions/upload-artifact@v2
        with:
          name: ks_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/ptrdist-1.1/ks/3c_performance_stats/
          retention-days: 5

      - name: Build converted ks (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/ptrdist-1.1/ks
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" || echo ks >>${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/ptrdist-1.1/failed-components-list.txt

      - name: Convert yacr2
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/ptrdist-1.1/yacr2
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of yacr2
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/ptrdist-1.1/yacr2
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of yacr2
        uses: actions/upload-artifact@v2
        with:
          name: yacr2_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/ptrdist-1.1/yacr2/3c_performance_stats/
          retention-days: 5

      - name: Build converted yacr2 (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/ptrdist-1.1/yacr2
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" || echo yacr2 >>${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/ptrdist-1.1/failed-components-list.txt

      - name: Check for deferred post-conversion build failures
        run: |
          if [ -e ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/ptrdist-1.1/failed-components-list.txt ]; then
              echo 'Failed components (see previous post-conversion build steps):'
              cat ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/ptrdist-1.1/failed-components-list.txt
              exit 1
          fi

  test_ptrdist_expand_macros_alltypes_only_g_sol:
    name: Test PtrDist (macro-expanded, -alltypes, greatest solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build PtrDist
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol
          tar -xvzf ${{env.benchmark_tar_dir}}/ptrdist-1.1.tar.gz
          cd ptrdist-1.1
          ( cd yacr2 ; \
            sed -Ei 's/^long (.*costMatrix)/ulong \1/' assign.h
            for header in *.h  ; do
              src="$(basename "$header" .h).c"
              new_header="$(basename "$header" .h)_code.h"
              test -e "$src" || continue
              sed -ne '/^#ifdef.*CODE/,/#else.*CODE/{ /^#/!p; }' "$header" >"$new_header"
              sed -i "/#define.*_CODE/d; /#include \"$header\"/a#include \"$new_header\"" "$src"
            done )
          for i in anagram bc ft ks yacr2 ; do \
            (cd $i ; bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE") \
          done

      - name: Convert anagram
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/ptrdist-1.1/anagram
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of anagram
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/ptrdist-1.1/anagram
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of anagram
        uses: actions/upload-artifact@v2
        with:
          name: anagram_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/ptrdist-1.1/anagram/3c_performance_stats/
          retention-days: 5

      - name: Build converted anagram (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/ptrdist-1.1/anagram
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo anagram >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/ptrdist-1.1/failed-components-list.txt

      - name: Convert bc
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/ptrdist-1.1/bc
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of bc
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/ptrdist-1.1/bc
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of bc
        uses: actions/upload-artifact@v2
        with:
          name: bc_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/ptrdist-1.1/bc/3c_performance_stats/
          retention-days: 5

      - name: Build converted bc (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/ptrdist-1.1/bc
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo bc >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/ptrdist-1.1/failed-components-list.txt

      - name: Convert ft
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/ptrdist-1.1/ft
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of ft
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/ptrdist-1.1/ft
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of ft
        uses: actions/upload-artifact@v2
        with:
          name: ft_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/ptrdist-1.1/ft/3c_performance_stats/
          retention-days: 5

      - name: Build converted ft (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/ptrdist-1.1/ft
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo ft >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/ptrdist-1.1/failed-components-list.txt

      - name: Convert ks
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/ptrdist-1.1/ks
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of ks
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/ptrdist-1.1/ks
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of ks
        uses: actions/upload-artifact@v2
        with:
          name: ks_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/ptrdist-1.1/ks/3c_performance_stats/
          retention-days: 5

      - name: Build converted ks (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/ptrdist-1.1/ks
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo ks >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/ptrdist-1.1/failed-components-list.txt

      - name: Convert yacr2
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/ptrdist-1.1/yacr2
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of yacr2
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/ptrdist-1.1/yacr2
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of yacr2
        uses: actions/upload-artifact@v2
        with:
          name: yacr2_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/ptrdist-1.1/yacr2/3c_performance_stats/
          retention-days: 5

      - name: Build converted yacr2 (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/ptrdist-1.1/yacr2
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo yacr2 >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/ptrdist-1.1/failed-components-list.txt

      - name: Check for deferred post-conversion build failures
        run: |
          if [ -e ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/ptrdist-1.1/failed-components-list.txt ]; then
              echo 'Failed components (see previous post-conversion build steps):'
              cat ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/ptrdist-1.1/failed-components-list.txt
              exit 1
          fi

  test_ptrdist_expand_macros_alltypes_only_l_sol:
    name: Test PtrDist (macro-expanded, -alltypes, least solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build PtrDist
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol
          tar -xvzf ${{env.benchmark_tar_dir}}/ptrdist-1.1.tar.gz
          cd ptrdist-1.1
          ( cd yacr2 ; \
            sed -Ei 's/^long (.*costMatrix)/ulong \1/' assign.h
            for header in *.h  ; do
              src="$(basename "$header" .h).c"
              new_header="$(basename "$header" .h)_code.h"
              test -e "$src" || continue
              sed -ne '/^#ifdef.*CODE/,/#else.*CODE/{ /^#/!p; }' "$header" >"$new_header"
              sed -i "/#define.*_CODE/d; /#include \"$header\"/a#include \"$new_header\"" "$src"
            done )
          for i in anagram bc ft ks yacr2 ; do \
            (cd $i ; bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE") \
          done

      - name: Convert anagram
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/ptrdist-1.1/anagram
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of anagram
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/ptrdist-1.1/anagram
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of anagram
        uses: actions/upload-artifact@v2
        with:
          name: anagram_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/ptrdist-1.1/anagram/3c_performance_stats/
          retention-days: 5

      - name: Build converted anagram (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/ptrdist-1.1/anagram
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo anagram >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/ptrdist-1.1/failed-components-list.txt

      - name: Convert bc
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/ptrdist-1.1/bc
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of bc
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/ptrdist-1.1/bc
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of bc
        uses: actions/upload-artifact@v2
        with:
          name: bc_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/ptrdist-1.1/bc/3c_performance_stats/
          retention-days: 5

      - name: Build converted bc (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/ptrdist-1.1/bc
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo bc >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/ptrdist-1.1/failed-components-list.txt

      - name: Convert ft
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/ptrdist-1.1/ft
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of ft
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/ptrdist-1.1/ft
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of ft
        uses: actions/upload-artifact@v2
        with:
          name: ft_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/ptrdist-1.1/ft/3c_performance_stats/
          retention-days: 5

      - name: Build converted ft (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/ptrdist-1.1/ft
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo ft >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/ptrdist-1.1/failed-components-list.txt

      - name: Convert ks
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/ptrdist-1.1/ks
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of ks
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/ptrdist-1.1/ks
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of ks
        uses: actions/upload-artifact@v2
        with:
          name: ks_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/ptrdist-1.1/ks/3c_performance_stats/
          retention-days: 5

      - name: Build converted ks (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/ptrdist-1.1/ks
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo ks >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/ptrdist-1.1/failed-components-list.txt

      - name: Convert yacr2
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/ptrdist-1.1/yacr2
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of yacr2
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/ptrdist-1.1/yacr2
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of yacr2
        uses: actions/upload-artifact@v2
        with:
          name: yacr2_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/ptrdist-1.1/yacr2/3c_performance_stats/
          retention-days: 5

      - name: Build converted yacr2 (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/ptrdist-1.1/yacr2
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo yacr2 >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/ptrdist-1.1/failed-components-list.txt

      - name: Check for deferred post-conversion build failures
        run: |
          if [ -e ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/ptrdist-1.1/failed-components-list.txt ]; then
              echo 'Failed components (see previous post-conversion build steps):'
              cat ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/ptrdist-1.1/failed-components-list.txt
              exit 1
          fi

  test_ptrdist_expand_macros_alltypes_disable_rds:
    name: Test PtrDist (macro-expanded, -alltypes, CCured solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build PtrDist
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds
          tar -xvzf ${{env.benchmark_tar_dir}}/ptrdist-1.1.tar.gz
          cd ptrdist-1.1
          ( cd yacr2 ; \
            sed -Ei 's/^long (.*costMatrix)/ulong \1/' assign.h
            for header in *.h  ; do
              src="$(basename "$header" .h).c"
              new_header="$(basename "$header" .h)_code.h"
              test -e "$src" || continue
              sed -ne '/^#ifdef.*CODE/,/#else.*CODE/{ /^#/!p; }' "$header" >"$new_header"
              sed -i "/#define.*_CODE/d; /#include \"$header\"/a#include \"$new_header\"" "$src"
            done )
          for i in anagram bc ft ks yacr2 ; do \
            (cd $i ; bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE") \
          done

      - name: Convert anagram
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/ptrdist-1.1/anagram
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of anagram
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/ptrdist-1.1/anagram
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of anagram
        uses: actions/upload-artifact@v2
        with:
          name: anagram_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/ptrdist-1.1/anagram/3c_performance_stats/
          retention-days: 5

      - name: Build converted anagram (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/ptrdist-1.1/anagram
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo anagram >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/ptrdist-1.1/failed-components-list.txt

      - name: Convert bc
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/ptrdist-1.1/bc
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of bc
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/ptrdist-1.1/bc
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of bc
        uses: actions/upload-artifact@v2
        with:
          name: bc_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/ptrdist-1.1/bc/3c_performance_stats/
          retention-days: 5

      - name: Build converted bc (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/ptrdist-1.1/bc
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo bc >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/ptrdist-1.1/failed-components-list.txt

      - name: Convert ft
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/ptrdist-1.1/ft
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of ft
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/ptrdist-1.1/ft
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of ft
        uses: actions/upload-artifact@v2
        with:
          name: ft_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/ptrdist-1.1/ft/3c_performance_stats/
          retention-days: 5

      - name: Build converted ft (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/ptrdist-1.1/ft
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo ft >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/ptrdist-1.1/failed-components-list.txt

      - name: Convert ks
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/ptrdist-1.1/ks
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of ks
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/ptrdist-1.1/ks
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of ks
        uses: actions/upload-artifact@v2
        with:
          name: ks_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/ptrdist-1.1/ks/3c_performance_stats/
          retention-days: 5

      - name: Build converted ks (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/ptrdist-1.1/ks
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo ks >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/ptrdist-1.1/failed-components-list.txt

      - name: Convert yacr2
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/ptrdist-1.1/yacr2
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of yacr2
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/ptrdist-1.1/yacr2
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of yacr2
        uses: actions/upload-artifact@v2
        with:
          name: yacr2_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/ptrdist-1.1/yacr2/3c_performance_stats/
          retention-days: 5

      - name: Build converted yacr2 (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/ptrdist-1.1/yacr2
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo yacr2 >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/ptrdist-1.1/failed-components-list.txt

      - name: Check for deferred post-conversion build failures
        run: |
          if [ -e ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/ptrdist-1.1/failed-components-list.txt ]; then
              echo 'Failed components (see previous post-conversion build steps):'
              cat ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/ptrdist-1.1/failed-components-list.txt
              exit 1
          fi

  test_ptrdist_expand_macros_alltypes_disable_fnedgs:
    name: Test PtrDist (macro-expanded, -alltypes, FuncRevEdges solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build PtrDist
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs
          tar -xvzf ${{env.benchmark_tar_dir}}/ptrdist-1.1.tar.gz
          cd ptrdist-1.1
          ( cd yacr2 ; \
            sed -Ei 's/^long (.*costMatrix)/ulong \1/' assign.h
            for header in *.h  ; do
              src="$(basename "$header" .h).c"
              new_header="$(basename "$header" .h)_code.h"
              test -e "$src" || continue
              sed -ne '/^#ifdef.*CODE/,/#else.*CODE/{ /^#/!p; }' "$header" >"$new_header"
              sed -i "/#define.*_CODE/d; /#include \"$header\"/a#include \"$new_header\"" "$src"
            done )
          for i in anagram bc ft ks yacr2 ; do \
            (cd $i ; bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE") \
          done

      - name: Convert anagram
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/ptrdist-1.1/anagram
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of anagram
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/ptrdist-1.1/anagram
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of anagram
        uses: actions/upload-artifact@v2
        with:
          name: anagram_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/ptrdist-1.1/anagram/3c_performance_stats/
          retention-days: 5

      - name: Build converted anagram (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/ptrdist-1.1/anagram
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo anagram >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/ptrdist-1.1/failed-components-list.txt

      - name: Convert bc
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/ptrdist-1.1/bc
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of bc
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/ptrdist-1.1/bc
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of bc
        uses: actions/upload-artifact@v2
        with:
          name: bc_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/ptrdist-1.1/bc/3c_performance_stats/
          retention-days: 5

      - name: Build converted bc (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/ptrdist-1.1/bc
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo bc >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/ptrdist-1.1/failed-components-list.txt

      - name: Convert ft
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/ptrdist-1.1/ft
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of ft
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/ptrdist-1.1/ft
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of ft
        uses: actions/upload-artifact@v2
        with:
          name: ft_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/ptrdist-1.1/ft/3c_performance_stats/
          retention-days: 5

      - name: Build converted ft (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/ptrdist-1.1/ft
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo ft >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/ptrdist-1.1/failed-components-list.txt

      - name: Convert ks
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/ptrdist-1.1/ks
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of ks
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/ptrdist-1.1/ks
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of ks
        uses: actions/upload-artifact@v2
        with:
          name: ks_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/ptrdist-1.1/ks/3c_performance_stats/
          retention-days: 5

      - name: Build converted ks (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/ptrdist-1.1/ks
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo ks >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/ptrdist-1.1/failed-components-list.txt

      - name: Convert yacr2
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/ptrdist-1.1/yacr2
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of yacr2
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/ptrdist-1.1/yacr2
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of yacr2
        uses: actions/upload-artifact@v2
        with:
          name: yacr2_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/ptrdist-1.1/yacr2/3c_performance_stats/
          retention-days: 5

      - name: Build converted yacr2 (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/ptrdist-1.1/yacr2
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo yacr2 >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/ptrdist-1.1/failed-components-list.txt

      - name: Check for deferred post-conversion build failures
        run: |
          if [ -e ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/ptrdist-1.1/failed-components-list.txt ]; then
              echo 'Failed components (see previous post-conversion build steps):'
              cat ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/ptrdist-1.1/failed-components-list.txt
              exit 1
          fi

  test_ptrdist_expand_macros_alltypes:
    name: Test PtrDist (macro-expanded, -alltypes)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build PtrDist
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/expand_macros_alltypes
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes
          tar -xvzf ${{env.benchmark_tar_dir}}/ptrdist-1.1.tar.gz
          cd ptrdist-1.1
          ( cd yacr2 ; \
            sed -Ei 's/^long (.*costMatrix)/ulong \1/' assign.h
            for header in *.h  ; do
              src="$(basename "$header" .h).c"
              new_header="$(basename "$header" .h)_code.h"
              test -e "$src" || continue
              sed -ne '/^#ifdef.*CODE/,/#else.*CODE/{ /^#/!p; }' "$header" >"$new_header"
              sed -i "/#define.*_CODE/d; /#include \"$header\"/a#include \"$new_header\"" "$src"
            done )
          for i in anagram bc ft ks yacr2 ; do \
            (cd $i ; bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE") \
          done

      - name: Convert anagram
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/ptrdist-1.1/anagram
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of anagram
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/ptrdist-1.1/anagram
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of anagram
        uses: actions/upload-artifact@v2
        with:
          name: anagram_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes/ptrdist-1.1/anagram/3c_performance_stats/
          retention-days: 5

      - name: Build converted anagram (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/ptrdist-1.1/anagram
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo anagram >>${{env.benchmark_conv_dir}}/expand_macros_alltypes/ptrdist-1.1/failed-components-list.txt

      - name: Convert bc
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/ptrdist-1.1/bc
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of bc
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/ptrdist-1.1/bc
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of bc
        uses: actions/upload-artifact@v2
        with:
          name: bc_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes/ptrdist-1.1/bc/3c_performance_stats/
          retention-days: 5

      - name: Build converted bc (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/ptrdist-1.1/bc
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo bc >>${{env.benchmark_conv_dir}}/expand_macros_alltypes/ptrdist-1.1/failed-components-list.txt

      - name: Convert ft
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/ptrdist-1.1/ft
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of ft
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/ptrdist-1.1/ft
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of ft
        uses: actions/upload-artifact@v2
        with:
          name: ft_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes/ptrdist-1.1/ft/3c_performance_stats/
          retention-days: 5

      - name: Build converted ft (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/ptrdist-1.1/ft
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo ft >>${{env.benchmark_conv_dir}}/expand_macros_alltypes/ptrdist-1.1/failed-components-list.txt

      - name: Convert ks
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/ptrdist-1.1/ks
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of ks
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/ptrdist-1.1/ks
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of ks
        uses: actions/upload-artifact@v2
        with:
          name: ks_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes/ptrdist-1.1/ks/3c_performance_stats/
          retention-days: 5

      - name: Build converted ks (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/ptrdist-1.1/ks
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo ks >>${{env.benchmark_conv_dir}}/expand_macros_alltypes/ptrdist-1.1/failed-components-list.txt

      - name: Convert yacr2
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/ptrdist-1.1/yacr2
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of yacr2
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/ptrdist-1.1/yacr2
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of yacr2
        uses: actions/upload-artifact@v2
        with:
          name: yacr2_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes/ptrdist-1.1/yacr2/3c_performance_stats/
          retention-days: 5

      - name: Build converted yacr2 (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/ptrdist-1.1/yacr2
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo yacr2 >>${{env.benchmark_conv_dir}}/expand_macros_alltypes/ptrdist-1.1/failed-components-list.txt

      - name: Check for deferred post-conversion build failures
        run: |
          if [ -e ${{env.benchmark_conv_dir}}/expand_macros_alltypes/ptrdist-1.1/failed-components-list.txt ]; then
              echo 'Failed components (see previous post-conversion build steps):'
              cat ${{env.benchmark_conv_dir}}/expand_macros_alltypes/ptrdist-1.1/failed-components-list.txt
              exit 1
          fi

  test_ptrdist-reverted_no_expand_macros_no_alltypes:
    name: Test PtrDistReverted (not macro-expanded, no -alltypes)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build PtrDistReverted
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes
          tar -xvzf ${{env.benchmark_tar_dir}}/ptrdist-1.1-reverted.tar.gz
          cd ptrdist-1.1-reverted
          ( cd yacr2 ; \
            sed -Ei 's/^long (.*costMatrix)/ulong \1/' assign.h
            for header in *.h  ; do
              src="$(basename "$header" .h).c"
              new_header="$(basename "$header" .h)_code.h"
              test -e "$src" || continue
              sed -ne '/^#ifdef.*CODE/,/#else.*CODE/{ /^#/!p; }' "$header" >"$new_header"
              sed -i "/#define.*_CODE/d; /#include \"$header\"/a#include \"$new_header\"" "$src"
            done )
          for i in anagram ft ks yacr2 ; do \
            (cd $i ; bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE") \
          done

      - name: Convert anagram
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/ptrdist-1.1-reverted/anagram
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --project_path .

      - name: Copy 3c stats of anagram
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/ptrdist-1.1-reverted/anagram
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of anagram
        uses: actions/upload-artifact@v2
        with:
          name: anagram_no_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/ptrdist-1.1-reverted/anagram/3c_performance_stats/
          retention-days: 5

      - name: Build converted anagram (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/ptrdist-1.1-reverted/anagram
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" || echo anagram >>${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/ptrdist-1.1-reverted/failed-components-list.txt

      - name: Convert ft
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/ptrdist-1.1-reverted/ft
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --project_path .

      - name: Copy 3c stats of ft
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/ptrdist-1.1-reverted/ft
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of ft
        uses: actions/upload-artifact@v2
        with:
          name: ft_no_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/ptrdist-1.1-reverted/ft/3c_performance_stats/
          retention-days: 5

      - name: Build converted ft (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/ptrdist-1.1-reverted/ft
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" || echo ft >>${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/ptrdist-1.1-reverted/failed-components-list.txt

      - name: Convert ks
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/ptrdist-1.1-reverted/ks
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --project_path .

      - name: Copy 3c stats of ks
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/ptrdist-1.1-reverted/ks
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of ks
        uses: actions/upload-artifact@v2
        with:
          name: ks_no_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/ptrdist-1.1-reverted/ks/3c_performance_stats/
          retention-days: 5

      - name: Build converted ks (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/ptrdist-1.1-reverted/ks
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" || echo ks >>${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/ptrdist-1.1-reverted/failed-components-list.txt

      - name: Convert yacr2
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/ptrdist-1.1-reverted/yacr2
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --project_path .

      - name: Copy 3c stats of yacr2
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/ptrdist-1.1-reverted/yacr2
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of yacr2
        uses: actions/upload-artifact@v2
        with:
          name: yacr2_no_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/ptrdist-1.1-reverted/yacr2/3c_performance_stats/
          retention-days: 5

      - name: Build converted yacr2 (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/ptrdist-1.1-reverted/yacr2
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" || echo yacr2 >>${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/ptrdist-1.1-reverted/failed-components-list.txt

      - name: Check for deferred post-conversion build failures
        run: |
          if [ -e ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/ptrdist-1.1-reverted/failed-components-list.txt ]; then
              echo 'Failed components (see previous post-conversion build steps):'
              cat ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/ptrdist-1.1-reverted/failed-components-list.txt
              exit 1
          fi

  test_ptrdist-reverted_no_expand_macros_alltypes_only_g_sol:
    name: Test PtrDistReverted (not macro-expanded, -alltypes, greatest solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build PtrDistReverted
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol
          tar -xvzf ${{env.benchmark_tar_dir}}/ptrdist-1.1-reverted.tar.gz
          cd ptrdist-1.1-reverted
          ( cd yacr2 ; \
            sed -Ei 's/^long (.*costMatrix)/ulong \1/' assign.h
            for header in *.h  ; do
              src="$(basename "$header" .h).c"
              new_header="$(basename "$header" .h)_code.h"
              test -e "$src" || continue
              sed -ne '/^#ifdef.*CODE/,/#else.*CODE/{ /^#/!p; }' "$header" >"$new_header"
              sed -i "/#define.*_CODE/d; /#include \"$header\"/a#include \"$new_header\"" "$src"
            done )
          for i in anagram ft ks yacr2 ; do \
            (cd $i ; bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE") \
          done

      - name: Convert anagram
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/ptrdist-1.1-reverted/anagram
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of anagram
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/ptrdist-1.1-reverted/anagram
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of anagram
        uses: actions/upload-artifact@v2
        with:
          name: anagram_no_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/ptrdist-1.1-reverted/anagram/3c_performance_stats/
          retention-days: 5

      - name: Build converted anagram (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/ptrdist-1.1-reverted/anagram
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo anagram >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/ptrdist-1.1-reverted/failed-components-list.txt

      - name: Convert ft
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/ptrdist-1.1-reverted/ft
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of ft
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/ptrdist-1.1-reverted/ft
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of ft
        uses: actions/upload-artifact@v2
        with:
          name: ft_no_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/ptrdist-1.1-reverted/ft/3c_performance_stats/
          retention-days: 5

      - name: Build converted ft (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/ptrdist-1.1-reverted/ft
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo ft >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/ptrdist-1.1-reverted/failed-components-list.txt

      - name: Convert ks
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/ptrdist-1.1-reverted/ks
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of ks
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/ptrdist-1.1-reverted/ks
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of ks
        uses: actions/upload-artifact@v2
        with:
          name: ks_no_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/ptrdist-1.1-reverted/ks/3c_performance_stats/
          retention-days: 5

      - name: Build converted ks (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/ptrdist-1.1-reverted/ks
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo ks >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/ptrdist-1.1-reverted/failed-components-list.txt

      - name: Convert yacr2
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/ptrdist-1.1-reverted/yacr2
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of yacr2
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/ptrdist-1.1-reverted/yacr2
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of yacr2
        uses: actions/upload-artifact@v2
        with:
          name: yacr2_no_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/ptrdist-1.1-reverted/yacr2/3c_performance_stats/
          retention-days: 5

      - name: Build converted yacr2 (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/ptrdist-1.1-reverted/yacr2
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo yacr2 >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/ptrdist-1.1-reverted/failed-components-list.txt

      - name: Check for deferred post-conversion build failures
        run: |
          if [ -e ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/ptrdist-1.1-reverted/failed-components-list.txt ]; then
              echo 'Failed components (see previous post-conversion build steps):'
              cat ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/ptrdist-1.1-reverted/failed-components-list.txt
              exit 1
          fi

  test_ptrdist-reverted_no_expand_macros_alltypes_only_l_sol:
    name: Test PtrDistReverted (not macro-expanded, -alltypes, least solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build PtrDistReverted
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol
          tar -xvzf ${{env.benchmark_tar_dir}}/ptrdist-1.1-reverted.tar.gz
          cd ptrdist-1.1-reverted
          ( cd yacr2 ; \
            sed -Ei 's/^long (.*costMatrix)/ulong \1/' assign.h
            for header in *.h  ; do
              src="$(basename "$header" .h).c"
              new_header="$(basename "$header" .h)_code.h"
              test -e "$src" || continue
              sed -ne '/^#ifdef.*CODE/,/#else.*CODE/{ /^#/!p; }' "$header" >"$new_header"
              sed -i "/#define.*_CODE/d; /#include \"$header\"/a#include \"$new_header\"" "$src"
            done )
          for i in anagram ft ks yacr2 ; do \
            (cd $i ; bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE") \
          done

      - name: Convert anagram
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/ptrdist-1.1-reverted/anagram
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of anagram
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/ptrdist-1.1-reverted/anagram
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of anagram
        uses: actions/upload-artifact@v2
        with:
          name: anagram_no_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/ptrdist-1.1-reverted/anagram/3c_performance_stats/
          retention-days: 5

      - name: Build converted anagram (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/ptrdist-1.1-reverted/anagram
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo anagram >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/ptrdist-1.1-reverted/failed-components-list.txt

      - name: Convert ft
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/ptrdist-1.1-reverted/ft
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of ft
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/ptrdist-1.1-reverted/ft
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of ft
        uses: actions/upload-artifact@v2
        with:
          name: ft_no_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/ptrdist-1.1-reverted/ft/3c_performance_stats/
          retention-days: 5

      - name: Build converted ft (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/ptrdist-1.1-reverted/ft
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo ft >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/ptrdist-1.1-reverted/failed-components-list.txt

      - name: Convert ks
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/ptrdist-1.1-reverted/ks
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of ks
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/ptrdist-1.1-reverted/ks
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of ks
        uses: actions/upload-artifact@v2
        with:
          name: ks_no_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/ptrdist-1.1-reverted/ks/3c_performance_stats/
          retention-days: 5

      - name: Build converted ks (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/ptrdist-1.1-reverted/ks
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo ks >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/ptrdist-1.1-reverted/failed-components-list.txt

      - name: Convert yacr2
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/ptrdist-1.1-reverted/yacr2
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of yacr2
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/ptrdist-1.1-reverted/yacr2
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of yacr2
        uses: actions/upload-artifact@v2
        with:
          name: yacr2_no_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/ptrdist-1.1-reverted/yacr2/3c_performance_stats/
          retention-days: 5

      - name: Build converted yacr2 (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/ptrdist-1.1-reverted/yacr2
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo yacr2 >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/ptrdist-1.1-reverted/failed-components-list.txt

      - name: Check for deferred post-conversion build failures
        run: |
          if [ -e ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/ptrdist-1.1-reverted/failed-components-list.txt ]; then
              echo 'Failed components (see previous post-conversion build steps):'
              cat ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/ptrdist-1.1-reverted/failed-components-list.txt
              exit 1
          fi

  test_ptrdist-reverted_no_expand_macros_alltypes_disable_rds:
    name: Test PtrDistReverted (not macro-expanded, -alltypes, CCured solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build PtrDistReverted
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds
          tar -xvzf ${{env.benchmark_tar_dir}}/ptrdist-1.1-reverted.tar.gz
          cd ptrdist-1.1-reverted
          ( cd yacr2 ; \
            sed -Ei 's/^long (.*costMatrix)/ulong \1/' assign.h
            for header in *.h  ; do
              src="$(basename "$header" .h).c"
              new_header="$(basename "$header" .h)_code.h"
              test -e "$src" || continue
              sed -ne '/^#ifdef.*CODE/,/#else.*CODE/{ /^#/!p; }' "$header" >"$new_header"
              sed -i "/#define.*_CODE/d; /#include \"$header\"/a#include \"$new_header\"" "$src"
            done )
          for i in anagram ft ks yacr2 ; do \
            (cd $i ; bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE") \
          done

      - name: Convert anagram
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/ptrdist-1.1-reverted/anagram
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of anagram
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/ptrdist-1.1-reverted/anagram
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of anagram
        uses: actions/upload-artifact@v2
        with:
          name: anagram_no_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/ptrdist-1.1-reverted/anagram/3c_performance_stats/
          retention-days: 5

      - name: Build converted anagram (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/ptrdist-1.1-reverted/anagram
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo anagram >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/ptrdist-1.1-reverted/failed-components-list.txt

      - name: Convert ft
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/ptrdist-1.1-reverted/ft
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of ft
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/ptrdist-1.1-reverted/ft
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of ft
        uses: actions/upload-artifact@v2
        with:
          name: ft_no_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/ptrdist-1.1-reverted/ft/3c_performance_stats/
          retention-days: 5

      - name: Build converted ft (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/ptrdist-1.1-reverted/ft
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo ft >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/ptrdist-1.1-reverted/failed-components-list.txt

      - name: Convert ks
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/ptrdist-1.1-reverted/ks
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of ks
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/ptrdist-1.1-reverted/ks
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of ks
        uses: actions/upload-artifact@v2
        with:
          name: ks_no_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/ptrdist-1.1-reverted/ks/3c_performance_stats/
          retention-days: 5

      - name: Build converted ks (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/ptrdist-1.1-reverted/ks
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo ks >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/ptrdist-1.1-reverted/failed-components-list.txt

      - name: Convert yacr2
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/ptrdist-1.1-reverted/yacr2
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of yacr2
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/ptrdist-1.1-reverted/yacr2
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of yacr2
        uses: actions/upload-artifact@v2
        with:
          name: yacr2_no_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/ptrdist-1.1-reverted/yacr2/3c_performance_stats/
          retention-days: 5

      - name: Build converted yacr2 (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/ptrdist-1.1-reverted/yacr2
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo yacr2 >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/ptrdist-1.1-reverted/failed-components-list.txt

      - name: Check for deferred post-conversion build failures
        run: |
          if [ -e ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/ptrdist-1.1-reverted/failed-components-list.txt ]; then
              echo 'Failed components (see previous post-conversion build steps):'
              cat ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/ptrdist-1.1-reverted/failed-components-list.txt
              exit 1
          fi

  test_ptrdist-reverted_no_expand_macros_alltypes_disable_fnedgs:
    name: Test PtrDistReverted (not macro-expanded, -alltypes, FuncRevEdges solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build PtrDistReverted
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs
          tar -xvzf ${{env.benchmark_tar_dir}}/ptrdist-1.1-reverted.tar.gz
          cd ptrdist-1.1-reverted
          ( cd yacr2 ; \
            sed -Ei 's/^long (.*costMatrix)/ulong \1/' assign.h
            for header in *.h  ; do
              src="$(basename "$header" .h).c"
              new_header="$(basename "$header" .h)_code.h"
              test -e "$src" || continue
              sed -ne '/^#ifdef.*CODE/,/#else.*CODE/{ /^#/!p; }' "$header" >"$new_header"
              sed -i "/#define.*_CODE/d; /#include \"$header\"/a#include \"$new_header\"" "$src"
            done )
          for i in anagram ft ks yacr2 ; do \
            (cd $i ; bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE") \
          done

      - name: Convert anagram
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/ptrdist-1.1-reverted/anagram
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of anagram
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/ptrdist-1.1-reverted/anagram
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of anagram
        uses: actions/upload-artifact@v2
        with:
          name: anagram_no_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/ptrdist-1.1-reverted/anagram/3c_performance_stats/
          retention-days: 5

      - name: Build converted anagram (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/ptrdist-1.1-reverted/anagram
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo anagram >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/ptrdist-1.1-reverted/failed-components-list.txt

      - name: Convert ft
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/ptrdist-1.1-reverted/ft
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of ft
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/ptrdist-1.1-reverted/ft
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of ft
        uses: actions/upload-artifact@v2
        with:
          name: ft_no_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/ptrdist-1.1-reverted/ft/3c_performance_stats/
          retention-days: 5

      - name: Build converted ft (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/ptrdist-1.1-reverted/ft
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo ft >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/ptrdist-1.1-reverted/failed-components-list.txt

      - name: Convert ks
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/ptrdist-1.1-reverted/ks
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of ks
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/ptrdist-1.1-reverted/ks
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of ks
        uses: actions/upload-artifact@v2
        with:
          name: ks_no_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/ptrdist-1.1-reverted/ks/3c_performance_stats/
          retention-days: 5

      - name: Build converted ks (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/ptrdist-1.1-reverted/ks
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo ks >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/ptrdist-1.1-reverted/failed-components-list.txt

      - name: Convert yacr2
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/ptrdist-1.1-reverted/yacr2
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of yacr2
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/ptrdist-1.1-reverted/yacr2
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of yacr2
        uses: actions/upload-artifact@v2
        with:
          name: yacr2_no_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/ptrdist-1.1-reverted/yacr2/3c_performance_stats/
          retention-days: 5

      - name: Build converted yacr2 (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/ptrdist-1.1-reverted/yacr2
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo yacr2 >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/ptrdist-1.1-reverted/failed-components-list.txt

      - name: Check for deferred post-conversion build failures
        run: |
          if [ -e ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/ptrdist-1.1-reverted/failed-components-list.txt ]; then
              echo 'Failed components (see previous post-conversion build steps):'
              cat ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/ptrdist-1.1-reverted/failed-components-list.txt
              exit 1
          fi

  test_ptrdist-reverted_no_expand_macros_alltypes:
    name: Test PtrDistReverted (not macro-expanded, -alltypes)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build PtrDistReverted
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes
          tar -xvzf ${{env.benchmark_tar_dir}}/ptrdist-1.1-reverted.tar.gz
          cd ptrdist-1.1-reverted
          ( cd yacr2 ; \
            sed -Ei 's/^long (.*costMatrix)/ulong \1/' assign.h
            for header in *.h  ; do
              src="$(basename "$header" .h).c"
              new_header="$(basename "$header" .h)_code.h"
              test -e "$src" || continue
              sed -ne '/^#ifdef.*CODE/,/#else.*CODE/{ /^#/!p; }' "$header" >"$new_header"
              sed -i "/#define.*_CODE/d; /#include \"$header\"/a#include \"$new_header\"" "$src"
            done )
          for i in anagram ft ks yacr2 ; do \
            (cd $i ; bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE") \
          done

      - name: Convert anagram
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/ptrdist-1.1-reverted/anagram
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --project_path .

      - name: Copy 3c stats of anagram
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/ptrdist-1.1-reverted/anagram
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of anagram
        uses: actions/upload-artifact@v2
        with:
          name: anagram_no_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/ptrdist-1.1-reverted/anagram/3c_performance_stats/
          retention-days: 5

      - name: Build converted anagram (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/ptrdist-1.1-reverted/anagram
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo anagram >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/ptrdist-1.1-reverted/failed-components-list.txt

      - name: Convert ft
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/ptrdist-1.1-reverted/ft
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --project_path .

      - name: Copy 3c stats of ft
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/ptrdist-1.1-reverted/ft
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of ft
        uses: actions/upload-artifact@v2
        with:
          name: ft_no_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/ptrdist-1.1-reverted/ft/3c_performance_stats/
          retention-days: 5

      - name: Build converted ft (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/ptrdist-1.1-reverted/ft
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo ft >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/ptrdist-1.1-reverted/failed-components-list.txt

      - name: Convert ks
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/ptrdist-1.1-reverted/ks
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --project_path .

      - name: Copy 3c stats of ks
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/ptrdist-1.1-reverted/ks
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of ks
        uses: actions/upload-artifact@v2
        with:
          name: ks_no_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/ptrdist-1.1-reverted/ks/3c_performance_stats/
          retention-days: 5

      - name: Build converted ks (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/ptrdist-1.1-reverted/ks
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo ks >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/ptrdist-1.1-reverted/failed-components-list.txt

      - name: Convert yacr2
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/ptrdist-1.1-reverted/yacr2
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --project_path .

      - name: Copy 3c stats of yacr2
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/ptrdist-1.1-reverted/yacr2
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of yacr2
        uses: actions/upload-artifact@v2
        with:
          name: yacr2_no_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/ptrdist-1.1-reverted/yacr2/3c_performance_stats/
          retention-days: 5

      - name: Build converted yacr2 (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/ptrdist-1.1-reverted/yacr2
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo yacr2 >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/ptrdist-1.1-reverted/failed-components-list.txt

      - name: Check for deferred post-conversion build failures
        run: |
          if [ -e ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/ptrdist-1.1-reverted/failed-components-list.txt ]; then
              echo 'Failed components (see previous post-conversion build steps):'
              cat ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/ptrdist-1.1-reverted/failed-components-list.txt
              exit 1
          fi

  test_ptrdist-reverted_expand_macros_no_alltypes:
    name: Test PtrDistReverted (macro-expanded, no -alltypes)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build PtrDistReverted
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes
          tar -xvzf ${{env.benchmark_tar_dir}}/ptrdist-1.1-reverted.tar.gz
          cd ptrdist-1.1-reverted
          ( cd yacr2 ; \
            sed -Ei 's/^long (.*costMatrix)/ulong \1/' assign.h
            for header in *.h  ; do
              src="$(basename "$header" .h).c"
              new_header="$(basename "$header" .h)_code.h"
              test -e "$src" || continue
              sed -ne '/^#ifdef.*CODE/,/#else.*CODE/{ /^#/!p; }' "$header" >"$new_header"
              sed -i "/#define.*_CODE/d; /#include \"$header\"/a#include \"$new_header\"" "$src"
            done )
          for i in anagram ft ks yacr2 ; do \
            (cd $i ; bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE") \
          done

      - name: Convert anagram
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/ptrdist-1.1-reverted/anagram
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of anagram
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/ptrdist-1.1-reverted/anagram
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of anagram
        uses: actions/upload-artifact@v2
        with:
          name: anagram_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/ptrdist-1.1-reverted/anagram/3c_performance_stats/
          retention-days: 5

      - name: Build converted anagram (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/ptrdist-1.1-reverted/anagram
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" || echo anagram >>${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/ptrdist-1.1-reverted/failed-components-list.txt

      - name: Convert ft
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/ptrdist-1.1-reverted/ft
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of ft
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/ptrdist-1.1-reverted/ft
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of ft
        uses: actions/upload-artifact@v2
        with:
          name: ft_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/ptrdist-1.1-reverted/ft/3c_performance_stats/
          retention-days: 5

      - name: Build converted ft (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/ptrdist-1.1-reverted/ft
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" || echo ft >>${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/ptrdist-1.1-reverted/failed-components-list.txt

      - name: Convert ks
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/ptrdist-1.1-reverted/ks
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of ks
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/ptrdist-1.1-reverted/ks
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of ks
        uses: actions/upload-artifact@v2
        with:
          name: ks_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/ptrdist-1.1-reverted/ks/3c_performance_stats/
          retention-days: 5

      - name: Build converted ks (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/ptrdist-1.1-reverted/ks
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" || echo ks >>${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/ptrdist-1.1-reverted/failed-components-list.txt

      - name: Convert yacr2
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/ptrdist-1.1-reverted/yacr2
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of yacr2
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/ptrdist-1.1-reverted/yacr2
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of yacr2
        uses: actions/upload-artifact@v2
        with:
          name: yacr2_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/ptrdist-1.1-reverted/yacr2/3c_performance_stats/
          retention-days: 5

      - name: Build converted yacr2 (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/ptrdist-1.1-reverted/yacr2
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" || echo yacr2 >>${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/ptrdist-1.1-reverted/failed-components-list.txt

      - name: Check for deferred post-conversion build failures
        run: |
          if [ -e ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/ptrdist-1.1-reverted/failed-components-list.txt ]; then
              echo 'Failed components (see previous post-conversion build steps):'
              cat ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/ptrdist-1.1-reverted/failed-components-list.txt
              exit 1
          fi

  test_ptrdist-reverted_expand_macros_alltypes_only_g_sol:
    name: Test PtrDistReverted (macro-expanded, -alltypes, greatest solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build PtrDistReverted
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol
          tar -xvzf ${{env.benchmark_tar_dir}}/ptrdist-1.1-reverted.tar.gz
          cd ptrdist-1.1-reverted
          ( cd yacr2 ; \
            sed -Ei 's/^long (.*costMatrix)/ulong \1/' assign.h
            for header in *.h  ; do
              src="$(basename "$header" .h).c"
              new_header="$(basename "$header" .h)_code.h"
              test -e "$src" || continue
              sed -ne '/^#ifdef.*CODE/,/#else.*CODE/{ /^#/!p; }' "$header" >"$new_header"
              sed -i "/#define.*_CODE/d; /#include \"$header\"/a#include \"$new_header\"" "$src"
            done )
          for i in anagram ft ks yacr2 ; do \
            (cd $i ; bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE") \
          done

      - name: Convert anagram
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/ptrdist-1.1-reverted/anagram
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of anagram
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/ptrdist-1.1-reverted/anagram
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of anagram
        uses: actions/upload-artifact@v2
        with:
          name: anagram_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/ptrdist-1.1-reverted/anagram/3c_performance_stats/
          retention-days: 5

      - name: Build converted anagram (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/ptrdist-1.1-reverted/anagram
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo anagram >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/ptrdist-1.1-reverted/failed-components-list.txt

      - name: Convert ft
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/ptrdist-1.1-reverted/ft
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of ft
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/ptrdist-1.1-reverted/ft
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of ft
        uses: actions/upload-artifact@v2
        with:
          name: ft_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/ptrdist-1.1-reverted/ft/3c_performance_stats/
          retention-days: 5

      - name: Build converted ft (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/ptrdist-1.1-reverted/ft
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo ft >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/ptrdist-1.1-reverted/failed-components-list.txt

      - name: Convert ks
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/ptrdist-1.1-reverted/ks
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of ks
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/ptrdist-1.1-reverted/ks
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of ks
        uses: actions/upload-artifact@v2
        with:
          name: ks_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/ptrdist-1.1-reverted/ks/3c_performance_stats/
          retention-days: 5

      - name: Build converted ks (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/ptrdist-1.1-reverted/ks
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo ks >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/ptrdist-1.1-reverted/failed-components-list.txt

      - name: Convert yacr2
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/ptrdist-1.1-reverted/yacr2
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of yacr2
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/ptrdist-1.1-reverted/yacr2
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of yacr2
        uses: actions/upload-artifact@v2
        with:
          name: yacr2_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/ptrdist-1.1-reverted/yacr2/3c_performance_stats/
          retention-days: 5

      - name: Build converted yacr2 (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/ptrdist-1.1-reverted/yacr2
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo yacr2 >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/ptrdist-1.1-reverted/failed-components-list.txt

      - name: Check for deferred post-conversion build failures
        run: |
          if [ -e ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/ptrdist-1.1-reverted/failed-components-list.txt ]; then
              echo 'Failed components (see previous post-conversion build steps):'
              cat ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/ptrdist-1.1-reverted/failed-components-list.txt
              exit 1
          fi

  test_ptrdist-reverted_expand_macros_alltypes_only_l_sol:
    name: Test PtrDistReverted (macro-expanded, -alltypes, least solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build PtrDistReverted
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol
          tar -xvzf ${{env.benchmark_tar_dir}}/ptrdist-1.1-reverted.tar.gz
          cd ptrdist-1.1-reverted
          ( cd yacr2 ; \
            sed -Ei 's/^long (.*costMatrix)/ulong \1/' assign.h
            for header in *.h  ; do
              src="$(basename "$header" .h).c"
              new_header="$(basename "$header" .h)_code.h"
              test -e "$src" || continue
              sed -ne '/^#ifdef.*CODE/,/#else.*CODE/{ /^#/!p; }' "$header" >"$new_header"
              sed -i "/#define.*_CODE/d; /#include \"$header\"/a#include \"$new_header\"" "$src"
            done )
          for i in anagram ft ks yacr2 ; do \
            (cd $i ; bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE") \
          done

      - name: Convert anagram
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/ptrdist-1.1-reverted/anagram
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of anagram
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/ptrdist-1.1-reverted/anagram
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of anagram
        uses: actions/upload-artifact@v2
        with:
          name: anagram_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/ptrdist-1.1-reverted/anagram/3c_performance_stats/
          retention-days: 5

      - name: Build converted anagram (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/ptrdist-1.1-reverted/anagram
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo anagram >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/ptrdist-1.1-reverted/failed-components-list.txt

      - name: Convert ft
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/ptrdist-1.1-reverted/ft
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of ft
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/ptrdist-1.1-reverted/ft
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of ft
        uses: actions/upload-artifact@v2
        with:
          name: ft_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/ptrdist-1.1-reverted/ft/3c_performance_stats/
          retention-days: 5

      - name: Build converted ft (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/ptrdist-1.1-reverted/ft
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo ft >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/ptrdist-1.1-reverted/failed-components-list.txt

      - name: Convert ks
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/ptrdist-1.1-reverted/ks
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of ks
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/ptrdist-1.1-reverted/ks
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of ks
        uses: actions/upload-artifact@v2
        with:
          name: ks_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/ptrdist-1.1-reverted/ks/3c_performance_stats/
          retention-days: 5

      - name: Build converted ks (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/ptrdist-1.1-reverted/ks
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo ks >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/ptrdist-1.1-reverted/failed-components-list.txt

      - name: Convert yacr2
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/ptrdist-1.1-reverted/yacr2
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of yacr2
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/ptrdist-1.1-reverted/yacr2
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of yacr2
        uses: actions/upload-artifact@v2
        with:
          name: yacr2_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/ptrdist-1.1-reverted/yacr2/3c_performance_stats/
          retention-days: 5

      - name: Build converted yacr2 (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/ptrdist-1.1-reverted/yacr2
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo yacr2 >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/ptrdist-1.1-reverted/failed-components-list.txt

      - name: Check for deferred post-conversion build failures
        run: |
          if [ -e ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/ptrdist-1.1-reverted/failed-components-list.txt ]; then
              echo 'Failed components (see previous post-conversion build steps):'
              cat ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/ptrdist-1.1-reverted/failed-components-list.txt
              exit 1
          fi

  test_ptrdist-reverted_expand_macros_alltypes_disable_rds:
    name: Test PtrDistReverted (macro-expanded, -alltypes, CCured solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build PtrDistReverted
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds
          tar -xvzf ${{env.benchmark_tar_dir}}/ptrdist-1.1-reverted.tar.gz
          cd ptrdist-1.1-reverted
          ( cd yacr2 ; \
            sed -Ei 's/^long (.*costMatrix)/ulong \1/' assign.h
            for header in *.h  ; do
              src="$(basename "$header" .h).c"
              new_header="$(basename "$header" .h)_code.h"
              test -e "$src" || continue
              sed -ne '/^#ifdef.*CODE/,/#else.*CODE/{ /^#/!p; }' "$header" >"$new_header"
              sed -i "/#define.*_CODE/d; /#include \"$header\"/a#include \"$new_header\"" "$src"
            done )
          for i in anagram ft ks yacr2 ; do \
            (cd $i ; bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE") \
          done

      - name: Convert anagram
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/ptrdist-1.1-reverted/anagram
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of anagram
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/ptrdist-1.1-reverted/anagram
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of anagram
        uses: actions/upload-artifact@v2
        with:
          name: anagram_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/ptrdist-1.1-reverted/anagram/3c_performance_stats/
          retention-days: 5

      - name: Build converted anagram (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/ptrdist-1.1-reverted/anagram
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo anagram >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/ptrdist-1.1-reverted/failed-components-list.txt

      - name: Convert ft
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/ptrdist-1.1-reverted/ft
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of ft
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/ptrdist-1.1-reverted/ft
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of ft
        uses: actions/upload-artifact@v2
        with:
          name: ft_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/ptrdist-1.1-reverted/ft/3c_performance_stats/
          retention-days: 5

      - name: Build converted ft (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/ptrdist-1.1-reverted/ft
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo ft >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/ptrdist-1.1-reverted/failed-components-list.txt

      - name: Convert ks
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/ptrdist-1.1-reverted/ks
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of ks
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/ptrdist-1.1-reverted/ks
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of ks
        uses: actions/upload-artifact@v2
        with:
          name: ks_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/ptrdist-1.1-reverted/ks/3c_performance_stats/
          retention-days: 5

      - name: Build converted ks (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/ptrdist-1.1-reverted/ks
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo ks >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/ptrdist-1.1-reverted/failed-components-list.txt

      - name: Convert yacr2
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/ptrdist-1.1-reverted/yacr2
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of yacr2
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/ptrdist-1.1-reverted/yacr2
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of yacr2
        uses: actions/upload-artifact@v2
        with:
          name: yacr2_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/ptrdist-1.1-reverted/yacr2/3c_performance_stats/
          retention-days: 5

      - name: Build converted yacr2 (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/ptrdist-1.1-reverted/yacr2
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo yacr2 >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/ptrdist-1.1-reverted/failed-components-list.txt

      - name: Check for deferred post-conversion build failures
        run: |
          if [ -e ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/ptrdist-1.1-reverted/failed-components-list.txt ]; then
              echo 'Failed components (see previous post-conversion build steps):'
              cat ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/ptrdist-1.1-reverted/failed-components-list.txt
              exit 1
          fi

  test_ptrdist-reverted_expand_macros_alltypes_disable_fnedgs:
    name: Test PtrDistReverted (macro-expanded, -alltypes, FuncRevEdges solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build PtrDistReverted
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs
          tar -xvzf ${{env.benchmark_tar_dir}}/ptrdist-1.1-reverted.tar.gz
          cd ptrdist-1.1-reverted
          ( cd yacr2 ; \
            sed -Ei 's/^long (.*costMatrix)/ulong \1/' assign.h
            for header in *.h  ; do
              src="$(basename "$header" .h).c"
              new_header="$(basename "$header" .h)_code.h"
              test -e "$src" || continue
              sed -ne '/^#ifdef.*CODE/,/#else.*CODE/{ /^#/!p; }' "$header" >"$new_header"
              sed -i "/#define.*_CODE/d; /#include \"$header\"/a#include \"$new_header\"" "$src"
            done )
          for i in anagram ft ks yacr2 ; do \
            (cd $i ; bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE") \
          done

      - name: Convert anagram
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/ptrdist-1.1-reverted/anagram
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of anagram
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/ptrdist-1.1-reverted/anagram
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of anagram
        uses: actions/upload-artifact@v2
        with:
          name: anagram_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/ptrdist-1.1-reverted/anagram/3c_performance_stats/
          retention-days: 5

      - name: Build converted anagram (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/ptrdist-1.1-reverted/anagram
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo anagram >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/ptrdist-1.1-reverted/failed-components-list.txt

      - name: Convert ft
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/ptrdist-1.1-reverted/ft
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of ft
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/ptrdist-1.1-reverted/ft
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of ft
        uses: actions/upload-artifact@v2
        with:
          name: ft_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/ptrdist-1.1-reverted/ft/3c_performance_stats/
          retention-days: 5

      - name: Build converted ft (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/ptrdist-1.1-reverted/ft
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo ft >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/ptrdist-1.1-reverted/failed-components-list.txt

      - name: Convert ks
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/ptrdist-1.1-reverted/ks
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of ks
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/ptrdist-1.1-reverted/ks
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of ks
        uses: actions/upload-artifact@v2
        with:
          name: ks_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/ptrdist-1.1-reverted/ks/3c_performance_stats/
          retention-days: 5

      - name: Build converted ks (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/ptrdist-1.1-reverted/ks
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo ks >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/ptrdist-1.1-reverted/failed-components-list.txt

      - name: Convert yacr2
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/ptrdist-1.1-reverted/yacr2
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of yacr2
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/ptrdist-1.1-reverted/yacr2
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of yacr2
        uses: actions/upload-artifact@v2
        with:
          name: yacr2_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/ptrdist-1.1-reverted/yacr2/3c_performance_stats/
          retention-days: 5

      - name: Build converted yacr2 (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/ptrdist-1.1-reverted/yacr2
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo yacr2 >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/ptrdist-1.1-reverted/failed-components-list.txt

      - name: Check for deferred post-conversion build failures
        run: |
          if [ -e ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/ptrdist-1.1-reverted/failed-components-list.txt ]; then
              echo 'Failed components (see previous post-conversion build steps):'
              cat ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/ptrdist-1.1-reverted/failed-components-list.txt
              exit 1
          fi

  test_ptrdist-reverted_expand_macros_alltypes:
    name: Test PtrDistReverted (macro-expanded, -alltypes)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build PtrDistReverted
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/expand_macros_alltypes
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes
          tar -xvzf ${{env.benchmark_tar_dir}}/ptrdist-1.1-reverted.tar.gz
          cd ptrdist-1.1-reverted
          ( cd yacr2 ; \
            sed -Ei 's/^long (.*costMatrix)/ulong \1/' assign.h
            for header in *.h  ; do
              src="$(basename "$header" .h).c"
              new_header="$(basename "$header" .h)_code.h"
              test -e "$src" || continue
              sed -ne '/^#ifdef.*CODE/,/#else.*CODE/{ /^#/!p; }' "$header" >"$new_header"
              sed -i "/#define.*_CODE/d; /#include \"$header\"/a#include \"$new_header\"" "$src"
            done )
          for i in anagram ft ks yacr2 ; do \
            (cd $i ; bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE") \
          done

      - name: Convert anagram
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/ptrdist-1.1-reverted/anagram
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of anagram
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/ptrdist-1.1-reverted/anagram
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of anagram
        uses: actions/upload-artifact@v2
        with:
          name: anagram_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes/ptrdist-1.1-reverted/anagram/3c_performance_stats/
          retention-days: 5

      - name: Build converted anagram (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/ptrdist-1.1-reverted/anagram
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo anagram >>${{env.benchmark_conv_dir}}/expand_macros_alltypes/ptrdist-1.1-reverted/failed-components-list.txt

      - name: Convert ft
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/ptrdist-1.1-reverted/ft
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of ft
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/ptrdist-1.1-reverted/ft
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of ft
        uses: actions/upload-artifact@v2
        with:
          name: ft_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes/ptrdist-1.1-reverted/ft/3c_performance_stats/
          retention-days: 5

      - name: Build converted ft (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/ptrdist-1.1-reverted/ft
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo ft >>${{env.benchmark_conv_dir}}/expand_macros_alltypes/ptrdist-1.1-reverted/failed-components-list.txt

      - name: Convert ks
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/ptrdist-1.1-reverted/ks
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of ks
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/ptrdist-1.1-reverted/ks
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of ks
        uses: actions/upload-artifact@v2
        with:
          name: ks_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes/ptrdist-1.1-reverted/ks/3c_performance_stats/
          retention-days: 5

      - name: Build converted ks (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/ptrdist-1.1-reverted/ks
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo ks >>${{env.benchmark_conv_dir}}/expand_macros_alltypes/ptrdist-1.1-reverted/failed-components-list.txt

      - name: Convert yacr2
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/ptrdist-1.1-reverted/yacr2
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of yacr2
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/ptrdist-1.1-reverted/yacr2
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of yacr2
        uses: actions/upload-artifact@v2
        with:
          name: yacr2_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes/ptrdist-1.1-reverted/yacr2/3c_performance_stats/
          retention-days: 5

      - name: Build converted yacr2 (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/ptrdist-1.1-reverted/yacr2
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo yacr2 >>${{env.benchmark_conv_dir}}/expand_macros_alltypes/ptrdist-1.1-reverted/failed-components-list.txt

      - name: Check for deferred post-conversion build failures
        run: |
          if [ -e ${{env.benchmark_conv_dir}}/expand_macros_alltypes/ptrdist-1.1-reverted/failed-components-list.txt ]; then
              echo 'Failed components (see previous post-conversion build steps):'
              cat ${{env.benchmark_conv_dir}}/expand_macros_alltypes/ptrdist-1.1-reverted/failed-components-list.txt
              exit 1
          fi

  test_ptrdist-manual_no_expand_macros_no_alltypes:
    name: Test PtrDistManual (not macro-expanded, no -alltypes)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build PtrDistManual
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes
          tar -xvzf ${{env.benchmark_tar_dir}}/ptrdist-1.1-manual.tar.gz
          cd ptrdist-1.1-manual
          ( cd yacr2 ; \
            sed -Ei 's/^long (.*costMatrix)/ulong \1/' assign.h
            for header in *.h  ; do
              src="$(basename "$header" .h).c"
              new_header="$(basename "$header" .h)_code.h"
              test -e "$src" || continue
              sed -ne '/^#ifdef.*CODE/,/#else.*CODE/{ /^#/!p; }' "$header" >"$new_header"
              sed -i "/#define.*_CODE/d; /#include \"$header\"/a#include \"$new_header\"" "$src"
            done )
          for i in anagram ft ks yacr2 ; do \
            (cd $i ; bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE") \
          done

      - name: Convert anagram
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/ptrdist-1.1-manual/anagram
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --project_path .

      - name: Copy 3c stats of anagram
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/ptrdist-1.1-manual/anagram
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of anagram
        uses: actions/upload-artifact@v2
        with:
          name: anagram_no_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/ptrdist-1.1-manual/anagram/3c_performance_stats/
          retention-days: 5

      - name: Build converted anagram (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/ptrdist-1.1-manual/anagram
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" || echo anagram >>${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/ptrdist-1.1-manual/failed-components-list.txt

      - name: Convert ft
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/ptrdist-1.1-manual/ft
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --project_path .

      - name: Copy 3c stats of ft
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/ptrdist-1.1-manual/ft
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of ft
        uses: actions/upload-artifact@v2
        with:
          name: ft_no_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/ptrdist-1.1-manual/ft/3c_performance_stats/
          retention-days: 5

      - name: Build converted ft (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/ptrdist-1.1-manual/ft
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" || echo ft >>${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/ptrdist-1.1-manual/failed-components-list.txt

      - name: Convert ks
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/ptrdist-1.1-manual/ks
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --project_path .

      - name: Copy 3c stats of ks
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/ptrdist-1.1-manual/ks
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of ks
        uses: actions/upload-artifact@v2
        with:
          name: ks_no_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/ptrdist-1.1-manual/ks/3c_performance_stats/
          retention-days: 5

      - name: Build converted ks (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/ptrdist-1.1-manual/ks
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" || echo ks >>${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/ptrdist-1.1-manual/failed-components-list.txt

      - name: Convert yacr2
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/ptrdist-1.1-manual/yacr2
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --project_path .

      - name: Copy 3c stats of yacr2
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/ptrdist-1.1-manual/yacr2
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of yacr2
        uses: actions/upload-artifact@v2
        with:
          name: yacr2_no_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/ptrdist-1.1-manual/yacr2/3c_performance_stats/
          retention-days: 5

      - name: Build converted yacr2 (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/ptrdist-1.1-manual/yacr2
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" || echo yacr2 >>${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/ptrdist-1.1-manual/failed-components-list.txt

      - name: Check for deferred post-conversion build failures
        run: |
          if [ -e ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/ptrdist-1.1-manual/failed-components-list.txt ]; then
              echo 'Failed components (see previous post-conversion build steps):'
              cat ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/ptrdist-1.1-manual/failed-components-list.txt
              exit 1
          fi

  test_ptrdist-manual_no_expand_macros_alltypes_only_g_sol:
    name: Test PtrDistManual (not macro-expanded, -alltypes, greatest solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build PtrDistManual
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol
          tar -xvzf ${{env.benchmark_tar_dir}}/ptrdist-1.1-manual.tar.gz
          cd ptrdist-1.1-manual
          ( cd yacr2 ; \
            sed -Ei 's/^long (.*costMatrix)/ulong \1/' assign.h
            for header in *.h  ; do
              src="$(basename "$header" .h).c"
              new_header="$(basename "$header" .h)_code.h"
              test -e "$src" || continue
              sed -ne '/^#ifdef.*CODE/,/#else.*CODE/{ /^#/!p; }' "$header" >"$new_header"
              sed -i "/#define.*_CODE/d; /#include \"$header\"/a#include \"$new_header\"" "$src"
            done )
          for i in anagram ft ks yacr2 ; do \
            (cd $i ; bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE") \
          done

      - name: Convert anagram
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/ptrdist-1.1-manual/anagram
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of anagram
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/ptrdist-1.1-manual/anagram
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of anagram
        uses: actions/upload-artifact@v2
        with:
          name: anagram_no_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/ptrdist-1.1-manual/anagram/3c_performance_stats/
          retention-days: 5

      - name: Build converted anagram (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/ptrdist-1.1-manual/anagram
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo anagram >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/ptrdist-1.1-manual/failed-components-list.txt

      - name: Convert ft
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/ptrdist-1.1-manual/ft
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of ft
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/ptrdist-1.1-manual/ft
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of ft
        uses: actions/upload-artifact@v2
        with:
          name: ft_no_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/ptrdist-1.1-manual/ft/3c_performance_stats/
          retention-days: 5

      - name: Build converted ft (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/ptrdist-1.1-manual/ft
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo ft >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/ptrdist-1.1-manual/failed-components-list.txt

      - name: Convert ks
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/ptrdist-1.1-manual/ks
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of ks
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/ptrdist-1.1-manual/ks
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of ks
        uses: actions/upload-artifact@v2
        with:
          name: ks_no_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/ptrdist-1.1-manual/ks/3c_performance_stats/
          retention-days: 5

      - name: Build converted ks (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/ptrdist-1.1-manual/ks
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo ks >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/ptrdist-1.1-manual/failed-components-list.txt

      - name: Convert yacr2
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/ptrdist-1.1-manual/yacr2
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of yacr2
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/ptrdist-1.1-manual/yacr2
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of yacr2
        uses: actions/upload-artifact@v2
        with:
          name: yacr2_no_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/ptrdist-1.1-manual/yacr2/3c_performance_stats/
          retention-days: 5

      - name: Build converted yacr2 (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/ptrdist-1.1-manual/yacr2
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo yacr2 >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/ptrdist-1.1-manual/failed-components-list.txt

      - name: Check for deferred post-conversion build failures
        run: |
          if [ -e ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/ptrdist-1.1-manual/failed-components-list.txt ]; then
              echo 'Failed components (see previous post-conversion build steps):'
              cat ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/ptrdist-1.1-manual/failed-components-list.txt
              exit 1
          fi

  test_ptrdist-manual_no_expand_macros_alltypes_only_l_sol:
    name: Test PtrDistManual (not macro-expanded, -alltypes, least solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build PtrDistManual
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol
          tar -xvzf ${{env.benchmark_tar_dir}}/ptrdist-1.1-manual.tar.gz
          cd ptrdist-1.1-manual
          ( cd yacr2 ; \
            sed -Ei 's/^long (.*costMatrix)/ulong \1/' assign.h
            for header in *.h  ; do
              src="$(basename "$header" .h).c"
              new_header="$(basename "$header" .h)_code.h"
              test -e "$src" || continue
              sed -ne '/^#ifdef.*CODE/,/#else.*CODE/{ /^#/!p; }' "$header" >"$new_header"
              sed -i "/#define.*_CODE/d; /#include \"$header\"/a#include \"$new_header\"" "$src"
            done )
          for i in anagram ft ks yacr2 ; do \
            (cd $i ; bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE") \
          done

      - name: Convert anagram
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/ptrdist-1.1-manual/anagram
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of anagram
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/ptrdist-1.1-manual/anagram
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of anagram
        uses: actions/upload-artifact@v2
        with:
          name: anagram_no_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/ptrdist-1.1-manual/anagram/3c_performance_stats/
          retention-days: 5

      - name: Build converted anagram (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/ptrdist-1.1-manual/anagram
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo anagram >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/ptrdist-1.1-manual/failed-components-list.txt

      - name: Convert ft
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/ptrdist-1.1-manual/ft
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of ft
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/ptrdist-1.1-manual/ft
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of ft
        uses: actions/upload-artifact@v2
        with:
          name: ft_no_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/ptrdist-1.1-manual/ft/3c_performance_stats/
          retention-days: 5

      - name: Build converted ft (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/ptrdist-1.1-manual/ft
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo ft >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/ptrdist-1.1-manual/failed-components-list.txt

      - name: Convert ks
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/ptrdist-1.1-manual/ks
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of ks
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/ptrdist-1.1-manual/ks
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of ks
        uses: actions/upload-artifact@v2
        with:
          name: ks_no_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/ptrdist-1.1-manual/ks/3c_performance_stats/
          retention-days: 5

      - name: Build converted ks (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/ptrdist-1.1-manual/ks
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo ks >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/ptrdist-1.1-manual/failed-components-list.txt

      - name: Convert yacr2
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/ptrdist-1.1-manual/yacr2
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of yacr2
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/ptrdist-1.1-manual/yacr2
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of yacr2
        uses: actions/upload-artifact@v2
        with:
          name: yacr2_no_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/ptrdist-1.1-manual/yacr2/3c_performance_stats/
          retention-days: 5

      - name: Build converted yacr2 (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/ptrdist-1.1-manual/yacr2
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo yacr2 >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/ptrdist-1.1-manual/failed-components-list.txt

      - name: Check for deferred post-conversion build failures
        run: |
          if [ -e ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/ptrdist-1.1-manual/failed-components-list.txt ]; then
              echo 'Failed components (see previous post-conversion build steps):'
              cat ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/ptrdist-1.1-manual/failed-components-list.txt
              exit 1
          fi

  test_ptrdist-manual_no_expand_macros_alltypes_disable_rds:
    name: Test PtrDistManual (not macro-expanded, -alltypes, CCured solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build PtrDistManual
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds
          tar -xvzf ${{env.benchmark_tar_dir}}/ptrdist-1.1-manual.tar.gz
          cd ptrdist-1.1-manual
          ( cd yacr2 ; \
            sed -Ei 's/^long (.*costMatrix)/ulong \1/' assign.h
            for header in *.h  ; do
              src="$(basename "$header" .h).c"
              new_header="$(basename "$header" .h)_code.h"
              test -e "$src" || continue
              sed -ne '/^#ifdef.*CODE/,/#else.*CODE/{ /^#/!p; }' "$header" >"$new_header"
              sed -i "/#define.*_CODE/d; /#include \"$header\"/a#include \"$new_header\"" "$src"
            done )
          for i in anagram ft ks yacr2 ; do \
            (cd $i ; bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE") \
          done

      - name: Convert anagram
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/ptrdist-1.1-manual/anagram
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of anagram
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/ptrdist-1.1-manual/anagram
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of anagram
        uses: actions/upload-artifact@v2
        with:
          name: anagram_no_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/ptrdist-1.1-manual/anagram/3c_performance_stats/
          retention-days: 5

      - name: Build converted anagram (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/ptrdist-1.1-manual/anagram
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo anagram >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/ptrdist-1.1-manual/failed-components-list.txt

      - name: Convert ft
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/ptrdist-1.1-manual/ft
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of ft
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/ptrdist-1.1-manual/ft
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of ft
        uses: actions/upload-artifact@v2
        with:
          name: ft_no_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/ptrdist-1.1-manual/ft/3c_performance_stats/
          retention-days: 5

      - name: Build converted ft (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/ptrdist-1.1-manual/ft
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo ft >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/ptrdist-1.1-manual/failed-components-list.txt

      - name: Convert ks
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/ptrdist-1.1-manual/ks
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of ks
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/ptrdist-1.1-manual/ks
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of ks
        uses: actions/upload-artifact@v2
        with:
          name: ks_no_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/ptrdist-1.1-manual/ks/3c_performance_stats/
          retention-days: 5

      - name: Build converted ks (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/ptrdist-1.1-manual/ks
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo ks >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/ptrdist-1.1-manual/failed-components-list.txt

      - name: Convert yacr2
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/ptrdist-1.1-manual/yacr2
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of yacr2
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/ptrdist-1.1-manual/yacr2
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of yacr2
        uses: actions/upload-artifact@v2
        with:
          name: yacr2_no_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/ptrdist-1.1-manual/yacr2/3c_performance_stats/
          retention-days: 5

      - name: Build converted yacr2 (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/ptrdist-1.1-manual/yacr2
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo yacr2 >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/ptrdist-1.1-manual/failed-components-list.txt

      - name: Check for deferred post-conversion build failures
        run: |
          if [ -e ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/ptrdist-1.1-manual/failed-components-list.txt ]; then
              echo 'Failed components (see previous post-conversion build steps):'
              cat ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/ptrdist-1.1-manual/failed-components-list.txt
              exit 1
          fi

  test_ptrdist-manual_no_expand_macros_alltypes_disable_fnedgs:
    name: Test PtrDistManual (not macro-expanded, -alltypes, FuncRevEdges solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build PtrDistManual
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs
          tar -xvzf ${{env.benchmark_tar_dir}}/ptrdist-1.1-manual.tar.gz
          cd ptrdist-1.1-manual
          ( cd yacr2 ; \
            sed -Ei 's/^long (.*costMatrix)/ulong \1/' assign.h
            for header in *.h  ; do
              src="$(basename "$header" .h).c"
              new_header="$(basename "$header" .h)_code.h"
              test -e "$src" || continue
              sed -ne '/^#ifdef.*CODE/,/#else.*CODE/{ /^#/!p; }' "$header" >"$new_header"
              sed -i "/#define.*_CODE/d; /#include \"$header\"/a#include \"$new_header\"" "$src"
            done )
          for i in anagram ft ks yacr2 ; do \
            (cd $i ; bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE") \
          done

      - name: Convert anagram
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/ptrdist-1.1-manual/anagram
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of anagram
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/ptrdist-1.1-manual/anagram
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of anagram
        uses: actions/upload-artifact@v2
        with:
          name: anagram_no_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/ptrdist-1.1-manual/anagram/3c_performance_stats/
          retention-days: 5

      - name: Build converted anagram (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/ptrdist-1.1-manual/anagram
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo anagram >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/ptrdist-1.1-manual/failed-components-list.txt

      - name: Convert ft
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/ptrdist-1.1-manual/ft
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of ft
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/ptrdist-1.1-manual/ft
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of ft
        uses: actions/upload-artifact@v2
        with:
          name: ft_no_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/ptrdist-1.1-manual/ft/3c_performance_stats/
          retention-days: 5

      - name: Build converted ft (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/ptrdist-1.1-manual/ft
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo ft >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/ptrdist-1.1-manual/failed-components-list.txt

      - name: Convert ks
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/ptrdist-1.1-manual/ks
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of ks
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/ptrdist-1.1-manual/ks
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of ks
        uses: actions/upload-artifact@v2
        with:
          name: ks_no_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/ptrdist-1.1-manual/ks/3c_performance_stats/
          retention-days: 5

      - name: Build converted ks (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/ptrdist-1.1-manual/ks
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo ks >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/ptrdist-1.1-manual/failed-components-list.txt

      - name: Convert yacr2
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/ptrdist-1.1-manual/yacr2
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of yacr2
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/ptrdist-1.1-manual/yacr2
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of yacr2
        uses: actions/upload-artifact@v2
        with:
          name: yacr2_no_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/ptrdist-1.1-manual/yacr2/3c_performance_stats/
          retention-days: 5

      - name: Build converted yacr2 (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/ptrdist-1.1-manual/yacr2
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo yacr2 >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/ptrdist-1.1-manual/failed-components-list.txt

      - name: Check for deferred post-conversion build failures
        run: |
          if [ -e ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/ptrdist-1.1-manual/failed-components-list.txt ]; then
              echo 'Failed components (see previous post-conversion build steps):'
              cat ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/ptrdist-1.1-manual/failed-components-list.txt
              exit 1
          fi

  test_ptrdist-manual_no_expand_macros_alltypes:
    name: Test PtrDistManual (not macro-expanded, -alltypes)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build PtrDistManual
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes
          tar -xvzf ${{env.benchmark_tar_dir}}/ptrdist-1.1-manual.tar.gz
          cd ptrdist-1.1-manual
          ( cd yacr2 ; \
            sed -Ei 's/^long (.*costMatrix)/ulong \1/' assign.h
            for header in *.h  ; do
              src="$(basename "$header" .h).c"
              new_header="$(basename "$header" .h)_code.h"
              test -e "$src" || continue
              sed -ne '/^#ifdef.*CODE/,/#else.*CODE/{ /^#/!p; }' "$header" >"$new_header"
              sed -i "/#define.*_CODE/d; /#include \"$header\"/a#include \"$new_header\"" "$src"
            done )
          for i in anagram ft ks yacr2 ; do \
            (cd $i ; bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE") \
          done

      - name: Convert anagram
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/ptrdist-1.1-manual/anagram
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --project_path .

      - name: Copy 3c stats of anagram
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/ptrdist-1.1-manual/anagram
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of anagram
        uses: actions/upload-artifact@v2
        with:
          name: anagram_no_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/ptrdist-1.1-manual/anagram/3c_performance_stats/
          retention-days: 5

      - name: Build converted anagram (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/ptrdist-1.1-manual/anagram
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo anagram >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/ptrdist-1.1-manual/failed-components-list.txt

      - name: Convert ft
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/ptrdist-1.1-manual/ft
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --project_path .

      - name: Copy 3c stats of ft
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/ptrdist-1.1-manual/ft
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of ft
        uses: actions/upload-artifact@v2
        with:
          name: ft_no_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/ptrdist-1.1-manual/ft/3c_performance_stats/
          retention-days: 5

      - name: Build converted ft (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/ptrdist-1.1-manual/ft
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo ft >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/ptrdist-1.1-manual/failed-components-list.txt

      - name: Convert ks
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/ptrdist-1.1-manual/ks
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --project_path .

      - name: Copy 3c stats of ks
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/ptrdist-1.1-manual/ks
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of ks
        uses: actions/upload-artifact@v2
        with:
          name: ks_no_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/ptrdist-1.1-manual/ks/3c_performance_stats/
          retention-days: 5

      - name: Build converted ks (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/ptrdist-1.1-manual/ks
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo ks >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/ptrdist-1.1-manual/failed-components-list.txt

      - name: Convert yacr2
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/ptrdist-1.1-manual/yacr2
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --project_path .

      - name: Copy 3c stats of yacr2
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/ptrdist-1.1-manual/yacr2
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of yacr2
        uses: actions/upload-artifact@v2
        with:
          name: yacr2_no_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/ptrdist-1.1-manual/yacr2/3c_performance_stats/
          retention-days: 5

      - name: Build converted yacr2 (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/ptrdist-1.1-manual/yacr2
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo yacr2 >>${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/ptrdist-1.1-manual/failed-components-list.txt

      - name: Check for deferred post-conversion build failures
        run: |
          if [ -e ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/ptrdist-1.1-manual/failed-components-list.txt ]; then
              echo 'Failed components (see previous post-conversion build steps):'
              cat ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/ptrdist-1.1-manual/failed-components-list.txt
              exit 1
          fi

  test_ptrdist-manual_expand_macros_no_alltypes:
    name: Test PtrDistManual (macro-expanded, no -alltypes)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build PtrDistManual
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes
          tar -xvzf ${{env.benchmark_tar_dir}}/ptrdist-1.1-manual.tar.gz
          cd ptrdist-1.1-manual
          ( cd yacr2 ; \
            sed -Ei 's/^long (.*costMatrix)/ulong \1/' assign.h
            for header in *.h  ; do
              src="$(basename "$header" .h).c"
              new_header="$(basename "$header" .h)_code.h"
              test -e "$src" || continue
              sed -ne '/^#ifdef.*CODE/,/#else.*CODE/{ /^#/!p; }' "$header" >"$new_header"
              sed -i "/#define.*_CODE/d; /#include \"$header\"/a#include \"$new_header\"" "$src"
            done )
          for i in anagram ft ks yacr2 ; do \
            (cd $i ; bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE") \
          done

      - name: Convert anagram
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/ptrdist-1.1-manual/anagram
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of anagram
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/ptrdist-1.1-manual/anagram
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of anagram
        uses: actions/upload-artifact@v2
        with:
          name: anagram_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/ptrdist-1.1-manual/anagram/3c_performance_stats/
          retention-days: 5

      - name: Build converted anagram (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/ptrdist-1.1-manual/anagram
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" || echo anagram >>${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/ptrdist-1.1-manual/failed-components-list.txt

      - name: Convert ft
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/ptrdist-1.1-manual/ft
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of ft
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/ptrdist-1.1-manual/ft
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of ft
        uses: actions/upload-artifact@v2
        with:
          name: ft_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/ptrdist-1.1-manual/ft/3c_performance_stats/
          retention-days: 5

      - name: Build converted ft (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/ptrdist-1.1-manual/ft
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" || echo ft >>${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/ptrdist-1.1-manual/failed-components-list.txt

      - name: Convert ks
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/ptrdist-1.1-manual/ks
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of ks
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/ptrdist-1.1-manual/ks
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of ks
        uses: actions/upload-artifact@v2
        with:
          name: ks_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/ptrdist-1.1-manual/ks/3c_performance_stats/
          retention-days: 5

      - name: Build converted ks (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/ptrdist-1.1-manual/ks
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" || echo ks >>${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/ptrdist-1.1-manual/failed-components-list.txt

      - name: Convert yacr2
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/ptrdist-1.1-manual/yacr2
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of yacr2
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/ptrdist-1.1-manual/yacr2
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of yacr2
        uses: actions/upload-artifact@v2
        with:
          name: yacr2_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/ptrdist-1.1-manual/yacr2/3c_performance_stats/
          retention-days: 5

      - name: Build converted yacr2 (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/ptrdist-1.1-manual/yacr2
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" || echo yacr2 >>${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/ptrdist-1.1-manual/failed-components-list.txt

      - name: Check for deferred post-conversion build failures
        run: |
          if [ -e ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/ptrdist-1.1-manual/failed-components-list.txt ]; then
              echo 'Failed components (see previous post-conversion build steps):'
              cat ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/ptrdist-1.1-manual/failed-components-list.txt
              exit 1
          fi

  test_ptrdist-manual_expand_macros_alltypes_only_g_sol:
    name: Test PtrDistManual (macro-expanded, -alltypes, greatest solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build PtrDistManual
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol
          tar -xvzf ${{env.benchmark_tar_dir}}/ptrdist-1.1-manual.tar.gz
          cd ptrdist-1.1-manual
          ( cd yacr2 ; \
            sed -Ei 's/^long (.*costMatrix)/ulong \1/' assign.h
            for header in *.h  ; do
              src="$(basename "$header" .h).c"
              new_header="$(basename "$header" .h)_code.h"
              test -e "$src" || continue
              sed -ne '/^#ifdef.*CODE/,/#else.*CODE/{ /^#/!p; }' "$header" >"$new_header"
              sed -i "/#define.*_CODE/d; /#include \"$header\"/a#include \"$new_header\"" "$src"
            done )
          for i in anagram ft ks yacr2 ; do \
            (cd $i ; bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE") \
          done

      - name: Convert anagram
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/ptrdist-1.1-manual/anagram
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of anagram
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/ptrdist-1.1-manual/anagram
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of anagram
        uses: actions/upload-artifact@v2
        with:
          name: anagram_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/ptrdist-1.1-manual/anagram/3c_performance_stats/
          retention-days: 5

      - name: Build converted anagram (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/ptrdist-1.1-manual/anagram
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo anagram >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/ptrdist-1.1-manual/failed-components-list.txt

      - name: Convert ft
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/ptrdist-1.1-manual/ft
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of ft
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/ptrdist-1.1-manual/ft
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of ft
        uses: actions/upload-artifact@v2
        with:
          name: ft_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/ptrdist-1.1-manual/ft/3c_performance_stats/
          retention-days: 5

      - name: Build converted ft (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/ptrdist-1.1-manual/ft
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo ft >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/ptrdist-1.1-manual/failed-components-list.txt

      - name: Convert ks
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/ptrdist-1.1-manual/ks
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of ks
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/ptrdist-1.1-manual/ks
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of ks
        uses: actions/upload-artifact@v2
        with:
          name: ks_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/ptrdist-1.1-manual/ks/3c_performance_stats/
          retention-days: 5

      - name: Build converted ks (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/ptrdist-1.1-manual/ks
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo ks >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/ptrdist-1.1-manual/failed-components-list.txt

      - name: Convert yacr2
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/ptrdist-1.1-manual/yacr2
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of yacr2
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/ptrdist-1.1-manual/yacr2
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of yacr2
        uses: actions/upload-artifact@v2
        with:
          name: yacr2_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/ptrdist-1.1-manual/yacr2/3c_performance_stats/
          retention-days: 5

      - name: Build converted yacr2 (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/ptrdist-1.1-manual/yacr2
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo yacr2 >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/ptrdist-1.1-manual/failed-components-list.txt

      - name: Check for deferred post-conversion build failures
        run: |
          if [ -e ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/ptrdist-1.1-manual/failed-components-list.txt ]; then
              echo 'Failed components (see previous post-conversion build steps):'
              cat ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/ptrdist-1.1-manual/failed-components-list.txt
              exit 1
          fi

  test_ptrdist-manual_expand_macros_alltypes_only_l_sol:
    name: Test PtrDistManual (macro-expanded, -alltypes, least solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build PtrDistManual
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol
          tar -xvzf ${{env.benchmark_tar_dir}}/ptrdist-1.1-manual.tar.gz
          cd ptrdist-1.1-manual
          ( cd yacr2 ; \
            sed -Ei 's/^long (.*costMatrix)/ulong \1/' assign.h
            for header in *.h  ; do
              src="$(basename "$header" .h).c"
              new_header="$(basename "$header" .h)_code.h"
              test -e "$src" || continue
              sed -ne '/^#ifdef.*CODE/,/#else.*CODE/{ /^#/!p; }' "$header" >"$new_header"
              sed -i "/#define.*_CODE/d; /#include \"$header\"/a#include \"$new_header\"" "$src"
            done )
          for i in anagram ft ks yacr2 ; do \
            (cd $i ; bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE") \
          done

      - name: Convert anagram
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/ptrdist-1.1-manual/anagram
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of anagram
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/ptrdist-1.1-manual/anagram
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of anagram
        uses: actions/upload-artifact@v2
        with:
          name: anagram_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/ptrdist-1.1-manual/anagram/3c_performance_stats/
          retention-days: 5

      - name: Build converted anagram (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/ptrdist-1.1-manual/anagram
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo anagram >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/ptrdist-1.1-manual/failed-components-list.txt

      - name: Convert ft
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/ptrdist-1.1-manual/ft
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of ft
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/ptrdist-1.1-manual/ft
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of ft
        uses: actions/upload-artifact@v2
        with:
          name: ft_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/ptrdist-1.1-manual/ft/3c_performance_stats/
          retention-days: 5

      - name: Build converted ft (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/ptrdist-1.1-manual/ft
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo ft >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/ptrdist-1.1-manual/failed-components-list.txt

      - name: Convert ks
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/ptrdist-1.1-manual/ks
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of ks
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/ptrdist-1.1-manual/ks
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of ks
        uses: actions/upload-artifact@v2
        with:
          name: ks_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/ptrdist-1.1-manual/ks/3c_performance_stats/
          retention-days: 5

      - name: Build converted ks (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/ptrdist-1.1-manual/ks
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo ks >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/ptrdist-1.1-manual/failed-components-list.txt

      - name: Convert yacr2
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/ptrdist-1.1-manual/yacr2
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of yacr2
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/ptrdist-1.1-manual/yacr2
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of yacr2
        uses: actions/upload-artifact@v2
        with:
          name: yacr2_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/ptrdist-1.1-manual/yacr2/3c_performance_stats/
          retention-days: 5

      - name: Build converted yacr2 (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/ptrdist-1.1-manual/yacr2
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo yacr2 >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/ptrdist-1.1-manual/failed-components-list.txt

      - name: Check for deferred post-conversion build failures
        run: |
          if [ -e ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/ptrdist-1.1-manual/failed-components-list.txt ]; then
              echo 'Failed components (see previous post-conversion build steps):'
              cat ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/ptrdist-1.1-manual/failed-components-list.txt
              exit 1
          fi

  test_ptrdist-manual_expand_macros_alltypes_disable_rds:
    name: Test PtrDistManual (macro-expanded, -alltypes, CCured solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build PtrDistManual
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds
          tar -xvzf ${{env.benchmark_tar_dir}}/ptrdist-1.1-manual.tar.gz
          cd ptrdist-1.1-manual
          ( cd yacr2 ; \
            sed -Ei 's/^long (.*costMatrix)/ulong \1/' assign.h
            for header in *.h  ; do
              src="$(basename "$header" .h).c"
              new_header="$(basename "$header" .h)_code.h"
              test -e "$src" || continue
              sed -ne '/^#ifdef.*CODE/,/#else.*CODE/{ /^#/!p; }' "$header" >"$new_header"
              sed -i "/#define.*_CODE/d; /#include \"$header\"/a#include \"$new_header\"" "$src"
            done )
          for i in anagram ft ks yacr2 ; do \
            (cd $i ; bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE") \
          done

      - name: Convert anagram
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/ptrdist-1.1-manual/anagram
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of anagram
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/ptrdist-1.1-manual/anagram
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of anagram
        uses: actions/upload-artifact@v2
        with:
          name: anagram_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/ptrdist-1.1-manual/anagram/3c_performance_stats/
          retention-days: 5

      - name: Build converted anagram (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/ptrdist-1.1-manual/anagram
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo anagram >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/ptrdist-1.1-manual/failed-components-list.txt

      - name: Convert ft
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/ptrdist-1.1-manual/ft
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of ft
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/ptrdist-1.1-manual/ft
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of ft
        uses: actions/upload-artifact@v2
        with:
          name: ft_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/ptrdist-1.1-manual/ft/3c_performance_stats/
          retention-days: 5

      - name: Build converted ft (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/ptrdist-1.1-manual/ft
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo ft >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/ptrdist-1.1-manual/failed-components-list.txt

      - name: Convert ks
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/ptrdist-1.1-manual/ks
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of ks
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/ptrdist-1.1-manual/ks
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of ks
        uses: actions/upload-artifact@v2
        with:
          name: ks_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/ptrdist-1.1-manual/ks/3c_performance_stats/
          retention-days: 5

      - name: Build converted ks (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/ptrdist-1.1-manual/ks
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo ks >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/ptrdist-1.1-manual/failed-components-list.txt

      - name: Convert yacr2
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/ptrdist-1.1-manual/yacr2
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of yacr2
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/ptrdist-1.1-manual/yacr2
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of yacr2
        uses: actions/upload-artifact@v2
        with:
          name: yacr2_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/ptrdist-1.1-manual/yacr2/3c_performance_stats/
          retention-days: 5

      - name: Build converted yacr2 (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/ptrdist-1.1-manual/yacr2
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo yacr2 >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/ptrdist-1.1-manual/failed-components-list.txt

      - name: Check for deferred post-conversion build failures
        run: |
          if [ -e ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/ptrdist-1.1-manual/failed-components-list.txt ]; then
              echo 'Failed components (see previous post-conversion build steps):'
              cat ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/ptrdist-1.1-manual/failed-components-list.txt
              exit 1
          fi

  test_ptrdist-manual_expand_macros_alltypes_disable_fnedgs:
    name: Test PtrDistManual (macro-expanded, -alltypes, FuncRevEdges solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build PtrDistManual
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs
          tar -xvzf ${{env.benchmark_tar_dir}}/ptrdist-1.1-manual.tar.gz
          cd ptrdist-1.1-manual
          ( cd yacr2 ; \
            sed -Ei 's/^long (.*costMatrix)/ulong \1/' assign.h
            for header in *.h  ; do
              src="$(basename "$header" .h).c"
              new_header="$(basename "$header" .h)_code.h"
              test -e "$src" || continue
              sed -ne '/^#ifdef.*CODE/,/#else.*CODE/{ /^#/!p; }' "$header" >"$new_header"
              sed -i "/#define.*_CODE/d; /#include \"$header\"/a#include \"$new_header\"" "$src"
            done )
          for i in anagram ft ks yacr2 ; do \
            (cd $i ; bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE") \
          done

      - name: Convert anagram
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/ptrdist-1.1-manual/anagram
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of anagram
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/ptrdist-1.1-manual/anagram
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of anagram
        uses: actions/upload-artifact@v2
        with:
          name: anagram_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/ptrdist-1.1-manual/anagram/3c_performance_stats/
          retention-days: 5

      - name: Build converted anagram (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/ptrdist-1.1-manual/anagram
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo anagram >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/ptrdist-1.1-manual/failed-components-list.txt

      - name: Convert ft
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/ptrdist-1.1-manual/ft
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of ft
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/ptrdist-1.1-manual/ft
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of ft
        uses: actions/upload-artifact@v2
        with:
          name: ft_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/ptrdist-1.1-manual/ft/3c_performance_stats/
          retention-days: 5

      - name: Build converted ft (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/ptrdist-1.1-manual/ft
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo ft >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/ptrdist-1.1-manual/failed-components-list.txt

      - name: Convert ks
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/ptrdist-1.1-manual/ks
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of ks
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/ptrdist-1.1-manual/ks
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of ks
        uses: actions/upload-artifact@v2
        with:
          name: ks_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/ptrdist-1.1-manual/ks/3c_performance_stats/
          retention-days: 5

      - name: Build converted ks (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/ptrdist-1.1-manual/ks
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo ks >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/ptrdist-1.1-manual/failed-components-list.txt

      - name: Convert yacr2
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/ptrdist-1.1-manual/yacr2
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of yacr2
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/ptrdist-1.1-manual/yacr2
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of yacr2
        uses: actions/upload-artifact@v2
        with:
          name: yacr2_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/ptrdist-1.1-manual/yacr2/3c_performance_stats/
          retention-days: 5

      - name: Build converted yacr2 (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/ptrdist-1.1-manual/yacr2
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo yacr2 >>${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/ptrdist-1.1-manual/failed-components-list.txt

      - name: Check for deferred post-conversion build failures
        run: |
          if [ -e ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/ptrdist-1.1-manual/failed-components-list.txt ]; then
              echo 'Failed components (see previous post-conversion build steps):'
              cat ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/ptrdist-1.1-manual/failed-components-list.txt
              exit 1
          fi

  test_ptrdist-manual_expand_macros_alltypes:
    name: Test PtrDistManual (macro-expanded, -alltypes)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build PtrDistManual
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/expand_macros_alltypes
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes
          tar -xvzf ${{env.benchmark_tar_dir}}/ptrdist-1.1-manual.tar.gz
          cd ptrdist-1.1-manual
          ( cd yacr2 ; \
            sed -Ei 's/^long (.*costMatrix)/ulong \1/' assign.h
            for header in *.h  ; do
              src="$(basename "$header" .h).c"
              new_header="$(basename "$header" .h)_code.h"
              test -e "$src" || continue
              sed -ne '/^#ifdef.*CODE/,/#else.*CODE/{ /^#/!p; }' "$header" >"$new_header"
              sed -i "/#define.*_CODE/d; /#include \"$header\"/a#include \"$new_header\"" "$src"
            done )
          for i in anagram ft ks yacr2 ; do \
            (cd $i ; bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE") \
          done

      - name: Convert anagram
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/ptrdist-1.1-manual/anagram
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of anagram
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/ptrdist-1.1-manual/anagram
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of anagram
        uses: actions/upload-artifact@v2
        with:
          name: anagram_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes/ptrdist-1.1-manual/anagram/3c_performance_stats/
          retention-days: 5

      - name: Build converted anagram (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/ptrdist-1.1-manual/anagram
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo anagram >>${{env.benchmark_conv_dir}}/expand_macros_alltypes/ptrdist-1.1-manual/failed-components-list.txt

      - name: Convert ft
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/ptrdist-1.1-manual/ft
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of ft
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/ptrdist-1.1-manual/ft
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of ft
        uses: actions/upload-artifact@v2
        with:
          name: ft_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes/ptrdist-1.1-manual/ft/3c_performance_stats/
          retention-days: 5

      - name: Build converted ft (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/ptrdist-1.1-manual/ft
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo ft >>${{env.benchmark_conv_dir}}/expand_macros_alltypes/ptrdist-1.1-manual/failed-components-list.txt

      - name: Convert ks
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/ptrdist-1.1-manual/ks
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of ks
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/ptrdist-1.1-manual/ks
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of ks
        uses: actions/upload-artifact@v2
        with:
          name: ks_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes/ptrdist-1.1-manual/ks/3c_performance_stats/
          retention-days: 5

      - name: Build converted ks (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/ptrdist-1.1-manual/ks
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo ks >>${{env.benchmark_conv_dir}}/expand_macros_alltypes/ptrdist-1.1-manual/failed-components-list.txt

      - name: Convert yacr2
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/ptrdist-1.1-manual/yacr2
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of yacr2
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/ptrdist-1.1-manual/yacr2
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of yacr2
        uses: actions/upload-artifact@v2
        with:
          name: yacr2_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes/ptrdist-1.1-manual/yacr2/3c_performance_stats/
          retention-days: 5

      - name: Build converted yacr2 (filter bounds inference errors) (defer failure)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/ptrdist-1.1-manual/yacr2
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k LOCAL_CFLAGS="-w -ferror-limit=0 -D_ISOC99_SOURCE" 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py || echo yacr2 >>${{env.benchmark_conv_dir}}/expand_macros_alltypes/ptrdist-1.1-manual/failed-components-list.txt

      - name: Check for deferred post-conversion build failures
        run: |
          if [ -e ${{env.benchmark_conv_dir}}/expand_macros_alltypes/ptrdist-1.1-manual/failed-components-list.txt ]; then
              echo 'Failed components (see previous post-conversion build steps):'
              cat ${{env.benchmark_conv_dir}}/expand_macros_alltypes/ptrdist-1.1-manual/failed-components-list.txt
              exit 1
          fi

  test_libarchive_no_expand_macros_no_alltypes:
    name: Test LibArchive (not macro-expanded, no -alltypes)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build LibArchive
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes
          tar -xvzf ${{env.benchmark_tar_dir}}/libarchive-3.4.3.tar.gz
          cd libarchive-3.4.3
          cd build
          cmake -DCMAKE_C_COMPILER=${{env.builddir}}/bin/clang -G Ninja -DCMAKE_C_FLAGS="-w -ferror-limit=0 -D_GNU_SOURCE" ..
          bear ninja -l $(nproc) archive

      - name: Convert LibArchive
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/libarchive-3.4.3
          ${{env.port_tools}}/convert_project.py \
            --skip '/.*/(test|test_utils|tar|cat|cpio|examples|contrib|libarchive_fe)/.*' \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --project_path . \
            --build_dir build

      - name: Copy 3c stats of LibArchive
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/libarchive-3.4.3
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of LibArchive
        uses: actions/upload-artifact@v2
        with:
          name: LibArchive_no_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/libarchive-3.4.3/3c_performance_stats/
          retention-days: 5

      - name: Build converted LibArchive
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/libarchive-3.4.3
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          cd build
          ninja -l $(nproc) -k 0 archive

  test_libarchive_no_expand_macros_alltypes_only_g_sol:
    name: Test LibArchive (not macro-expanded, -alltypes, greatest solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build LibArchive
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol
          tar -xvzf ${{env.benchmark_tar_dir}}/libarchive-3.4.3.tar.gz
          cd libarchive-3.4.3
          cd build
          cmake -DCMAKE_C_COMPILER=${{env.builddir}}/bin/clang -G Ninja -DCMAKE_C_FLAGS="-w -ferror-limit=0 -D_GNU_SOURCE" ..
          bear ninja -l $(nproc) archive

      - name: Convert LibArchive
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/libarchive-3.4.3
          ${{env.port_tools}}/convert_project.py \
            --skip '/.*/(test|test_utils|tar|cat|cpio|examples|contrib|libarchive_fe)/.*' \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-g-sol \
            --project_path . \
            --build_dir build

      - name: Copy 3c stats of LibArchive
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/libarchive-3.4.3
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of LibArchive
        uses: actions/upload-artifact@v2
        with:
          name: LibArchive_no_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/libarchive-3.4.3/3c_performance_stats/
          retention-days: 5

      - name: Build converted LibArchive (filter bounds inference errors)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/libarchive-3.4.3
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          cd build
          ninja -l $(nproc) -k 0 archive 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py

  test_libarchive_no_expand_macros_alltypes_only_l_sol:
    name: Test LibArchive (not macro-expanded, -alltypes, least solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build LibArchive
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol
          tar -xvzf ${{env.benchmark_tar_dir}}/libarchive-3.4.3.tar.gz
          cd libarchive-3.4.3
          cd build
          cmake -DCMAKE_C_COMPILER=${{env.builddir}}/bin/clang -G Ninja -DCMAKE_C_FLAGS="-w -ferror-limit=0 -D_GNU_SOURCE" ..
          bear ninja -l $(nproc) archive

      - name: Convert LibArchive
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/libarchive-3.4.3
          ${{env.port_tools}}/convert_project.py \
            --skip '/.*/(test|test_utils|tar|cat|cpio|examples|contrib|libarchive_fe)/.*' \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-l-sol \
            --project_path . \
            --build_dir build

      - name: Copy 3c stats of LibArchive
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/libarchive-3.4.3
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of LibArchive
        uses: actions/upload-artifact@v2
        with:
          name: LibArchive_no_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/libarchive-3.4.3/3c_performance_stats/
          retention-days: 5

      - name: Build converted LibArchive (filter bounds inference errors)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/libarchive-3.4.3
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          cd build
          ninja -l $(nproc) -k 0 archive 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py

  test_libarchive_no_expand_macros_alltypes_disable_rds:
    name: Test LibArchive (not macro-expanded, -alltypes, CCured solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build LibArchive
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds
          tar -xvzf ${{env.benchmark_tar_dir}}/libarchive-3.4.3.tar.gz
          cd libarchive-3.4.3
          cd build
          cmake -DCMAKE_C_COMPILER=${{env.builddir}}/bin/clang -G Ninja -DCMAKE_C_FLAGS="-w -ferror-limit=0 -D_GNU_SOURCE" ..
          bear ninja -l $(nproc) archive

      - name: Convert LibArchive
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/libarchive-3.4.3
          ${{env.port_tools}}/convert_project.py \
            --skip '/.*/(test|test_utils|tar|cat|cpio|examples|contrib|libarchive_fe)/.*' \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-rds \
            --project_path . \
            --build_dir build

      - name: Copy 3c stats of LibArchive
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/libarchive-3.4.3
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of LibArchive
        uses: actions/upload-artifact@v2
        with:
          name: LibArchive_no_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/libarchive-3.4.3/3c_performance_stats/
          retention-days: 5

      - name: Build converted LibArchive (filter bounds inference errors)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/libarchive-3.4.3
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          cd build
          ninja -l $(nproc) -k 0 archive 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py

  test_libarchive_no_expand_macros_alltypes_disable_fnedgs:
    name: Test LibArchive (not macro-expanded, -alltypes, FuncRevEdges solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build LibArchive
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs
          tar -xvzf ${{env.benchmark_tar_dir}}/libarchive-3.4.3.tar.gz
          cd libarchive-3.4.3
          cd build
          cmake -DCMAKE_C_COMPILER=${{env.builddir}}/bin/clang -G Ninja -DCMAKE_C_FLAGS="-w -ferror-limit=0 -D_GNU_SOURCE" ..
          bear ninja -l $(nproc) archive

      - name: Convert LibArchive
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/libarchive-3.4.3
          ${{env.port_tools}}/convert_project.py \
            --skip '/.*/(test|test_utils|tar|cat|cpio|examples|contrib|libarchive_fe)/.*' \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-fnedgs \
            --project_path . \
            --build_dir build

      - name: Copy 3c stats of LibArchive
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/libarchive-3.4.3
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of LibArchive
        uses: actions/upload-artifact@v2
        with:
          name: LibArchive_no_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/libarchive-3.4.3/3c_performance_stats/
          retention-days: 5

      - name: Build converted LibArchive (filter bounds inference errors)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/libarchive-3.4.3
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          cd build
          ninja -l $(nproc) -k 0 archive 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py

  test_libarchive_no_expand_macros_alltypes:
    name: Test LibArchive (not macro-expanded, -alltypes)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build LibArchive
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes
          tar -xvzf ${{env.benchmark_tar_dir}}/libarchive-3.4.3.tar.gz
          cd libarchive-3.4.3
          cd build
          cmake -DCMAKE_C_COMPILER=${{env.builddir}}/bin/clang -G Ninja -DCMAKE_C_FLAGS="-w -ferror-limit=0 -D_GNU_SOURCE" ..
          bear ninja -l $(nproc) archive

      - name: Convert LibArchive
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/libarchive-3.4.3
          ${{env.port_tools}}/convert_project.py \
            --skip '/.*/(test|test_utils|tar|cat|cpio|examples|contrib|libarchive_fe)/.*' \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --project_path . \
            --build_dir build

      - name: Copy 3c stats of LibArchive
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/libarchive-3.4.3
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of LibArchive
        uses: actions/upload-artifact@v2
        with:
          name: LibArchive_no_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/libarchive-3.4.3/3c_performance_stats/
          retention-days: 5

      - name: Build converted LibArchive (filter bounds inference errors)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/libarchive-3.4.3
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          cd build
          ninja -l $(nproc) -k 0 archive 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py

  test_libarchive_expand_macros_no_alltypes:
    name: Test LibArchive (macro-expanded, no -alltypes)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build LibArchive
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes
          tar -xvzf ${{env.benchmark_tar_dir}}/libarchive-3.4.3.tar.gz
          cd libarchive-3.4.3
          cd build
          cmake -DCMAKE_C_COMPILER=${{env.builddir}}/bin/clang -G Ninja -DCMAKE_C_FLAGS="-w -ferror-limit=0 -D_GNU_SOURCE" ..
          bear ninja -l $(nproc) archive

      - name: Convert LibArchive
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/libarchive-3.4.3
          ${{env.port_tools}}/convert_project.py \
            --skip '/.*/(test|test_utils|tar|cat|cpio|examples|contrib|libarchive_fe)/.*' \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --expand_macros_before_conversion \
            --project_path . \
            --build_dir build

      - name: Copy 3c stats of LibArchive
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/libarchive-3.4.3
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of LibArchive
        uses: actions/upload-artifact@v2
        with:
          name: LibArchive_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/libarchive-3.4.3/3c_performance_stats/
          retention-days: 5

      - name: Build converted LibArchive
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/libarchive-3.4.3
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          cd build
          ninja -l $(nproc) -k 0 archive

  test_libarchive_expand_macros_alltypes_only_g_sol:
    name: Test LibArchive (macro-expanded, -alltypes, greatest solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build LibArchive
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol
          tar -xvzf ${{env.benchmark_tar_dir}}/libarchive-3.4.3.tar.gz
          cd libarchive-3.4.3
          cd build
          cmake -DCMAKE_C_COMPILER=${{env.builddir}}/bin/clang -G Ninja -DCMAKE_C_FLAGS="-w -ferror-limit=0 -D_GNU_SOURCE" ..
          bear ninja -l $(nproc) archive

      - name: Convert LibArchive
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/libarchive-3.4.3
          ${{env.port_tools}}/convert_project.py \
            --skip '/.*/(test|test_utils|tar|cat|cpio|examples|contrib|libarchive_fe)/.*' \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-g-sol \
            --project_path . \
            --build_dir build

      - name: Copy 3c stats of LibArchive
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/libarchive-3.4.3
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of LibArchive
        uses: actions/upload-artifact@v2
        with:
          name: LibArchive_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/libarchive-3.4.3/3c_performance_stats/
          retention-days: 5

      - name: Build converted LibArchive (filter bounds inference errors)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/libarchive-3.4.3
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          cd build
          ninja -l $(nproc) -k 0 archive 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py

  test_libarchive_expand_macros_alltypes_only_l_sol:
    name: Test LibArchive (macro-expanded, -alltypes, least solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build LibArchive
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol
          tar -xvzf ${{env.benchmark_tar_dir}}/libarchive-3.4.3.tar.gz
          cd libarchive-3.4.3
          cd build
          cmake -DCMAKE_C_COMPILER=${{env.builddir}}/bin/clang -G Ninja -DCMAKE_C_FLAGS="-w -ferror-limit=0 -D_GNU_SOURCE" ..
          bear ninja -l $(nproc) archive

      - name: Convert LibArchive
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/libarchive-3.4.3
          ${{env.port_tools}}/convert_project.py \
            --skip '/.*/(test|test_utils|tar|cat|cpio|examples|contrib|libarchive_fe)/.*' \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-l-sol \
            --project_path . \
            --build_dir build

      - name: Copy 3c stats of LibArchive
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/libarchive-3.4.3
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of LibArchive
        uses: actions/upload-artifact@v2
        with:
          name: LibArchive_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/libarchive-3.4.3/3c_performance_stats/
          retention-days: 5

      - name: Build converted LibArchive (filter bounds inference errors)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/libarchive-3.4.3
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          cd build
          ninja -l $(nproc) -k 0 archive 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py

  test_libarchive_expand_macros_alltypes_disable_rds:
    name: Test LibArchive (macro-expanded, -alltypes, CCured solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build LibArchive
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds
          tar -xvzf ${{env.benchmark_tar_dir}}/libarchive-3.4.3.tar.gz
          cd libarchive-3.4.3
          cd build
          cmake -DCMAKE_C_COMPILER=${{env.builddir}}/bin/clang -G Ninja -DCMAKE_C_FLAGS="-w -ferror-limit=0 -D_GNU_SOURCE" ..
          bear ninja -l $(nproc) archive

      - name: Convert LibArchive
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/libarchive-3.4.3
          ${{env.port_tools}}/convert_project.py \
            --skip '/.*/(test|test_utils|tar|cat|cpio|examples|contrib|libarchive_fe)/.*' \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-rds \
            --project_path . \
            --build_dir build

      - name: Copy 3c stats of LibArchive
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/libarchive-3.4.3
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of LibArchive
        uses: actions/upload-artifact@v2
        with:
          name: LibArchive_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/libarchive-3.4.3/3c_performance_stats/
          retention-days: 5

      - name: Build converted LibArchive (filter bounds inference errors)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/libarchive-3.4.3
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          cd build
          ninja -l $(nproc) -k 0 archive 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py

  test_libarchive_expand_macros_alltypes_disable_fnedgs:
    name: Test LibArchive (macro-expanded, -alltypes, FuncRevEdges solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build LibArchive
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs
          tar -xvzf ${{env.benchmark_tar_dir}}/libarchive-3.4.3.tar.gz
          cd libarchive-3.4.3
          cd build
          cmake -DCMAKE_C_COMPILER=${{env.builddir}}/bin/clang -G Ninja -DCMAKE_C_FLAGS="-w -ferror-limit=0 -D_GNU_SOURCE" ..
          bear ninja -l $(nproc) archive

      - name: Convert LibArchive
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/libarchive-3.4.3
          ${{env.port_tools}}/convert_project.py \
            --skip '/.*/(test|test_utils|tar|cat|cpio|examples|contrib|libarchive_fe)/.*' \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-fnedgs \
            --project_path . \
            --build_dir build

      - name: Copy 3c stats of LibArchive
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/libarchive-3.4.3
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of LibArchive
        uses: actions/upload-artifact@v2
        with:
          name: LibArchive_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/libarchive-3.4.3/3c_performance_stats/
          retention-days: 5

      - name: Build converted LibArchive (filter bounds inference errors)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/libarchive-3.4.3
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          cd build
          ninja -l $(nproc) -k 0 archive 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py

  test_libarchive_expand_macros_alltypes:
    name: Test LibArchive (macro-expanded, -alltypes)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build LibArchive
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/expand_macros_alltypes
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes
          tar -xvzf ${{env.benchmark_tar_dir}}/libarchive-3.4.3.tar.gz
          cd libarchive-3.4.3
          cd build
          cmake -DCMAKE_C_COMPILER=${{env.builddir}}/bin/clang -G Ninja -DCMAKE_C_FLAGS="-w -ferror-limit=0 -D_GNU_SOURCE" ..
          bear ninja -l $(nproc) archive

      - name: Convert LibArchive
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/libarchive-3.4.3
          ${{env.port_tools}}/convert_project.py \
            --skip '/.*/(test|test_utils|tar|cat|cpio|examples|contrib|libarchive_fe)/.*' \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --project_path . \
            --build_dir build

      - name: Copy 3c stats of LibArchive
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/libarchive-3.4.3
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of LibArchive
        uses: actions/upload-artifact@v2
        with:
          name: LibArchive_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes/libarchive-3.4.3/3c_performance_stats/
          retention-days: 5

      - name: Build converted LibArchive (filter bounds inference errors)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/libarchive-3.4.3
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          cd build
          ninja -l $(nproc) -k 0 archive 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py

  test_lua_no_expand_macros_no_alltypes:
    name: Test Lua (not macro-expanded, no -alltypes)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Lua
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes
          tar -xvzf ${{env.benchmark_tar_dir}}/lua-5.4.1.tar.gz
          cd lua-5.4.1
          bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" CFLAGS="-w -ferror-limit=0" linux
          ( cd src ; \
            clang-rename-10 -pl -i \
              --qualified-name=main \
              --new-name=luac_main \
              luac.c )

      - name: Convert Lua
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/lua-5.4.1
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --project_path .

      - name: Copy 3c stats of Lua
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/lua-5.4.1
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of Lua
        uses: actions/upload-artifact@v2
        with:
          name: Lua_no_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/lua-5.4.1/3c_performance_stats/
          retention-days: 5

      - name: Build converted Lua
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/lua-5.4.1
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          sed -i "s/luac_main/main/" src/luac.c
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k CFLAGS="-w -ferror-limit=0" linux

  test_lua_no_expand_macros_alltypes_only_g_sol:
    name: Test Lua (not macro-expanded, -alltypes, greatest solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Lua
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol
          tar -xvzf ${{env.benchmark_tar_dir}}/lua-5.4.1.tar.gz
          cd lua-5.4.1
          bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" CFLAGS="-w -ferror-limit=0" linux
          ( cd src ; \
            clang-rename-10 -pl -i \
              --qualified-name=main \
              --new-name=luac_main \
              luac.c )

      - name: Convert Lua
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/lua-5.4.1
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of Lua
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/lua-5.4.1
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of Lua
        uses: actions/upload-artifact@v2
        with:
          name: Lua_no_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/lua-5.4.1/3c_performance_stats/
          retention-days: 5

      - name: Build converted Lua (filter bounds inference errors)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/lua-5.4.1
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          sed -i "s/luac_main/main/" src/luac.c
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k CFLAGS="-w -ferror-limit=0" linux 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py

  test_lua_no_expand_macros_alltypes_only_l_sol:
    name: Test Lua (not macro-expanded, -alltypes, least solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Lua
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol
          tar -xvzf ${{env.benchmark_tar_dir}}/lua-5.4.1.tar.gz
          cd lua-5.4.1
          bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" CFLAGS="-w -ferror-limit=0" linux
          ( cd src ; \
            clang-rename-10 -pl -i \
              --qualified-name=main \
              --new-name=luac_main \
              luac.c )

      - name: Convert Lua
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/lua-5.4.1
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of Lua
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/lua-5.4.1
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of Lua
        uses: actions/upload-artifact@v2
        with:
          name: Lua_no_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/lua-5.4.1/3c_performance_stats/
          retention-days: 5

      - name: Build converted Lua (filter bounds inference errors)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/lua-5.4.1
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          sed -i "s/luac_main/main/" src/luac.c
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k CFLAGS="-w -ferror-limit=0" linux 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py

  test_lua_no_expand_macros_alltypes_disable_rds:
    name: Test Lua (not macro-expanded, -alltypes, CCured solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Lua
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds
          tar -xvzf ${{env.benchmark_tar_dir}}/lua-5.4.1.tar.gz
          cd lua-5.4.1
          bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" CFLAGS="-w -ferror-limit=0" linux
          ( cd src ; \
            clang-rename-10 -pl -i \
              --qualified-name=main \
              --new-name=luac_main \
              luac.c )

      - name: Convert Lua
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/lua-5.4.1
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of Lua
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/lua-5.4.1
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of Lua
        uses: actions/upload-artifact@v2
        with:
          name: Lua_no_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/lua-5.4.1/3c_performance_stats/
          retention-days: 5

      - name: Build converted Lua (filter bounds inference errors)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/lua-5.4.1
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          sed -i "s/luac_main/main/" src/luac.c
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k CFLAGS="-w -ferror-limit=0" linux 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py

  test_lua_no_expand_macros_alltypes_disable_fnedgs:
    name: Test Lua (not macro-expanded, -alltypes, FuncRevEdges solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Lua
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs
          tar -xvzf ${{env.benchmark_tar_dir}}/lua-5.4.1.tar.gz
          cd lua-5.4.1
          bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" CFLAGS="-w -ferror-limit=0" linux
          ( cd src ; \
            clang-rename-10 -pl -i \
              --qualified-name=main \
              --new-name=luac_main \
              luac.c )

      - name: Convert Lua
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/lua-5.4.1
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of Lua
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/lua-5.4.1
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of Lua
        uses: actions/upload-artifact@v2
        with:
          name: Lua_no_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/lua-5.4.1/3c_performance_stats/
          retention-days: 5

      - name: Build converted Lua (filter bounds inference errors)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/lua-5.4.1
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          sed -i "s/luac_main/main/" src/luac.c
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k CFLAGS="-w -ferror-limit=0" linux 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py

  test_lua_no_expand_macros_alltypes:
    name: Test Lua (not macro-expanded, -alltypes)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Lua
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes
          tar -xvzf ${{env.benchmark_tar_dir}}/lua-5.4.1.tar.gz
          cd lua-5.4.1
          bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" CFLAGS="-w -ferror-limit=0" linux
          ( cd src ; \
            clang-rename-10 -pl -i \
              --qualified-name=main \
              --new-name=luac_main \
              luac.c )

      - name: Convert Lua
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/lua-5.4.1
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --project_path .

      - name: Copy 3c stats of Lua
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/lua-5.4.1
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of Lua
        uses: actions/upload-artifact@v2
        with:
          name: Lua_no_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/lua-5.4.1/3c_performance_stats/
          retention-days: 5

      - name: Build converted Lua (filter bounds inference errors)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/lua-5.4.1
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          sed -i "s/luac_main/main/" src/luac.c
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k CFLAGS="-w -ferror-limit=0" linux 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py

  test_lua_expand_macros_no_alltypes:
    name: Test Lua (macro-expanded, no -alltypes)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Lua
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes
          tar -xvzf ${{env.benchmark_tar_dir}}/lua-5.4.1.tar.gz
          cd lua-5.4.1
          bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" CFLAGS="-w -ferror-limit=0" linux
          ( cd src ; \
            clang-rename-10 -pl -i \
              --qualified-name=main \
              --new-name=luac_main \
              luac.c )

      - name: Convert Lua
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/lua-5.4.1
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of Lua
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/lua-5.4.1
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of Lua
        uses: actions/upload-artifact@v2
        with:
          name: Lua_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/lua-5.4.1/3c_performance_stats/
          retention-days: 5

      - name: Build converted Lua
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/lua-5.4.1
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          sed -i "s/luac_main/main/" src/luac.c
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k CFLAGS="-w -ferror-limit=0" linux

  test_lua_expand_macros_alltypes_only_g_sol:
    name: Test Lua (macro-expanded, -alltypes, greatest solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Lua
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol
          tar -xvzf ${{env.benchmark_tar_dir}}/lua-5.4.1.tar.gz
          cd lua-5.4.1
          bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" CFLAGS="-w -ferror-limit=0" linux
          ( cd src ; \
            clang-rename-10 -pl -i \
              --qualified-name=main \
              --new-name=luac_main \
              luac.c )

      - name: Convert Lua
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/lua-5.4.1
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of Lua
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/lua-5.4.1
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of Lua
        uses: actions/upload-artifact@v2
        with:
          name: Lua_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/lua-5.4.1/3c_performance_stats/
          retention-days: 5

      - name: Build converted Lua (filter bounds inference errors)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/lua-5.4.1
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          sed -i "s/luac_main/main/" src/luac.c
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k CFLAGS="-w -ferror-limit=0" linux 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py

  test_lua_expand_macros_alltypes_only_l_sol:
    name: Test Lua (macro-expanded, -alltypes, least solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Lua
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol
          tar -xvzf ${{env.benchmark_tar_dir}}/lua-5.4.1.tar.gz
          cd lua-5.4.1
          bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" CFLAGS="-w -ferror-limit=0" linux
          ( cd src ; \
            clang-rename-10 -pl -i \
              --qualified-name=main \
              --new-name=luac_main \
              luac.c )

      - name: Convert Lua
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/lua-5.4.1
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of Lua
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/lua-5.4.1
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of Lua
        uses: actions/upload-artifact@v2
        with:
          name: Lua_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/lua-5.4.1/3c_performance_stats/
          retention-days: 5

      - name: Build converted Lua (filter bounds inference errors)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/lua-5.4.1
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          sed -i "s/luac_main/main/" src/luac.c
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k CFLAGS="-w -ferror-limit=0" linux 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py

  test_lua_expand_macros_alltypes_disable_rds:
    name: Test Lua (macro-expanded, -alltypes, CCured solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Lua
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds
          tar -xvzf ${{env.benchmark_tar_dir}}/lua-5.4.1.tar.gz
          cd lua-5.4.1
          bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" CFLAGS="-w -ferror-limit=0" linux
          ( cd src ; \
            clang-rename-10 -pl -i \
              --qualified-name=main \
              --new-name=luac_main \
              luac.c )

      - name: Convert Lua
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/lua-5.4.1
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of Lua
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/lua-5.4.1
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of Lua
        uses: actions/upload-artifact@v2
        with:
          name: Lua_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/lua-5.4.1/3c_performance_stats/
          retention-days: 5

      - name: Build converted Lua (filter bounds inference errors)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/lua-5.4.1
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          sed -i "s/luac_main/main/" src/luac.c
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k CFLAGS="-w -ferror-limit=0" linux 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py

  test_lua_expand_macros_alltypes_disable_fnedgs:
    name: Test Lua (macro-expanded, -alltypes, FuncRevEdges solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Lua
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs
          tar -xvzf ${{env.benchmark_tar_dir}}/lua-5.4.1.tar.gz
          cd lua-5.4.1
          bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" CFLAGS="-w -ferror-limit=0" linux
          ( cd src ; \
            clang-rename-10 -pl -i \
              --qualified-name=main \
              --new-name=luac_main \
              luac.c )

      - name: Convert Lua
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/lua-5.4.1
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of Lua
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/lua-5.4.1
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of Lua
        uses: actions/upload-artifact@v2
        with:
          name: Lua_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/lua-5.4.1/3c_performance_stats/
          retention-days: 5

      - name: Build converted Lua (filter bounds inference errors)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/lua-5.4.1
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          sed -i "s/luac_main/main/" src/luac.c
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k CFLAGS="-w -ferror-limit=0" linux 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py

  test_lua_expand_macros_alltypes:
    name: Test Lua (macro-expanded, -alltypes)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Lua
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/expand_macros_alltypes
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes
          tar -xvzf ${{env.benchmark_tar_dir}}/lua-5.4.1.tar.gz
          cd lua-5.4.1
          bear make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" CFLAGS="-w -ferror-limit=0" linux
          ( cd src ; \
            clang-rename-10 -pl -i \
              --qualified-name=main \
              --new-name=luac_main \
              luac.c )

      - name: Convert Lua
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/lua-5.4.1
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of Lua
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/lua-5.4.1
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of Lua
        uses: actions/upload-artifact@v2
        with:
          name: Lua_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes/lua-5.4.1/3c_performance_stats/
          retention-days: 5

      - name: Build converted Lua (filter bounds inference errors)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/lua-5.4.1
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          sed -i "s/luac_main/main/" src/luac.c
          make -j $(nproc) -l $(nproc) --output-sync CC="${{env.builddir}}/bin/clang" -k CFLAGS="-w -ferror-limit=0" linux 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py

  test_libtiff_no_expand_macros_no_alltypes:
    name: Test LibTiff (not macro-expanded, no -alltypes)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build LibTiff
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes
          tar -xvzf ${{env.benchmark_tar_dir}}/tiff-4.1.0.tar.gz
          for i in ${{env.benchmark_tar_dir}}/tiff-4.1.0_patches/*; do patch -s -p0 < $i; done
          cd tiff-4.1.0
          cmake -DCMAKE_C_COMPILER=${{env.builddir}}/bin/clang -G Ninja -DCMAKE_C_FLAGS="-w -ferror-limit=0" .
          bear ninja -l $(nproc) tiff
          ( cd tools ; \
            for i in *.c ; do \
              clang-rename-10 -pl -i \
                --qualified-name=main \
                --new-name=$(basename -s .c $i)_main $i ; \
            done)

      - name: Convert LibTiff
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/tiff-4.1.0
          ${{env.port_tools}}/convert_project.py \
            --skip '/.*/tif_stream.cxx' \
            --skip '.*/test/.*\.c' \
            --skip '.*/contrib/.*\.c' \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --project_path .

      - name: Copy 3c stats of LibTiff
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/tiff-4.1.0
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of LibTiff
        uses: actions/upload-artifact@v2
        with:
          name: LibTiff_no_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/tiff-4.1.0/3c_performance_stats/
          retention-days: 5

      - name: Build converted LibTiff
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/tiff-4.1.0
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          ninja -l $(nproc) -k 0 tiff

  test_libtiff_no_expand_macros_alltypes_only_g_sol:
    name: Test LibTiff (not macro-expanded, -alltypes, greatest solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build LibTiff
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol
          tar -xvzf ${{env.benchmark_tar_dir}}/tiff-4.1.0.tar.gz
          for i in ${{env.benchmark_tar_dir}}/tiff-4.1.0_patches/*; do patch -s -p0 < $i; done
          cd tiff-4.1.0
          cmake -DCMAKE_C_COMPILER=${{env.builddir}}/bin/clang -G Ninja -DCMAKE_C_FLAGS="-w -ferror-limit=0" .
          bear ninja -l $(nproc) tiff
          ( cd tools ; \
            for i in *.c ; do \
              clang-rename-10 -pl -i \
                --qualified-name=main \
                --new-name=$(basename -s .c $i)_main $i ; \
            done)

      - name: Convert LibTiff
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/tiff-4.1.0
          ${{env.port_tools}}/convert_project.py \
            --skip '/.*/tif_stream.cxx' \
            --skip '.*/test/.*\.c' \
            --skip '.*/contrib/.*\.c' \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of LibTiff
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/tiff-4.1.0
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of LibTiff
        uses: actions/upload-artifact@v2
        with:
          name: LibTiff_no_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/tiff-4.1.0/3c_performance_stats/
          retention-days: 5

      - name: Build converted LibTiff (filter bounds inference errors)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/tiff-4.1.0
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          ninja -l $(nproc) -k 0 tiff 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py

  test_libtiff_no_expand_macros_alltypes_only_l_sol:
    name: Test LibTiff (not macro-expanded, -alltypes, least solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build LibTiff
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol
          tar -xvzf ${{env.benchmark_tar_dir}}/tiff-4.1.0.tar.gz
          for i in ${{env.benchmark_tar_dir}}/tiff-4.1.0_patches/*; do patch -s -p0 < $i; done
          cd tiff-4.1.0
          cmake -DCMAKE_C_COMPILER=${{env.builddir}}/bin/clang -G Ninja -DCMAKE_C_FLAGS="-w -ferror-limit=0" .
          bear ninja -l $(nproc) tiff
          ( cd tools ; \
            for i in *.c ; do \
              clang-rename-10 -pl -i \
                --qualified-name=main \
                --new-name=$(basename -s .c $i)_main $i ; \
            done)

      - name: Convert LibTiff
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/tiff-4.1.0
          ${{env.port_tools}}/convert_project.py \
            --skip '/.*/tif_stream.cxx' \
            --skip '.*/test/.*\.c' \
            --skip '.*/contrib/.*\.c' \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of LibTiff
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/tiff-4.1.0
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of LibTiff
        uses: actions/upload-artifact@v2
        with:
          name: LibTiff_no_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/tiff-4.1.0/3c_performance_stats/
          retention-days: 5

      - name: Build converted LibTiff (filter bounds inference errors)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/tiff-4.1.0
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          ninja -l $(nproc) -k 0 tiff 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py

  test_libtiff_no_expand_macros_alltypes_disable_rds:
    name: Test LibTiff (not macro-expanded, -alltypes, CCured solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build LibTiff
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds
          tar -xvzf ${{env.benchmark_tar_dir}}/tiff-4.1.0.tar.gz
          for i in ${{env.benchmark_tar_dir}}/tiff-4.1.0_patches/*; do patch -s -p0 < $i; done
          cd tiff-4.1.0
          cmake -DCMAKE_C_COMPILER=${{env.builddir}}/bin/clang -G Ninja -DCMAKE_C_FLAGS="-w -ferror-limit=0" .
          bear ninja -l $(nproc) tiff
          ( cd tools ; \
            for i in *.c ; do \
              clang-rename-10 -pl -i \
                --qualified-name=main \
                --new-name=$(basename -s .c $i)_main $i ; \
            done)

      - name: Convert LibTiff
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/tiff-4.1.0
          ${{env.port_tools}}/convert_project.py \
            --skip '/.*/tif_stream.cxx' \
            --skip '.*/test/.*\.c' \
            --skip '.*/contrib/.*\.c' \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of LibTiff
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/tiff-4.1.0
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of LibTiff
        uses: actions/upload-artifact@v2
        with:
          name: LibTiff_no_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/tiff-4.1.0/3c_performance_stats/
          retention-days: 5

      - name: Build converted LibTiff (filter bounds inference errors)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/tiff-4.1.0
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          ninja -l $(nproc) -k 0 tiff 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py

  test_libtiff_no_expand_macros_alltypes_disable_fnedgs:
    name: Test LibTiff (not macro-expanded, -alltypes, FuncRevEdges solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build LibTiff
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs
          tar -xvzf ${{env.benchmark_tar_dir}}/tiff-4.1.0.tar.gz
          for i in ${{env.benchmark_tar_dir}}/tiff-4.1.0_patches/*; do patch -s -p0 < $i; done
          cd tiff-4.1.0
          cmake -DCMAKE_C_COMPILER=${{env.builddir}}/bin/clang -G Ninja -DCMAKE_C_FLAGS="-w -ferror-limit=0" .
          bear ninja -l $(nproc) tiff
          ( cd tools ; \
            for i in *.c ; do \
              clang-rename-10 -pl -i \
                --qualified-name=main \
                --new-name=$(basename -s .c $i)_main $i ; \
            done)

      - name: Convert LibTiff
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/tiff-4.1.0
          ${{env.port_tools}}/convert_project.py \
            --skip '/.*/tif_stream.cxx' \
            --skip '.*/test/.*\.c' \
            --skip '.*/contrib/.*\.c' \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of LibTiff
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/tiff-4.1.0
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of LibTiff
        uses: actions/upload-artifact@v2
        with:
          name: LibTiff_no_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/tiff-4.1.0/3c_performance_stats/
          retention-days: 5

      - name: Build converted LibTiff (filter bounds inference errors)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/tiff-4.1.0
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          ninja -l $(nproc) -k 0 tiff 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py

  test_libtiff_no_expand_macros_alltypes:
    name: Test LibTiff (not macro-expanded, -alltypes)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build LibTiff
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes
          tar -xvzf ${{env.benchmark_tar_dir}}/tiff-4.1.0.tar.gz
          for i in ${{env.benchmark_tar_dir}}/tiff-4.1.0_patches/*; do patch -s -p0 < $i; done
          cd tiff-4.1.0
          cmake -DCMAKE_C_COMPILER=${{env.builddir}}/bin/clang -G Ninja -DCMAKE_C_FLAGS="-w -ferror-limit=0" .
          bear ninja -l $(nproc) tiff
          ( cd tools ; \
            for i in *.c ; do \
              clang-rename-10 -pl -i \
                --qualified-name=main \
                --new-name=$(basename -s .c $i)_main $i ; \
            done)

      - name: Convert LibTiff
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/tiff-4.1.0
          ${{env.port_tools}}/convert_project.py \
            --skip '/.*/tif_stream.cxx' \
            --skip '.*/test/.*\.c' \
            --skip '.*/contrib/.*\.c' \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --project_path .

      - name: Copy 3c stats of LibTiff
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/tiff-4.1.0
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of LibTiff
        uses: actions/upload-artifact@v2
        with:
          name: LibTiff_no_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/tiff-4.1.0/3c_performance_stats/
          retention-days: 5

      - name: Build converted LibTiff (filter bounds inference errors)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/tiff-4.1.0
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          ninja -l $(nproc) -k 0 tiff 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py

  test_libtiff_expand_macros_no_alltypes:
    name: Test LibTiff (macro-expanded, no -alltypes)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build LibTiff
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes
          tar -xvzf ${{env.benchmark_tar_dir}}/tiff-4.1.0.tar.gz
          for i in ${{env.benchmark_tar_dir}}/tiff-4.1.0_patches/*; do patch -s -p0 < $i; done
          cd tiff-4.1.0
          cmake -DCMAKE_C_COMPILER=${{env.builddir}}/bin/clang -G Ninja -DCMAKE_C_FLAGS="-w -ferror-limit=0" .
          bear ninja -l $(nproc) tiff
          ( cd tools ; \
            for i in *.c ; do \
              clang-rename-10 -pl -i \
                --qualified-name=main \
                --new-name=$(basename -s .c $i)_main $i ; \
            done)

      - name: Convert LibTiff
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/tiff-4.1.0
          ${{env.port_tools}}/convert_project.py \
            --skip '/.*/tif_stream.cxx' \
            --skip '.*/test/.*\.c' \
            --skip '.*/contrib/.*\.c' \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of LibTiff
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/tiff-4.1.0
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of LibTiff
        uses: actions/upload-artifact@v2
        with:
          name: LibTiff_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/tiff-4.1.0/3c_performance_stats/
          retention-days: 5

      - name: Build converted LibTiff
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/tiff-4.1.0
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          ninja -l $(nproc) -k 0 tiff

  test_libtiff_expand_macros_alltypes_only_g_sol:
    name: Test LibTiff (macro-expanded, -alltypes, greatest solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build LibTiff
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol
          tar -xvzf ${{env.benchmark_tar_dir}}/tiff-4.1.0.tar.gz
          for i in ${{env.benchmark_tar_dir}}/tiff-4.1.0_patches/*; do patch -s -p0 < $i; done
          cd tiff-4.1.0
          cmake -DCMAKE_C_COMPILER=${{env.builddir}}/bin/clang -G Ninja -DCMAKE_C_FLAGS="-w -ferror-limit=0" .
          bear ninja -l $(nproc) tiff
          ( cd tools ; \
            for i in *.c ; do \
              clang-rename-10 -pl -i \
                --qualified-name=main \
                --new-name=$(basename -s .c $i)_main $i ; \
            done)

      - name: Convert LibTiff
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/tiff-4.1.0
          ${{env.port_tools}}/convert_project.py \
            --skip '/.*/tif_stream.cxx' \
            --skip '.*/test/.*\.c' \
            --skip '.*/contrib/.*\.c' \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of LibTiff
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/tiff-4.1.0
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of LibTiff
        uses: actions/upload-artifact@v2
        with:
          name: LibTiff_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/tiff-4.1.0/3c_performance_stats/
          retention-days: 5

      - name: Build converted LibTiff (filter bounds inference errors)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/tiff-4.1.0
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          ninja -l $(nproc) -k 0 tiff 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py

  test_libtiff_expand_macros_alltypes_only_l_sol:
    name: Test LibTiff (macro-expanded, -alltypes, least solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build LibTiff
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol
          tar -xvzf ${{env.benchmark_tar_dir}}/tiff-4.1.0.tar.gz
          for i in ${{env.benchmark_tar_dir}}/tiff-4.1.0_patches/*; do patch -s -p0 < $i; done
          cd tiff-4.1.0
          cmake -DCMAKE_C_COMPILER=${{env.builddir}}/bin/clang -G Ninja -DCMAKE_C_FLAGS="-w -ferror-limit=0" .
          bear ninja -l $(nproc) tiff
          ( cd tools ; \
            for i in *.c ; do \
              clang-rename-10 -pl -i \
                --qualified-name=main \
                --new-name=$(basename -s .c $i)_main $i ; \
            done)

      - name: Convert LibTiff
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/tiff-4.1.0
          ${{env.port_tools}}/convert_project.py \
            --skip '/.*/tif_stream.cxx' \
            --skip '.*/test/.*\.c' \
            --skip '.*/contrib/.*\.c' \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of LibTiff
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/tiff-4.1.0
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of LibTiff
        uses: actions/upload-artifact@v2
        with:
          name: LibTiff_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/tiff-4.1.0/3c_performance_stats/
          retention-days: 5

      - name: Build converted LibTiff (filter bounds inference errors)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/tiff-4.1.0
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          ninja -l $(nproc) -k 0 tiff 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py

  test_libtiff_expand_macros_alltypes_disable_rds:
    name: Test LibTiff (macro-expanded, -alltypes, CCured solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build LibTiff
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds
          tar -xvzf ${{env.benchmark_tar_dir}}/tiff-4.1.0.tar.gz
          for i in ${{env.benchmark_tar_dir}}/tiff-4.1.0_patches/*; do patch -s -p0 < $i; done
          cd tiff-4.1.0
          cmake -DCMAKE_C_COMPILER=${{env.builddir}}/bin/clang -G Ninja -DCMAKE_C_FLAGS="-w -ferror-limit=0" .
          bear ninja -l $(nproc) tiff
          ( cd tools ; \
            for i in *.c ; do \
              clang-rename-10 -pl -i \
                --qualified-name=main \
                --new-name=$(basename -s .c $i)_main $i ; \
            done)

      - name: Convert LibTiff
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/tiff-4.1.0
          ${{env.port_tools}}/convert_project.py \
            --skip '/.*/tif_stream.cxx' \
            --skip '.*/test/.*\.c' \
            --skip '.*/contrib/.*\.c' \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of LibTiff
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/tiff-4.1.0
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of LibTiff
        uses: actions/upload-artifact@v2
        with:
          name: LibTiff_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/tiff-4.1.0/3c_performance_stats/
          retention-days: 5

      - name: Build converted LibTiff (filter bounds inference errors)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/tiff-4.1.0
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          ninja -l $(nproc) -k 0 tiff 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py

  test_libtiff_expand_macros_alltypes_disable_fnedgs:
    name: Test LibTiff (macro-expanded, -alltypes, FuncRevEdges solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build LibTiff
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs
          tar -xvzf ${{env.benchmark_tar_dir}}/tiff-4.1.0.tar.gz
          for i in ${{env.benchmark_tar_dir}}/tiff-4.1.0_patches/*; do patch -s -p0 < $i; done
          cd tiff-4.1.0
          cmake -DCMAKE_C_COMPILER=${{env.builddir}}/bin/clang -G Ninja -DCMAKE_C_FLAGS="-w -ferror-limit=0" .
          bear ninja -l $(nproc) tiff
          ( cd tools ; \
            for i in *.c ; do \
              clang-rename-10 -pl -i \
                --qualified-name=main \
                --new-name=$(basename -s .c $i)_main $i ; \
            done)

      - name: Convert LibTiff
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/tiff-4.1.0
          ${{env.port_tools}}/convert_project.py \
            --skip '/.*/tif_stream.cxx' \
            --skip '.*/test/.*\.c' \
            --skip '.*/contrib/.*\.c' \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of LibTiff
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/tiff-4.1.0
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of LibTiff
        uses: actions/upload-artifact@v2
        with:
          name: LibTiff_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/tiff-4.1.0/3c_performance_stats/
          retention-days: 5

      - name: Build converted LibTiff (filter bounds inference errors)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/tiff-4.1.0
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          ninja -l $(nproc) -k 0 tiff 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py

  test_libtiff_expand_macros_alltypes:
    name: Test LibTiff (macro-expanded, -alltypes)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build LibTiff
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/expand_macros_alltypes
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes
          tar -xvzf ${{env.benchmark_tar_dir}}/tiff-4.1.0.tar.gz
          for i in ${{env.benchmark_tar_dir}}/tiff-4.1.0_patches/*; do patch -s -p0 < $i; done
          cd tiff-4.1.0
          cmake -DCMAKE_C_COMPILER=${{env.builddir}}/bin/clang -G Ninja -DCMAKE_C_FLAGS="-w -ferror-limit=0" .
          bear ninja -l $(nproc) tiff
          ( cd tools ; \
            for i in *.c ; do \
              clang-rename-10 -pl -i \
                --qualified-name=main \
                --new-name=$(basename -s .c $i)_main $i ; \
            done)

      - name: Convert LibTiff
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/tiff-4.1.0
          ${{env.port_tools}}/convert_project.py \
            --skip '/.*/tif_stream.cxx' \
            --skip '.*/test/.*\.c' \
            --skip '.*/contrib/.*\.c' \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of LibTiff
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/tiff-4.1.0
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of LibTiff
        uses: actions/upload-artifact@v2
        with:
          name: LibTiff_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes/tiff-4.1.0/3c_performance_stats/
          retention-days: 5

      - name: Build converted LibTiff (filter bounds inference errors)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/tiff-4.1.0
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          ninja -l $(nproc) -k 0 tiff 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py

  test_zlib_no_expand_macros_no_alltypes:
    name: Test ZLib (not macro-expanded, no -alltypes)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build ZLib
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes
          tar -xvzf ${{env.benchmark_tar_dir}}/zlib-1.2.11.tar.gz
          cd zlib-1.2.11
          mkdir build
          cd build
          cmake -DCMAKE_C_COMPILER=${{env.builddir}}/bin/clang -G Ninja -DCMAKE_C_FLAGS="-w -ferror-limit=0" ..
          bear ninja -l $(nproc) zlib

      - name: Convert ZLib
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/zlib-1.2.11
          ${{env.port_tools}}/convert_project.py \
            --skip '/.*/test/.*' \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --project_path . \
            --build_dir build

      - name: Copy 3c stats of ZLib
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/zlib-1.2.11
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of ZLib
        uses: actions/upload-artifact@v2
        with:
          name: ZLib_no_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/zlib-1.2.11/3c_performance_stats/
          retention-days: 5

      - name: Build converted ZLib
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/zlib-1.2.11
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          cd build
          ninja -l $(nproc) -k 0 zlib

  test_zlib_no_expand_macros_alltypes_only_g_sol:
    name: Test ZLib (not macro-expanded, -alltypes, greatest solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build ZLib
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol
          tar -xvzf ${{env.benchmark_tar_dir}}/zlib-1.2.11.tar.gz
          cd zlib-1.2.11
          mkdir build
          cd build
          cmake -DCMAKE_C_COMPILER=${{env.builddir}}/bin/clang -G Ninja -DCMAKE_C_FLAGS="-w -ferror-limit=0" ..
          bear ninja -l $(nproc) zlib

      - name: Convert ZLib
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/zlib-1.2.11
          ${{env.port_tools}}/convert_project.py \
            --skip '/.*/test/.*' \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-g-sol \
            --project_path . \
            --build_dir build

      - name: Copy 3c stats of ZLib
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/zlib-1.2.11
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of ZLib
        uses: actions/upload-artifact@v2
        with:
          name: ZLib_no_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/zlib-1.2.11/3c_performance_stats/
          retention-days: 5

      - name: Build converted ZLib (filter bounds inference errors)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/zlib-1.2.11
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          cd build
          ninja -l $(nproc) -k 0 zlib 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py

  test_zlib_no_expand_macros_alltypes_only_l_sol:
    name: Test ZLib (not macro-expanded, -alltypes, least solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build ZLib
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol
          tar -xvzf ${{env.benchmark_tar_dir}}/zlib-1.2.11.tar.gz
          cd zlib-1.2.11
          mkdir build
          cd build
          cmake -DCMAKE_C_COMPILER=${{env.builddir}}/bin/clang -G Ninja -DCMAKE_C_FLAGS="-w -ferror-limit=0" ..
          bear ninja -l $(nproc) zlib

      - name: Convert ZLib
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/zlib-1.2.11
          ${{env.port_tools}}/convert_project.py \
            --skip '/.*/test/.*' \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-l-sol \
            --project_path . \
            --build_dir build

      - name: Copy 3c stats of ZLib
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/zlib-1.2.11
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of ZLib
        uses: actions/upload-artifact@v2
        with:
          name: ZLib_no_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/zlib-1.2.11/3c_performance_stats/
          retention-days: 5

      - name: Build converted ZLib (filter bounds inference errors)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/zlib-1.2.11
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          cd build
          ninja -l $(nproc) -k 0 zlib 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py

  test_zlib_no_expand_macros_alltypes_disable_rds:
    name: Test ZLib (not macro-expanded, -alltypes, CCured solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build ZLib
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds
          tar -xvzf ${{env.benchmark_tar_dir}}/zlib-1.2.11.tar.gz
          cd zlib-1.2.11
          mkdir build
          cd build
          cmake -DCMAKE_C_COMPILER=${{env.builddir}}/bin/clang -G Ninja -DCMAKE_C_FLAGS="-w -ferror-limit=0" ..
          bear ninja -l $(nproc) zlib

      - name: Convert ZLib
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/zlib-1.2.11
          ${{env.port_tools}}/convert_project.py \
            --skip '/.*/test/.*' \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-rds \
            --project_path . \
            --build_dir build

      - name: Copy 3c stats of ZLib
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/zlib-1.2.11
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of ZLib
        uses: actions/upload-artifact@v2
        with:
          name: ZLib_no_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/zlib-1.2.11/3c_performance_stats/
          retention-days: 5

      - name: Build converted ZLib (filter bounds inference errors)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/zlib-1.2.11
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          cd build
          ninja -l $(nproc) -k 0 zlib 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py

  test_zlib_no_expand_macros_alltypes_disable_fnedgs:
    name: Test ZLib (not macro-expanded, -alltypes, FuncRevEdges solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build ZLib
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs
          tar -xvzf ${{env.benchmark_tar_dir}}/zlib-1.2.11.tar.gz
          cd zlib-1.2.11
          mkdir build
          cd build
          cmake -DCMAKE_C_COMPILER=${{env.builddir}}/bin/clang -G Ninja -DCMAKE_C_FLAGS="-w -ferror-limit=0" ..
          bear ninja -l $(nproc) zlib

      - name: Convert ZLib
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/zlib-1.2.11
          ${{env.port_tools}}/convert_project.py \
            --skip '/.*/test/.*' \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-fnedgs \
            --project_path . \
            --build_dir build

      - name: Copy 3c stats of ZLib
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/zlib-1.2.11
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of ZLib
        uses: actions/upload-artifact@v2
        with:
          name: ZLib_no_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/zlib-1.2.11/3c_performance_stats/
          retention-days: 5

      - name: Build converted ZLib (filter bounds inference errors)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/zlib-1.2.11
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          cd build
          ninja -l $(nproc) -k 0 zlib 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py

  test_zlib_no_expand_macros_alltypes:
    name: Test ZLib (not macro-expanded, -alltypes)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build ZLib
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes
          tar -xvzf ${{env.benchmark_tar_dir}}/zlib-1.2.11.tar.gz
          cd zlib-1.2.11
          mkdir build
          cd build
          cmake -DCMAKE_C_COMPILER=${{env.builddir}}/bin/clang -G Ninja -DCMAKE_C_FLAGS="-w -ferror-limit=0" ..
          bear ninja -l $(nproc) zlib

      - name: Convert ZLib
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/zlib-1.2.11
          ${{env.port_tools}}/convert_project.py \
            --skip '/.*/test/.*' \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --project_path . \
            --build_dir build

      - name: Copy 3c stats of ZLib
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/zlib-1.2.11
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of ZLib
        uses: actions/upload-artifact@v2
        with:
          name: ZLib_no_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/zlib-1.2.11/3c_performance_stats/
          retention-days: 5

      - name: Build converted ZLib (filter bounds inference errors)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/zlib-1.2.11
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          cd build
          ninja -l $(nproc) -k 0 zlib 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py

  test_zlib_expand_macros_no_alltypes:
    name: Test ZLib (macro-expanded, no -alltypes)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build ZLib
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes
          tar -xvzf ${{env.benchmark_tar_dir}}/zlib-1.2.11.tar.gz
          cd zlib-1.2.11
          mkdir build
          cd build
          cmake -DCMAKE_C_COMPILER=${{env.builddir}}/bin/clang -G Ninja -DCMAKE_C_FLAGS="-w -ferror-limit=0" ..
          bear ninja -l $(nproc) zlib

      - name: Convert ZLib
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/zlib-1.2.11
          ${{env.port_tools}}/convert_project.py \
            --skip '/.*/test/.*' \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --expand_macros_before_conversion \
            --project_path . \
            --build_dir build

      - name: Copy 3c stats of ZLib
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/zlib-1.2.11
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of ZLib
        uses: actions/upload-artifact@v2
        with:
          name: ZLib_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/zlib-1.2.11/3c_performance_stats/
          retention-days: 5

      - name: Build converted ZLib
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/zlib-1.2.11
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          cd build
          ninja -l $(nproc) -k 0 zlib

  test_zlib_expand_macros_alltypes_only_g_sol:
    name: Test ZLib (macro-expanded, -alltypes, greatest solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build ZLib
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol
          tar -xvzf ${{env.benchmark_tar_dir}}/zlib-1.2.11.tar.gz
          cd zlib-1.2.11
          mkdir build
          cd build
          cmake -DCMAKE_C_COMPILER=${{env.builddir}}/bin/clang -G Ninja -DCMAKE_C_FLAGS="-w -ferror-limit=0" ..
          bear ninja -l $(nproc) zlib

      - name: Convert ZLib
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/zlib-1.2.11
          ${{env.port_tools}}/convert_project.py \
            --skip '/.*/test/.*' \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-g-sol \
            --project_path . \
            --build_dir build

      - name: Copy 3c stats of ZLib
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/zlib-1.2.11
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of ZLib
        uses: actions/upload-artifact@v2
        with:
          name: ZLib_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/zlib-1.2.11/3c_performance_stats/
          retention-days: 5

      - name: Build converted ZLib (filter bounds inference errors)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/zlib-1.2.11
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          cd build
          ninja -l $(nproc) -k 0 zlib 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py

  test_zlib_expand_macros_alltypes_only_l_sol:
    name: Test ZLib (macro-expanded, -alltypes, least solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build ZLib
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol
          tar -xvzf ${{env.benchmark_tar_dir}}/zlib-1.2.11.tar.gz
          cd zlib-1.2.11
          mkdir build
          cd build
          cmake -DCMAKE_C_COMPILER=${{env.builddir}}/bin/clang -G Ninja -DCMAKE_C_FLAGS="-w -ferror-limit=0" ..
          bear ninja -l $(nproc) zlib

      - name: Convert ZLib
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/zlib-1.2.11
          ${{env.port_tools}}/convert_project.py \
            --skip '/.*/test/.*' \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-l-sol \
            --project_path . \
            --build_dir build

      - name: Copy 3c stats of ZLib
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/zlib-1.2.11
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of ZLib
        uses: actions/upload-artifact@v2
        with:
          name: ZLib_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/zlib-1.2.11/3c_performance_stats/
          retention-days: 5

      - name: Build converted ZLib (filter bounds inference errors)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/zlib-1.2.11
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          cd build
          ninja -l $(nproc) -k 0 zlib 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py

  test_zlib_expand_macros_alltypes_disable_rds:
    name: Test ZLib (macro-expanded, -alltypes, CCured solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build ZLib
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds
          tar -xvzf ${{env.benchmark_tar_dir}}/zlib-1.2.11.tar.gz
          cd zlib-1.2.11
          mkdir build
          cd build
          cmake -DCMAKE_C_COMPILER=${{env.builddir}}/bin/clang -G Ninja -DCMAKE_C_FLAGS="-w -ferror-limit=0" ..
          bear ninja -l $(nproc) zlib

      - name: Convert ZLib
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/zlib-1.2.11
          ${{env.port_tools}}/convert_project.py \
            --skip '/.*/test/.*' \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-rds \
            --project_path . \
            --build_dir build

      - name: Copy 3c stats of ZLib
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/zlib-1.2.11
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of ZLib
        uses: actions/upload-artifact@v2
        with:
          name: ZLib_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/zlib-1.2.11/3c_performance_stats/
          retention-days: 5

      - name: Build converted ZLib (filter bounds inference errors)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/zlib-1.2.11
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          cd build
          ninja -l $(nproc) -k 0 zlib 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py

  test_zlib_expand_macros_alltypes_disable_fnedgs:
    name: Test ZLib (macro-expanded, -alltypes, FuncRevEdges solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build ZLib
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs
          tar -xvzf ${{env.benchmark_tar_dir}}/zlib-1.2.11.tar.gz
          cd zlib-1.2.11
          mkdir build
          cd build
          cmake -DCMAKE_C_COMPILER=${{env.builddir}}/bin/clang -G Ninja -DCMAKE_C_FLAGS="-w -ferror-limit=0" ..
          bear ninja -l $(nproc) zlib

      - name: Convert ZLib
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/zlib-1.2.11
          ${{env.port_tools}}/convert_project.py \
            --skip '/.*/test/.*' \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-fnedgs \
            --project_path . \
            --build_dir build

      - name: Copy 3c stats of ZLib
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/zlib-1.2.11
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of ZLib
        uses: actions/upload-artifact@v2
        with:
          name: ZLib_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/zlib-1.2.11/3c_performance_stats/
          retention-days: 5

      - name: Build converted ZLib (filter bounds inference errors)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/zlib-1.2.11
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          cd build
          ninja -l $(nproc) -k 0 zlib 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py

  test_zlib_expand_macros_alltypes:
    name: Test ZLib (macro-expanded, -alltypes)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build ZLib
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/expand_macros_alltypes
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes
          tar -xvzf ${{env.benchmark_tar_dir}}/zlib-1.2.11.tar.gz
          cd zlib-1.2.11
          mkdir build
          cd build
          cmake -DCMAKE_C_COMPILER=${{env.builddir}}/bin/clang -G Ninja -DCMAKE_C_FLAGS="-w -ferror-limit=0" ..
          bear ninja -l $(nproc) zlib

      - name: Convert ZLib
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/zlib-1.2.11
          ${{env.port_tools}}/convert_project.py \
            --skip '/.*/test/.*' \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --project_path . \
            --build_dir build

      - name: Copy 3c stats of ZLib
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/zlib-1.2.11
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of ZLib
        uses: actions/upload-artifact@v2
        with:
          name: ZLib_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes/zlib-1.2.11/3c_performance_stats/
          retention-days: 5

      - name: Build converted ZLib (filter bounds inference errors)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/zlib-1.2.11
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          cd build
          ninja -l $(nproc) -k 0 zlib 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py

  test_icecast_no_expand_macros_no_alltypes:
    name: Test Icecast (not macro-expanded, no -alltypes)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Icecast
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes
          tar -xvzf ${{env.benchmark_tar_dir}}/icecast-2.4.4.tar.gz
          cd icecast-2.4.4
          sed -i '/_GNU_SOURCE/d' configure
          CC="${{env.builddir}}/bin/clang" CFLAGS="-w -ferror-limit=0" ./configure
          bear make -j $(nproc) -l $(nproc) --output-sync

      - name: Convert Icecast
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/icecast-2.4.4
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --project_path .

      - name: Copy 3c stats of Icecast
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/icecast-2.4.4
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of Icecast
        uses: actions/upload-artifact@v2
        with:
          name: Icecast_no_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/icecast-2.4.4/3c_performance_stats/
          retention-days: 5

      - name: Build converted Icecast
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/icecast-2.4.4
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync -k

  test_icecast_no_expand_macros_alltypes_only_g_sol:
    name: Test Icecast (not macro-expanded, -alltypes, greatest solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Icecast
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol
          tar -xvzf ${{env.benchmark_tar_dir}}/icecast-2.4.4.tar.gz
          cd icecast-2.4.4
          sed -i '/_GNU_SOURCE/d' configure
          CC="${{env.builddir}}/bin/clang" CFLAGS="-w -ferror-limit=0" ./configure
          bear make -j $(nproc) -l $(nproc) --output-sync

      - name: Convert Icecast
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/icecast-2.4.4
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of Icecast
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/icecast-2.4.4
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of Icecast
        uses: actions/upload-artifact@v2
        with:
          name: Icecast_no_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/icecast-2.4.4/3c_performance_stats/
          retention-days: 5

      - name: Build converted Icecast (filter bounds inference errors)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/icecast-2.4.4
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync -k 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py

  test_icecast_no_expand_macros_alltypes_only_l_sol:
    name: Test Icecast (not macro-expanded, -alltypes, least solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Icecast
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol
          tar -xvzf ${{env.benchmark_tar_dir}}/icecast-2.4.4.tar.gz
          cd icecast-2.4.4
          sed -i '/_GNU_SOURCE/d' configure
          CC="${{env.builddir}}/bin/clang" CFLAGS="-w -ferror-limit=0" ./configure
          bear make -j $(nproc) -l $(nproc) --output-sync

      - name: Convert Icecast
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/icecast-2.4.4
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of Icecast
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/icecast-2.4.4
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of Icecast
        uses: actions/upload-artifact@v2
        with:
          name: Icecast_no_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/icecast-2.4.4/3c_performance_stats/
          retention-days: 5

      - name: Build converted Icecast (filter bounds inference errors)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/icecast-2.4.4
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync -k 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py

  test_icecast_no_expand_macros_alltypes_disable_rds:
    name: Test Icecast (not macro-expanded, -alltypes, CCured solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Icecast
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds
          tar -xvzf ${{env.benchmark_tar_dir}}/icecast-2.4.4.tar.gz
          cd icecast-2.4.4
          sed -i '/_GNU_SOURCE/d' configure
          CC="${{env.builddir}}/bin/clang" CFLAGS="-w -ferror-limit=0" ./configure
          bear make -j $(nproc) -l $(nproc) --output-sync

      - name: Convert Icecast
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/icecast-2.4.4
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of Icecast
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/icecast-2.4.4
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of Icecast
        uses: actions/upload-artifact@v2
        with:
          name: Icecast_no_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/icecast-2.4.4/3c_performance_stats/
          retention-days: 5

      - name: Build converted Icecast (filter bounds inference errors)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/icecast-2.4.4
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync -k 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py

  test_icecast_no_expand_macros_alltypes_disable_fnedgs:
    name: Test Icecast (not macro-expanded, -alltypes, FuncRevEdges solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Icecast
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs
          tar -xvzf ${{env.benchmark_tar_dir}}/icecast-2.4.4.tar.gz
          cd icecast-2.4.4
          sed -i '/_GNU_SOURCE/d' configure
          CC="${{env.builddir}}/bin/clang" CFLAGS="-w -ferror-limit=0" ./configure
          bear make -j $(nproc) -l $(nproc) --output-sync

      - name: Convert Icecast
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/icecast-2.4.4
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of Icecast
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/icecast-2.4.4
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of Icecast
        uses: actions/upload-artifact@v2
        with:
          name: Icecast_no_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/icecast-2.4.4/3c_performance_stats/
          retention-days: 5

      - name: Build converted Icecast (filter bounds inference errors)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/icecast-2.4.4
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync -k 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py

  test_icecast_no_expand_macros_alltypes:
    name: Test Icecast (not macro-expanded, -alltypes)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Icecast
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes
          tar -xvzf ${{env.benchmark_tar_dir}}/icecast-2.4.4.tar.gz
          cd icecast-2.4.4
          sed -i '/_GNU_SOURCE/d' configure
          CC="${{env.builddir}}/bin/clang" CFLAGS="-w -ferror-limit=0" ./configure
          bear make -j $(nproc) -l $(nproc) --output-sync

      - name: Convert Icecast
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/icecast-2.4.4
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --project_path .

      - name: Copy 3c stats of Icecast
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/icecast-2.4.4
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of Icecast
        uses: actions/upload-artifact@v2
        with:
          name: Icecast_no_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/icecast-2.4.4/3c_performance_stats/
          retention-days: 5

      - name: Build converted Icecast (filter bounds inference errors)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/icecast-2.4.4
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync -k 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py

  test_icecast_expand_macros_no_alltypes:
    name: Test Icecast (macro-expanded, no -alltypes)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Icecast
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes
          tar -xvzf ${{env.benchmark_tar_dir}}/icecast-2.4.4.tar.gz
          cd icecast-2.4.4
          sed -i '/_GNU_SOURCE/d' configure
          CC="${{env.builddir}}/bin/clang" CFLAGS="-w -ferror-limit=0" ./configure
          bear make -j $(nproc) -l $(nproc) --output-sync

      - name: Convert Icecast
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/icecast-2.4.4
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of Icecast
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/icecast-2.4.4
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of Icecast
        uses: actions/upload-artifact@v2
        with:
          name: Icecast_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/icecast-2.4.4/3c_performance_stats/
          retention-days: 5

      - name: Build converted Icecast
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/icecast-2.4.4
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync -k

  test_icecast_expand_macros_alltypes_only_g_sol:
    name: Test Icecast (macro-expanded, -alltypes, greatest solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Icecast
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol
          tar -xvzf ${{env.benchmark_tar_dir}}/icecast-2.4.4.tar.gz
          cd icecast-2.4.4
          sed -i '/_GNU_SOURCE/d' configure
          CC="${{env.builddir}}/bin/clang" CFLAGS="-w -ferror-limit=0" ./configure
          bear make -j $(nproc) -l $(nproc) --output-sync

      - name: Convert Icecast
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/icecast-2.4.4
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of Icecast
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/icecast-2.4.4
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of Icecast
        uses: actions/upload-artifact@v2
        with:
          name: Icecast_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/icecast-2.4.4/3c_performance_stats/
          retention-days: 5

      - name: Build converted Icecast (filter bounds inference errors)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/icecast-2.4.4
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync -k 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py

  test_icecast_expand_macros_alltypes_only_l_sol:
    name: Test Icecast (macro-expanded, -alltypes, least solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Icecast
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol
          tar -xvzf ${{env.benchmark_tar_dir}}/icecast-2.4.4.tar.gz
          cd icecast-2.4.4
          sed -i '/_GNU_SOURCE/d' configure
          CC="${{env.builddir}}/bin/clang" CFLAGS="-w -ferror-limit=0" ./configure
          bear make -j $(nproc) -l $(nproc) --output-sync

      - name: Convert Icecast
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/icecast-2.4.4
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of Icecast
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/icecast-2.4.4
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of Icecast
        uses: actions/upload-artifact@v2
        with:
          name: Icecast_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/icecast-2.4.4/3c_performance_stats/
          retention-days: 5

      - name: Build converted Icecast (filter bounds inference errors)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/icecast-2.4.4
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync -k 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py

  test_icecast_expand_macros_alltypes_disable_rds:
    name: Test Icecast (macro-expanded, -alltypes, CCured solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Icecast
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds
          tar -xvzf ${{env.benchmark_tar_dir}}/icecast-2.4.4.tar.gz
          cd icecast-2.4.4
          sed -i '/_GNU_SOURCE/d' configure
          CC="${{env.builddir}}/bin/clang" CFLAGS="-w -ferror-limit=0" ./configure
          bear make -j $(nproc) -l $(nproc) --output-sync

      - name: Convert Icecast
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/icecast-2.4.4
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of Icecast
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/icecast-2.4.4
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of Icecast
        uses: actions/upload-artifact@v2
        with:
          name: Icecast_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/icecast-2.4.4/3c_performance_stats/
          retention-days: 5

      - name: Build converted Icecast (filter bounds inference errors)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/icecast-2.4.4
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync -k 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py

  test_icecast_expand_macros_alltypes_disable_fnedgs:
    name: Test Icecast (macro-expanded, -alltypes, FuncRevEdges solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Icecast
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs
          tar -xvzf ${{env.benchmark_tar_dir}}/icecast-2.4.4.tar.gz
          cd icecast-2.4.4
          sed -i '/_GNU_SOURCE/d' configure
          CC="${{env.builddir}}/bin/clang" CFLAGS="-w -ferror-limit=0" ./configure
          bear make -j $(nproc) -l $(nproc) --output-sync

      - name: Convert Icecast
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/icecast-2.4.4
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of Icecast
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/icecast-2.4.4
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of Icecast
        uses: actions/upload-artifact@v2
        with:
          name: Icecast_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/icecast-2.4.4/3c_performance_stats/
          retention-days: 5

      - name: Build converted Icecast (filter bounds inference errors)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/icecast-2.4.4
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync -k 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py

  test_icecast_expand_macros_alltypes:
    name: Test Icecast (macro-expanded, -alltypes)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Icecast
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/expand_macros_alltypes
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes
          tar -xvzf ${{env.benchmark_tar_dir}}/icecast-2.4.4.tar.gz
          cd icecast-2.4.4
          sed -i '/_GNU_SOURCE/d' configure
          CC="${{env.builddir}}/bin/clang" CFLAGS="-w -ferror-limit=0" ./configure
          bear make -j $(nproc) -l $(nproc) --output-sync

      - name: Convert Icecast
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/icecast-2.4.4
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of Icecast
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/icecast-2.4.4
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of Icecast
        uses: actions/upload-artifact@v2
        with:
          name: Icecast_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes/icecast-2.4.4/3c_performance_stats/
          retention-days: 5

      - name: Build converted Icecast (filter bounds inference errors)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/icecast-2.4.4
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make -j $(nproc) -l $(nproc) --output-sync -k 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py

  test_thttpd_no_expand_macros_no_alltypes:
    name: Test Thttpd (not macro-expanded, no -alltypes)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Thttpd
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes
          tar -xvzf ${{env.benchmark_tar_dir}}/thttpd-2.29.tar.gz
          for i in ${{env.benchmark_tar_dir}}/thttpd-2.29_patches/*; do patch -s -p0 < $i; done
          cd thttpd-2.29
          CC="${{env.builddir}}/bin/clang" ./configure
          chmod -R 777 *
          bear make CC="${{env.builddir}}/bin/clang -w -ferror-limit=0"

      - name: Convert Thttpd
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/thttpd-2.29
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --project_path .

      - name: Copy 3c stats of Thttpd
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/thttpd-2.29
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of Thttpd
        uses: actions/upload-artifact@v2
        with:
          name: Thttpd_no_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/thttpd-2.29/3c_performance_stats/
          retention-days: 5

      - name: Build converted Thttpd
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_no_alltypes/thttpd-2.29
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make CC="${{env.builddir}}/bin/clang -w -ferror-limit=0" -k

  test_thttpd_no_expand_macros_alltypes_only_g_sol:
    name: Test Thttpd (not macro-expanded, -alltypes, greatest solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Thttpd
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol
          tar -xvzf ${{env.benchmark_tar_dir}}/thttpd-2.29.tar.gz
          for i in ${{env.benchmark_tar_dir}}/thttpd-2.29_patches/*; do patch -s -p0 < $i; done
          cd thttpd-2.29
          CC="${{env.builddir}}/bin/clang" ./configure
          chmod -R 777 *
          bear make CC="${{env.builddir}}/bin/clang -w -ferror-limit=0"

      - name: Convert Thttpd
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/thttpd-2.29
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of Thttpd
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/thttpd-2.29
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of Thttpd
        uses: actions/upload-artifact@v2
        with:
          name: Thttpd_no_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/thttpd-2.29/3c_performance_stats/
          retention-days: 5

      - name: Build converted Thttpd (filter bounds inference errors)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_g_sol/thttpd-2.29
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make CC="${{env.builddir}}/bin/clang -w -ferror-limit=0" -k 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py

  test_thttpd_no_expand_macros_alltypes_only_l_sol:
    name: Test Thttpd (not macro-expanded, -alltypes, least solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Thttpd
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol
          tar -xvzf ${{env.benchmark_tar_dir}}/thttpd-2.29.tar.gz
          for i in ${{env.benchmark_tar_dir}}/thttpd-2.29_patches/*; do patch -s -p0 < $i; done
          cd thttpd-2.29
          CC="${{env.builddir}}/bin/clang" ./configure
          chmod -R 777 *
          bear make CC="${{env.builddir}}/bin/clang -w -ferror-limit=0"

      - name: Convert Thttpd
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/thttpd-2.29
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of Thttpd
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/thttpd-2.29
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of Thttpd
        uses: actions/upload-artifact@v2
        with:
          name: Thttpd_no_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/thttpd-2.29/3c_performance_stats/
          retention-days: 5

      - name: Build converted Thttpd (filter bounds inference errors)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_only_l_sol/thttpd-2.29
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make CC="${{env.builddir}}/bin/clang -w -ferror-limit=0" -k 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py

  test_thttpd_no_expand_macros_alltypes_disable_rds:
    name: Test Thttpd (not macro-expanded, -alltypes, CCured solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Thttpd
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds
          tar -xvzf ${{env.benchmark_tar_dir}}/thttpd-2.29.tar.gz
          for i in ${{env.benchmark_tar_dir}}/thttpd-2.29_patches/*; do patch -s -p0 < $i; done
          cd thttpd-2.29
          CC="${{env.builddir}}/bin/clang" ./configure
          chmod -R 777 *
          bear make CC="${{env.builddir}}/bin/clang -w -ferror-limit=0"

      - name: Convert Thttpd
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/thttpd-2.29
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of Thttpd
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/thttpd-2.29
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of Thttpd
        uses: actions/upload-artifact@v2
        with:
          name: Thttpd_no_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/thttpd-2.29/3c_performance_stats/
          retention-days: 5

      - name: Build converted Thttpd (filter bounds inference errors)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_rds/thttpd-2.29
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make CC="${{env.builddir}}/bin/clang -w -ferror-limit=0" -k 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py

  test_thttpd_no_expand_macros_alltypes_disable_fnedgs:
    name: Test Thttpd (not macro-expanded, -alltypes, FuncRevEdges solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Thttpd
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs
          tar -xvzf ${{env.benchmark_tar_dir}}/thttpd-2.29.tar.gz
          for i in ${{env.benchmark_tar_dir}}/thttpd-2.29_patches/*; do patch -s -p0 < $i; done
          cd thttpd-2.29
          CC="${{env.builddir}}/bin/clang" ./configure
          chmod -R 777 *
          bear make CC="${{env.builddir}}/bin/clang -w -ferror-limit=0"

      - name: Convert Thttpd
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/thttpd-2.29
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of Thttpd
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/thttpd-2.29
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of Thttpd
        uses: actions/upload-artifact@v2
        with:
          name: Thttpd_no_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/thttpd-2.29/3c_performance_stats/
          retention-days: 5

      - name: Build converted Thttpd (filter bounds inference errors)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes_disable_fnedgs/thttpd-2.29
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make CC="${{env.builddir}}/bin/clang -w -ferror-limit=0" -k 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py

  test_thttpd_no_expand_macros_alltypes:
    name: Test Thttpd (not macro-expanded, -alltypes)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Thttpd
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes
          tar -xvzf ${{env.benchmark_tar_dir}}/thttpd-2.29.tar.gz
          for i in ${{env.benchmark_tar_dir}}/thttpd-2.29_patches/*; do patch -s -p0 < $i; done
          cd thttpd-2.29
          CC="${{env.builddir}}/bin/clang" ./configure
          chmod -R 777 *
          bear make CC="${{env.builddir}}/bin/clang -w -ferror-limit=0"

      - name: Convert Thttpd
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/thttpd-2.29
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --project_path .

      - name: Copy 3c stats of Thttpd
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/thttpd-2.29
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of Thttpd
        uses: actions/upload-artifact@v2
        with:
          name: Thttpd_no_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/thttpd-2.29/3c_performance_stats/
          retention-days: 5

      - name: Build converted Thttpd (filter bounds inference errors)
        run: |
          cd ${{env.benchmark_conv_dir}}/no_expand_macros_alltypes/thttpd-2.29
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make CC="${{env.builddir}}/bin/clang -w -ferror-limit=0" -k 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py

  test_thttpd_expand_macros_no_alltypes:
    name: Test Thttpd (macro-expanded, no -alltypes)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Thttpd
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes
          tar -xvzf ${{env.benchmark_tar_dir}}/thttpd-2.29.tar.gz
          for i in ${{env.benchmark_tar_dir}}/thttpd-2.29_patches/*; do patch -s -p0 < $i; done
          cd thttpd-2.29
          CC="${{env.builddir}}/bin/clang" ./configure
          chmod -R 777 *
          bear make CC="${{env.builddir}}/bin/clang -w -ferror-limit=0"

      - name: Convert Thttpd
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/thttpd-2.29
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of Thttpd
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/thttpd-2.29
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of Thttpd
        uses: actions/upload-artifact@v2
        with:
          name: Thttpd_expand_macros_no_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/thttpd-2.29/3c_performance_stats/
          retention-days: 5

      - name: Build converted Thttpd
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_no_alltypes/thttpd-2.29
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make CC="${{env.builddir}}/bin/clang -w -ferror-limit=0" -k

  test_thttpd_expand_macros_alltypes_only_g_sol:
    name: Test Thttpd (macro-expanded, -alltypes, greatest solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Thttpd
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol
          tar -xvzf ${{env.benchmark_tar_dir}}/thttpd-2.29.tar.gz
          for i in ${{env.benchmark_tar_dir}}/thttpd-2.29_patches/*; do patch -s -p0 < $i; done
          cd thttpd-2.29
          CC="${{env.builddir}}/bin/clang" ./configure
          chmod -R 777 *
          bear make CC="${{env.builddir}}/bin/clang -w -ferror-limit=0"

      - name: Convert Thttpd
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/thttpd-2.29
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-g-sol \
            --project_path .

      - name: Copy 3c stats of Thttpd
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/thttpd-2.29
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of Thttpd
        uses: actions/upload-artifact@v2
        with:
          name: Thttpd_expand_macros_alltypes_only_g_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/thttpd-2.29/3c_performance_stats/
          retention-days: 5

      - name: Build converted Thttpd (filter bounds inference errors)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_g_sol/thttpd-2.29
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make CC="${{env.builddir}}/bin/clang -w -ferror-limit=0" -k 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py

  test_thttpd_expand_macros_alltypes_only_l_sol:
    name: Test Thttpd (macro-expanded, -alltypes, least solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Thttpd
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol
          tar -xvzf ${{env.benchmark_tar_dir}}/thttpd-2.29.tar.gz
          for i in ${{env.benchmark_tar_dir}}/thttpd-2.29_patches/*; do patch -s -p0 < $i; done
          cd thttpd-2.29
          CC="${{env.builddir}}/bin/clang" ./configure
          chmod -R 777 *
          bear make CC="${{env.builddir}}/bin/clang -w -ferror-limit=0"

      - name: Convert Thttpd
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/thttpd-2.29
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-only-l-sol \
            --project_path .

      - name: Copy 3c stats of Thttpd
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/thttpd-2.29
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of Thttpd
        uses: actions/upload-artifact@v2
        with:
          name: Thttpd_expand_macros_alltypes_only_l_sol
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/thttpd-2.29/3c_performance_stats/
          retention-days: 5

      - name: Build converted Thttpd (filter bounds inference errors)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_only_l_sol/thttpd-2.29
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make CC="${{env.builddir}}/bin/clang -w -ferror-limit=0" -k 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py

  test_thttpd_expand_macros_alltypes_disable_rds:
    name: Test Thttpd (macro-expanded, -alltypes, CCured solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Thttpd
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds
          tar -xvzf ${{env.benchmark_tar_dir}}/thttpd-2.29.tar.gz
          for i in ${{env.benchmark_tar_dir}}/thttpd-2.29_patches/*; do patch -s -p0 < $i; done
          cd thttpd-2.29
          CC="${{env.builddir}}/bin/clang" ./configure
          chmod -R 777 *
          bear make CC="${{env.builddir}}/bin/clang -w -ferror-limit=0"

      - name: Convert Thttpd
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/thttpd-2.29
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-rds \
            --project_path .

      - name: Copy 3c stats of Thttpd
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/thttpd-2.29
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of Thttpd
        uses: actions/upload-artifact@v2
        with:
          name: Thttpd_expand_macros_alltypes_disable_rds
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/thttpd-2.29/3c_performance_stats/
          retention-days: 5

      - name: Build converted Thttpd (filter bounds inference errors)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_rds/thttpd-2.29
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make CC="${{env.builddir}}/bin/clang -w -ferror-limit=0" -k 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py

  test_thttpd_expand_macros_alltypes_disable_fnedgs:
    name: Test Thttpd (macro-expanded, -alltypes, FuncRevEdges solution)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Thttpd
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs
          tar -xvzf ${{env.benchmark_tar_dir}}/thttpd-2.29.tar.gz
          for i in ${{env.benchmark_tar_dir}}/thttpd-2.29_patches/*; do patch -s -p0 < $i; done
          cd thttpd-2.29
          CC="${{env.builddir}}/bin/clang" ./configure
          chmod -R 777 *
          bear make CC="${{env.builddir}}/bin/clang -w -ferror-limit=0"

      - name: Convert Thttpd
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/thttpd-2.29
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --extra-3c-arg=-disable-fnedgs \
            --project_path .

      - name: Copy 3c stats of Thttpd
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/thttpd-2.29
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of Thttpd
        uses: actions/upload-artifact@v2
        with:
          name: Thttpd_expand_macros_alltypes_disable_fnedgs
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/thttpd-2.29/3c_performance_stats/
          retention-days: 5

      - name: Build converted Thttpd (filter bounds inference errors)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes_disable_fnedgs/thttpd-2.29
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make CC="${{env.builddir}}/bin/clang -w -ferror-limit=0" -k 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py

  test_thttpd_expand_macros_alltypes:
    name: Test Thttpd (macro-expanded, -alltypes)
    needs: build_3c
    runs-on: self-hosted
    steps:
      - name: Build Thttpd
        run: |
          mkdir -p ${{env.benchmark_conv_dir}}/expand_macros_alltypes
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes
          tar -xvzf ${{env.benchmark_tar_dir}}/thttpd-2.29.tar.gz
          for i in ${{env.benchmark_tar_dir}}/thttpd-2.29_patches/*; do patch -s -p0 < $i; done
          cd thttpd-2.29
          CC="${{env.builddir}}/bin/clang" ./configure
          chmod -R 777 *
          bear make CC="${{env.builddir}}/bin/clang -w -ferror-limit=0"

      - name: Convert Thttpd
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/thttpd-2.29
          ${{env.port_tools}}/convert_project.py \
            --includeDir ${{env.include_dir}} \
            --prog_name ${{env.builddir}}/bin/3c \
            --extra-3c-arg=-alltypes \
            --expand_macros_before_conversion \
            --project_path .

      - name: Copy 3c stats of Thttpd
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/thttpd-2.29
          mkdir 3c_performance_stats/
          cp *.json 3c_performance_stats/

      - name: Upload 3c stats of Thttpd
        uses: actions/upload-artifact@v2
        with:
          name: Thttpd_expand_macros_alltypes
          path: ${{env.benchmark_conv_dir}}/expand_macros_alltypes/thttpd-2.29/3c_performance_stats/
          retention-days: 5

      - name: Build converted Thttpd (filter bounds inference errors)
        run: |
          cd ${{env.benchmark_conv_dir}}/expand_macros_alltypes/thttpd-2.29
          if [ -f "out.checked" ]; then cp -r out.checked/* . && rm -r out.checked; fi
          make CC="${{env.builddir}}/bin/clang -w -ferror-limit=0" -k 2>&1 | ${{github.workspace}}/depsfolder/actions/filter-bounds-inference-errors.py
